# Shelf Life — Roadmap

Ranked by priority, highest first within each tier — no longer organized by where an idea came from (Taylor, outside feedback, Claude). When it's time to pick what's next, work top to bottom. Nothing here is committed or scheduled, it's still a menu, just a sorted one now. Shipped features live in `CHANGELOG.md`.

## Waiting on Apple

**App Store submission resubmitted — waiting on a fresh review, nothing left to build here.** Second rejection was **Guideline 4.1(a) Copycats**: "the app's screenshots includes references to Pokemon," creating a misleading association with Pokémon's IP. Two separate Pokémon references got found and fixed across two passes: `app/page.js`'s logged-out home page hero was pulling a real, live-fetched Charizard VMAX card (via `lib/showcaseCovers.js`) into the trading-card showcase slot, publisher field literally reading "Pokémon" — that slot now uses fully generic, made-up card details ("Rare Holo," "First Edition," "Trading Card Co.") and fetches no image at all, and the one Pokémon-referencing example row in the downloadable CSV import template got swapped to the same generic details. Separately, the actual App Store Connect screenshots Apple reviewed were checked directly (Aug 27) and confirmed clean — no Pokémon or other third-party-IP references across all 7 iPhone and 6 iPad screenshots. The stale submission from Aug 12 (which had sat in a dead "Waiting for Review" state with no actual review activity in 15 days — editing metadata alone doesn't restart a review pass) was canceled and a real new submission went in same-day, now genuinely queued for a first look. Normal turnaround from here is 24-72 hours; nothing to do until Apple responds. If it's rejected again, bring the exact rejection message (guideline number + reviewer notes).

## Needs a quick action from you — nothing left for me to build here

1. **White border around the app icons** — a visual issue with the icon artwork itself (likely transparency/safe-zone padding in the source PNGs), not something fixable in app code — needs corrected icon art.
2. **Domain name** — `shelflife.gg` or a `.com` over the current `.site`, per outside feedback, for credibility/memorability. A budget-and-branding call, not mine to make.
3. **Google Play Store release** — queued directly behind the App Store, deliberately not started yet: same PWA-wrapping approach as iOS, just for Android (most likely PWABuilder's Android/TWA output). The moment Shelf Life is live on the App Store, this jumps to the top of the buildable list below.
4. **Passkey sign-in doesn't work in the wrapped app** (confirmed on TestFlight) — needs Apple's Associated Domains entitlement in Xcode plus a `/.well-known/apple-app-site-association` file on the domain declaring the app's real Team ID + Bundle ID. I can build the website-side file the moment you give me those two values from the signed Xcode project; the Xcode capability itself is on you either way.
5. **AdSense** — a signup + env var whenever you want ads live (deliberately off for this submission). Worth knowing going in: AdSense's policy blocks plain AdSense inside a native app wrapper entirely, so ads would need to be suppressed specifically inside the iOS app rather than risk a policy violation — meaning app users generate $0 ad revenue under this setup even once it's on for the website.
6. **External cron watchdog — the one piece of "cron heartbeat" that isn't buildable from here** — `/admin/stats` now shows a "Cron jobs" section (last run time + status per job, backed by a new `cron_runs` table each cron upserts on completion), and `lib/cronAlert.js` already emails you when a job runs and errors. What neither can catch is a cron that never fires at all — a `vercel.json` config change, a broken build, a Vercel Cron outage — because nothing runs to report the silence. Closing that needs a free dead-man's-switch service outside Vercel entirely (healthchecks.io or Cronitor both have no-cost tiers): sign up, create a check for each of the now 7 cron jobs (`price-drop-check`, `process-account-deletions`, `refresh-currency-rates`, `weekly-stats-digest`, `email-data-backup`, `refresh-master-sets`, and the new `email-activity-digest` — see `CHANGELOG.md`) with its expected schedule, and send me the 7 ping URLs — I'll wire one ping call into each route.
7. **Verify the site in Google Search Console and submit the sitemap** — now that `sitemap.js` includes the 4 new SEO landing pages (`/video-game-collection-tracker`, `/comic-collection-app`, `/collectible-database`, `/retro-game-inventory` — see `CHANGELOG.md`), the last step to actually get them crawled/indexed is the one Google-account step from README.md's "Get found in search" section (verify ownership, submit `sitemap.xml`) — not something I can do on your behalf.
8. **Remaining mobile bug: auto-zoom on opening a page (Sep 2026) — investigated thoroughly, couldn't reproduce or find a code-level cause yet, needs more from you to go further.** (The profile mosaic looking like "a load of squeezed together photos," reported alongside this one, is now fixed — see `CHANGELOG.md`; Taylor's screenshots showing the real issue were the key to finding it.) For the zoom issue specifically: checked and confirmed correct the `viewport` export in `app/layout.js` (`width: 'device-width', initialScale: 1` — the exact fix for a past "everything looks zoomed in" bug, still intact and the only `viewport` export in the app) and the site-wide `overflow-x: hidden` safety net on `html`/`body` in `app/globals.css` (not overridden by the new bottom-nav CSS). Everything checkable from here says the shipped code is correct, but that was also true of the mosaic bug right up until real screenshots showed what was actually different from the design intent — so the same is likely true here: I need a current screenshot (or screen recording) of a page opening zoomed in, which specific page(s) it happens on, and what device/browser, to actually find this one.

## High priority

- **Widened `games` RLS policy for `wishlist_public` technically permits wishlist rows through *any* games query, not just the gift list page** — noticed while building it (see `CHANGELOG.md`): the policy change is scoped to `ownership = 'wishlist'` rows only, and both existing views that query another user's `games` (`/compare/[username]`, the shelf mosaic) were checked and are safe — both already gate on `is_public` at the app level before ever running the query, so nothing leaks today. But any *future* feature that queries `games` for a non-owner without an explicit `is_public` check would now silently see that profile's wishlist items even when the profile is otherwise private. Worth keeping in mind rather than a build item on its own — flagging so it isn't a surprise later.
- **Worth watching: crowdsourced series matching now fetches every row of the item type, not just matches** — noticed while adding normalization to `lib/seriesCrowdsource.js` (see `CHANGELOG.md`): merging near-duplicate series names (punctuation/spacing differences) properly means comparing normalized text in JS, which can't be done as a server-side exact match — so it now pulls every row of that `item_type` and filters client-side instead of letting Postgres narrow it down first. Fine at today's scale (Funko Pops only, RLS-scoped to public profiles plus the viewer's own), but would need a real search index if this ever handled a type with meaningfully more rows.
- **Worth watching: the full release catalogue falls back to loose name matching for a platform IGDB can't resolve at all** — "owned" matching (`lib/platformCatalogueMatch.js`, see `CHANGELOG.md`) runs through real IGDB platform ids now, not free-typed name comparison, so common abbreviations ("PS2" vs. "PlayStation 2") match correctly. The one case this doesn't cover: a platform IGDB has no record of whatsoever — a genuinely obscure/regional/bootleg system — resolves to no id on either side, which falls back to the original loose substring comparison. Not expected to come up often (IGDB's platform coverage is broad), but worth knowing if a specific report of a missed match ever comes in.
- **Bug: Full release catalogue shows "No results" for every platform, not just an edge case — flagged directly with a screenshot (Nintendo Switch), confirmed broader than one platform. Narrowed down significantly (Sep 2026) — a temporary diagnostic is deployed, waiting on one real reproduction to close it out.** Checked this properly rather than guessing at a fix: production logs (`get_runtime_logs`/`get_runtime_errors` for `/api/igdb-platform-catalogue`) show real `200` responses with no errors recorded, and `CatalogueClient.jsx` already renders a distinct message for a misconfigured IGDB key (`data.error === 'not_configured'`) or an unresolvable platform name (`data.error === 'platform_not_found'`) — neither of those is what showed on screen, ruling out the two most likely causes. That leaves the actual IGDB query in `lib/igdbPlatformCatalogue.js`'s `getPlatformCatalogue()` — `where platforms = (${resolved.id}) & category = 0;` on the `/v4/games` endpoint — as the real suspect. **New this round:** hit the live production API directly (`mcp__Vercel__web_fetch_vercel_url`, which reaches the real deployment even though this sandbox still has no general network path to IGDB itself) and confirmed the bug is real and current: `/api/igdb-platform-catalogue?platform=Nintendo%20Switch` correctly resolves the platform (`platformId: 130`) but returns `games: []` — including with `&search=Zelda` added, which should have surfaced *something*. Cross-checked against `/api/igdb-search?q=Breath%20of%20the%20Wild`, which correctly shows game id 7346 with `"Nintendo Switch"` in its `platforms` list — so a game that unambiguously belongs to this exact platform/id still doesn't come back from the catalogue query. Also checked the official Apicalypse filter spec (apicalypse.io) directly: `where field = (x)` is documented as "contains at least one of" for an array field, which is the semantic this query wants and looks like it should already be correct — meaning the bug is more likely in `category = 0`, the resolved id itself being subtly wrong for this query context, or something not visible from outside the request/response cycle, not a wrong choice of bracket type. Rather than guess further or risk a wrong fix, added a **temporary diagnostic-only `console.error`** in `getPlatformCatalogue()` (logs the exact query string and raw IGDB response body, only when the result comes back empty — doesn't change what any user sees). **Next step to actually close this out, once this diagnostic build is live:** reproduce once (load `/dashboard/catalogue` for any platform, or I can hit the API directly), then read the log back via `get_runtime_logs` — that'll show the real IGDB response body directly instead of guessing further. Remove the diagnostic block once the real fix ships. Whole feature is non-functional until this is fixed — every platform, not a rare gap.

## Medium priority

- **Image handling for cover art — half closed.** The "at least validate/gracefully degrade" half is done (see `CHANGELOG.md`): every cover-rendering spot site-wide now falls back to a placeholder instead of the browser's default broken-image icon when a URL is dead. Still open: actually downloading and re-hosting cover art in Supabase Storage instead of trusting external URLs long-term — a real infra/cost decision (storage scales with usage), not a quick follow-up.
- **The public profile's own "More actions" menu is the same hidden-features pattern the dashboard sidebar just fixed, one size down** — flagged directly with a screenshot of a profile's "..." button open showing just "Shelf mosaic." Checked the actual menu (`app/u/[username]/page.js`, rendered via `ActionMenu.jsx`): it's not really a 1-item menu — a viewer sees up to 5 conditionally-shown items (Compare collections, Shelf mosaic, Gift list, Refresh prices, Report profile), an owner sees up to 4 (Shelf mosaic, Gift list, Manage showcase, Custom lists) — most just happened to be hidden in that screenshot by their own visibility conditions (`owned > 0`, `wishlistCount > 0`, etc.), same real shape as the old dashboard dropdown before it grew to 9 items. Not urgent — a public profile's actions aren't things you reach for daily the way dashboard tools are, and this hasn't been flagged as confusing on its own — but worth a similar look (small persistent buttons for the couple of things every profile actually has, an overflow menu only for the conditional extras) if it ever gets the same "too many things buried" complaint the dashboard did.
- **Public profile still visually reads as a copy of the dashboard — the real follow-up from deciding to keep My Collection and My Profile as two separate destinations in the new phone bottom bar (Sep 2026, see `CHANGELOG.md`).** Confirmed directly: both pages render the same collection grid and a similar stats-bar layout, which is the actual reason they feel redundant even though they serve genuinely different purposes (Dashboard = editing/management, Profile = public/social — followers, comments, Trophy Case, showcase). Restyling the profile's collection view to lead with the social content instead of the grid (or at least visually distinguish it — different card treatment, social stuff above the fold, grid secondary) would make the two feel like two different pages again instead of needing to know their internals to tell them apart. Not attempted yet — decided to keep both bottom-bar icons as-is for this round and revisit the actual visual distinction separately.
- **Notification bell isn't reachable from the phone bottom bar** — noticed while building it (see `CHANGELOG.md`'s bottom-nav entry): the bell still only lives inside the hamburger menu, same as before, so on a phone it now takes one extra tap (open the ☰ menu) to check notifications instead of the one-tap access the 4 bottom-bar items get. Not a regression (it was already behind the hamburger before this shipped) but worth watching — if it ever gets flagged as "I keep forgetting to check my notifications on my phone," it's a strong candidate for a 5th bottom-bar slot (a bell icon with the existing unread-count badge) rather than a full redesign.
- **App icon refresh to the new navy/gold/mint palette** — flagged directly in `CHANGELOG.md`'s visual design pass: the whole site reskinned to the brief's navy/gold/mint brand this round, but `app/icon.png`/`app/apple-icon.png` (the actual installed-app icon) still show the old blue palette, deliberately left alone per the brief's own "for later" framing on the app-icon direction. Also feeds `app-store-checklist.md`/App Store Connect's uploaded screenshots and the 1024×1024 App Store icon, so a refresh needs a careful, deliberate pass (new artwork, regenerate every derived size, re-check nothing in the App Store submission goes stale) rather than a quick swap folded into a CSS-only round.

**The item below (plus one more, in "Later" further down) came out of an actual competitor pass (Aug 2026) — Discogs, CLZ/Collectorz, iCollect Everything, Collectr, TCGplayer, League of Comic Geeks, Backloggd, and Grouvee. Full comparison shared in chat; short version: nobody else covers this much breadth *and* real social features *and* real per-type completion data at once, but a few of them each have one specific thing worth borrowing. Three of the original four shipped (see `CHANGELOG.md`: the `/lists` directory closes the Backloggd/Grouvee "browsable community lists" gap, aggregate ratings now show on both the collectible detail page and search/browse tiles closing the Backloggd crowd-rating gap — the earlier note claiming the detail page had nothing was actually stale, it's had an avg rating stat since the page itself shipped, the browse-tile visibility was the real remaining gap — and the Upcoming Releases page closes the League of Comic Geeks "upcoming-release calendar with spend forecasting" gap).**

- **Single-item AI photo auto-fill** — iCollect Everything's standout feature, and a much smaller, more provably-accurate slice of the "AI shelf-photo bulk import" idea already below: point the camera at one item, get title/cover/details auto-filled, instead of a whole shelf at once. Worth doing this first and learning from real accuracy before attempting the harder multi-item version. **Checked what this actually needs to build (Aug 2026):** the camera-capture and Supabase Storage upload plumbing already exists, reusable from `GameModal.jsx`'s condition-photo upload — but there's no vision/image-recognition API configured anywhere in this project today — no Anthropic/OpenAI/Google Vision key, nothing in `.env.local.example`. This is genuinely blocked on you adding a new AI vision API key (and accepting its per-call cost) before it's buildable, not something with a free/already-configured path the way the rest of this tier has been. (Note: barcode scanning, mentioned here in earlier drafts of this note as a source of reusable camera-access code, was removed — see `CHANGELOG.md` — since per-type title search covers auto-fill with better coverage than a UPC lookup ever had; any future camera-capture work here would be built fresh, not adapted from that.)

## Later (bigger, more design work)

- **Shared/household collections** — let a second account co-manage one collection. Real permission/ownership questions to work through, not a quick add.
- **Multi-language support (i18n)** — everything's English-only, fine for a friends-and-family project but a real ceiling for an actual App Store product. Worth it once there's a specific market to justify it.
- **AI shelf-photo bulk import** — one photo of a physical shelf, detect and suggest multiple items at once instead of searching or typing each one in by hand. The most speculative idea here — accuracy on a photo full of spines needs real testing before promising it works.
- **Import from Goodreads / Discogs** — same idea as the existing Steam import, for Books and Vinyl/CDs respectively.
- **Live currency conversion — narrowed to what's left.** The leaderboard piece is done (see `CHANGELOG.md`): the "Most valuable" podium now shows a converted figure alongside each collector's own-currency total — in the viewer's own preferred currency (Settings > Currency), not a fixed USD — using the exact rate data the ranking itself already sorted on. Still open: everywhere else display stays raw-currency-only (your own dashboard total, a public profile's "Collection value" stat, per-item prices) — real conversion there needs the same "what's the true underlying value" product call this line always flagged, since (unlike the leaderboard, which only ever needed one converted number to sort by) changing what a total *displays* touches something someone might actually budget against.
- **Xbox / PlayStation account integration** — Steam is done. Xbox is gated behind app registration; PlayStation has no official public API at all (reverse-engineered access only, fragile and ToS-gray-area).
- **Global chat** — a site-wide message board, separate from per-profile comment walls. Needs real-time updates and real moderation once it's not just friends talking.
- **More Shelf Life milestone variety — narrowed to what's left.** Platform-completionist, genre-breadth, and 5 oddball badges (tagging, notes, condition photos, for-sale, sold) all shipped (see `CHANGELOG.md`). Still open: decade-spanning badges — `games` has no original-release-date field to compute a decade from (only `purchase_date`, when *you* bought it, not when the item itself came out), so this would need per-item release-year enrichment from IGDB/Comic Vine/etc. stored on the row, a real schema addition, not a quick follow-up.
- **Even more collectible types** — board games, action figures/toys, coins, whenever there's demand.
- **iPad-specific polish** — the app went in as a Universal build (phone layout scaled up); a real iPad layout would be a nice follow-up if iPad usage turns out meaningful.
- **Native ad revenue from the app itself (AdMob)** — even with AdSense on for the website, the app earns $0 on its own; real native integration (AdMob SDK or Google's WebView Ads API) needs genuine Swift/native work. Not worth it at current traffic.
- **In-app marketplace: sell to another collector directly, not just an affiliate redirect** — distinct from the affiliate "buy it" links and the "For sale" flag, both already shipped (see `CHANGELOG.md`): those point outward or just show interest, this would be an actual listing another collector could act on inside Shelf Life. Real trust, payment, and dispute questions come with that — easily the biggest-scope item on this list. Worth starting with "list for sale + a way to message the seller" long before anything resembling in-app payments.
- **Native home-screen widgets (iOS/Android)** — today's stats, Play Next, or trophy progress right on the home screen, now that the app is wrapped for both platforms. Needs genuine native work (WidgetKit on iOS, App Widgets on Android) — same caveat as AdMob above, not buildable from this codebase alone.
- **Browser extension: add an item while shopping** — a small "Add to Shelf Life" extension that captures a title/price/image straight off an eBay/Amazon/retailer listing page instead of typing it in from memory later. A genuinely new platform surface — its own codebase, its own store listing and review process (Chrome Web Store this time, not Apple's or Google's).
- **Community-verified master sets** — master-set data currently comes entirely from TCGdex/Comic Vine/IGDB, or Shelf Life's own crowdsourced logs; a "flag this as wrong" or trusted-contributor correction layer would start to matter once master sets cover enough collectible types that data errors actually show up. Same flavor of real moderation/trust question as Global chat above.
- **Real sold-price data, not just current listings** — same competitor pass: Collectr and TCGplayer both lean on actual transaction/sold-price data as their credibility anchor for "what's this actually worth." Shelf Life's own eBay price check is explicitly current active-listing prices, not sold comps (a known, already-documented limitation — see `CHANGELOG.md`), because eBay stopped offering free access to sold-listing data. Flagging honestly rather than pretending it's a clean build: closing this gap for real needs either a paid market-data source or something against eBay's ToS, so it's more of an "if the budget ever supports it" item than a normal build.
- **Upcoming Releases for the remaining collectible types** — checked while building the Upcoming Releases page (Sep 2026, see `CHANGELOG.md`): games and comics are covered (IGDB/Comic Vine both expose real future dates). Trading cards are the only other type where this might be reachable — Scryfall and TCGdex both have a real release-date field on their Set objects — but two real open questions before it's worth attempting: (1) neither's docs confirm whether a not-yet-released set is actually listed ahead of time the way IGDB/Comic Vine list unreleased games/issues, only that the field exists on sets that have already released, and (2) the feature shape is genuinely different from games/comics — you can't own a card from a set that hasn't released yet, so "upcoming" here would have to mean "a new set is coming for Pokémon/Magic, the TCG(s) you collect," not "the next item in a series you already own," which is a coarser per-TCG signal rather than a per-series one. Funko Pops, Vinyl, CDs, DVDs, VHS, and Books have no path at all — same wall already hit for master-set/series lookups on these types (see `lib/seriesCrowdsource.js`'s module comment and the "Real master sets" entries in `CHANGELOG.md`): no free, live, per-item database exists for any of them, so there's no future-date field to even check. Not worth building on a guess; worth revisiting trading cards specifically if either API's actual behavior around unreleased sets ever gets confirmed, or if a new source appears for the rest.

## Accessibility (full list)

The modal/dropdown pass (keyboard operability, focus trapping, `role="dialog"`) covered what was flatly unusable without a mouse. The highest-impact items from this list were promoted to High priority above; this is the complete set for reference.

**Screen readers (VoiceOver / TalkBack)** — ~~site-wide `aria-label` audit on icon-only buttons~~, ~~`aria-live` regions for toasts~~ (done — toasts, CSV/Steam-import progress readouts, and now `TrophyToastListener`'s trophy-earned popup too), ~~real `<label for="">`/`id` pairing on every form field~~, and ~~meaningful `alt` text on cover art/avatars~~ are done (see `CHANGELOG.md`). Still open: a text-based "view as a list" alternative for the shelf mosaic; proper landmark regions (`<nav>`/`<main>`/`<aside>`) and a clean, non-skipping heading hierarchy.

**Keyboard-only navigation** — ~~a skip-to-content link~~ (done); ~~real keyboard support for the star rating~~ (done — arrow keys step the rating up/down, Enter/Space already worked via the underlying buttons); ~~confirming the browser's default focus outline is never suppressed without a replacement~~ (audited — the one real gap found, `.chip-input`, is fixed; `.mosaic-tile`'s `outline: none` already had a proper `:focus-visible` replacement); ~~a full tab-order check across the dashboard grid, leaderboard, and search results~~ (done — found and fixed a real mismatch on the leaderboard podium, where CSS visually reordered 1st/2nd/3rd place but the DOM/Tab order didn't follow; dashboard grid and search results were already clean, see `CHANGELOG.md`).

**Low vision / visual** — ~~a WCAG AA contrast audit~~ (done — `.sub` muted text and main body text both measured comfortably above 4.5:1 in both themes already; the real bug was `--owned`/`--sold`/`--tier-bronze`/`--tier-silver`/`--tier-gold` silently going unset in the light theme and inheriting dark-tuned values as low as 1.58:1 — now fixed, see `CHANGELOG.md`); ~~differentiating status without relying on color alone~~ (done — checked ownership badges, podium medals, and trophy tiers; all three already render the status as literal text, not color alone, no fix needed); ~~confirming pinch-to-zoom is never disabled~~ (done — `app/layout.js`'s viewport export never set `maximumScale`/`userScalable: false`, nothing to fix); ~~a manual text-size control~~ (done — see `CHANGELOG.md`; CSS `zoom` sidestepped the "no root rem/em scale" obstacle entirely rather than needing every hardcoded `font-size: NNpx` declaration rewritten, and it was visually verified across several pages/viewports on a real dev server before shipping, per this line's own earlier caution).

**Other** — ~~a longer or pause/extendable undo window~~ (done — the Undo-delete toast now pauses its 6-second countdown on hover or keyboard focus and restarts a full window when you look away, rather than a flat unpausable one); ~~a public `/accessibility` page matching the existing `/privacy`/`/feedback` pattern~~ (done, linked in the footer) — still open: the kind of thing that helps if the App Store's Accessibility Nutrition Label answers ever need revisiting with real, tested claims instead of "not verified yet."

## Paid / premium ideas (business decisions, not just builds)

Everything Shelf Life does today is free, and there's no payment processor wired up anywhere in this codebase — before any single idea below is buildable, that has to exist first: most likely Stripe given the Next.js/Vercel/Supabase stack, plus a `subscriptions` table, webhook handling, and an entitlements check gating whichever features end up behind it. That's real work on its own, on top of the actual pricing/tier design, which isn't something I can decide — these are flagged here as ideas worth having on file, not prioritized or sequenced like the tiers above.

- **A Pro subscription tier** — the prerequisite for everything else in this section (payment processor + entitlements, described above). Worth deciding what actually goes behind it before building any of it — probably some mix of the ideas below rather than one single "upgrade" button.
- **Real sold-price data** — already flagged in the "Later" tier above as a known gap (Shelf Life's eBay price check is current listings only, since eBay stopped offering free access to sold-listing data). A paid collectibles pricing API (PriceCharting, CardLadder, or similar) could close it for real, but the ongoing per-lookup cost only pencils out behind a Pro tier or a pass-through charge.
- **PSA/CGC population data for graded items** — already a Medium-priority idea (trading cards/comics already have a Grade field; showing how rare that exact grade is would be a nice addition). The reliable, complete population-lookup data on PSA's/CGC's own side tends to sit behind their own paid tiers, so this is really the same "needs a paid upstream data source" shape as sold-price data above.
- **AI photo/shelf auto-fill** — already flagged elsewhere in this file as blocked without a new AI vision API key (nothing like that is configured today, and it'd carry a real per-call cost). A natural Pro-tier perk once billing exists, rather than something offered free to every account.
- **Collection appraisal/insurance PDF export** — the free version has already shipped (see `CHANGELOG.md`: a print-optimized `/dashboard/appraisal` page). A paid tier could go further — an actual generated PDF file (not just print-to-PDF) with a real letterhead/signature block, or bundling in condition photos per item — rather than gating the basic export itself.
- **Boosted/featured "For sale" listings** — the "For sale" flag itself has shipped (see `CHANGELOG.md`); once the in-app marketplace (Later tier) also ships, a "boost this listing" placement — similar spirit to eBay's promoted listings — would be a natural, low-effort monetization hook layered on top of features that are already worth building for their own sake.
- **Bigger photo/storage limits** — condition photos are capped at 4 per item today; Supabase Storage cost scales with actual usage, so a Pro tier raising that cap (more condition photos, higher-res uploads) ties a real infra cost directly to a real, easy-to-understand perk.
- **Ad-free browsing for logged-in users** — AdSense on the website is already planned but not yet live (see the "Needs a quick action from you" section above). Once it is, removing ads for paying accounts is a standard, low-build-effort Pro perk to pair with it.
- **A vanity profile handle or "verified collector" badge** — purely cosmetic, no ongoing cost once billing exists, and the safest first thing to ship behind a Pro tier before committing to any of the pricier, ongoing-cost ideas above.
- **Public read-only API access — narrowed now that it's shipped (see `CHANGELOG.md`).** Personal access tokens for pulling your own collection into a spreadsheet, dashboard, or script are live today, free, rate-limited to 60 requests/minute per token. Still open: whether a higher/uncapped rate limit (or usage past some volume) should become a Pro perk rather than staying free for every account — a real ongoing-infra and support-surface question, worth deciding deliberately rather than assuming either way.

## Not building (decisions, not build items)

- **Promotion channels** (TikTok, Reddit, YouTube, Discord, Product Hunt) — generic startup-marketing playbook from the outside review, not specific to Shelf Life. Worth doing eventually, just not a build item for me.

---

Nothing on this list needs to happen in order beyond the tiers themselves — treat each tier as sorted, the tiers as sorted relative to each other. When you're ready to build the next thing, just point me at whichever line sounds most useful, or say "pick the next 3" and I will, top to bottom.
