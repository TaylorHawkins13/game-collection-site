# Shelf Life — Changelog

Everything that's shipped, roughly in the order it was built. See `ROADMAP.md` for what's still to do.

- **Comics support** — track comic books alongside games in the same collection: series, issue number, publisher, writer/artist, CGC-style grade, variant covers.
- **Rebrand to Shelf Life** — new name across the app.
- **Currency selection** — pick a display currency in Profile Settings instead of being locked to USD (display only, no live conversion between currencies).
- **Avatar upload** — upload a photo directly (via Supabase Storage) instead of only pasting an image URL.
- **Per-type stats** — dashboard stats bar shows a Games/Comics split.
- **Find Collectors search page** — search public profiles by username/display name, with a default "recently joined" list.
- **Cover images fit the frame** — switched from cropping covers to fill the box to showing the whole image, letterboxed if needed.
- **Edit profile shortcut** — your own profile page now has an "Edit profile" button that jumps straight to Profile Settings.
- **Trophies** — a PlayStation Trophies-style achievement system: bronze/silver/gold/platinum badges for collection milestones (first item, 10/100 items, 25 completed, follower counts, variant hunting, etc.), shown as a trophy case on public profiles. Awarded server-side so they can't be gamed.
- **Mobile layout pass** — smaller card grid, scaled-down type, and a decluttered card layout on phones so nothing feels oversized or cramped.
- **Top Trumps-style card redesign** — full-bleed cover art, a color nameplate pulled from the art itself, and a grading-slab-style stat block instead of loose badges.
- **More collectible types** — Trading Cards, Vinyl Records, Books, DVDs, and CDs added alongside Games and Comics (seven types total), each fully separate with their own tailored fields.
- **Home page redesign** — split hero with a card showcase, alternating value-prop rows built from the app's real trophy/leaderboard styling instead of generic template blocks.
- **Switched game auto-fill from RAWG to IGDB** — RAWG had become unreliable (widely reported as unmaintained, frequent downtime). Auto-fill now runs through IGDB via a server-side route that keeps the Twitch client secret private.
- **Barcode scanning via phone camera** — a "Scan" button next to the Barcode/UPC field opens the camera, reads the barcode, and looks it up to auto-fill title/cover (and author/artist/publisher where available): Open Library for books, a general UPC database for everything else. Works on Safari/iOS as well as Android/desktop. Coverage isn't universal — not every barcode is in either database, especially for older or niche items — and the free UPC lookup is capped at 100 lookups/day site-wide.
- **Duplicate/copy an item** — a "Duplicate" button when editing an item opens a new Add form pre-filled with everything except the barcode, so adding a similar item (another card from the same set, another platform's copy of a game) doesn't mean retyping every field.
- **Trading card search** — for trading cards, a Search button next to Title looks up the typed name on free Pokémon TCG and Magic: The Gathering databases to auto-fill set, card number, cover art, and publisher. (A camera-based "Scan Card" version using on-device OCR was tried and pulled back out — reading card names off photos was unreliable enough to be more frustrating than useful. Typing the title and hitting Search works well instead.)
- **Autocomplete from your own collection** — fields like Publisher, Artist, Genre, Card Set, and Platforms now suggest values you've already typed before, pulled from your existing items. The more you add, the more it starts filling itself in — no typing "Marvel" or "Nintendo Switch" from scratch every time.
- **Community suggestions while adding an item** — as you type a title, if anyone else has already added a matching item (or you have, previously), it shows up in a "already in the community" list — click it and it fills in cover, platform, publisher, and the rest from their entry instead of typing it all out. Only ever pulls from your own items or from public profiles, using the same privacy rule that already keeps private collections out of the leaderboard.
- **Browse by system on the dashboard** — a row of big tappable tiles above the filter bar, one per platform you own games for, each showing how many items you've got, color-coded by platform brand. Tap one to filter straight to that system; tap again (or Clear) to go back to everything.
- **Clickable followers/following lists** — the follower/following counts on a profile are now links to a full list of who's actually there, each linking through to their own profile. Same privacy behavior as the counts already had (visible regardless of whether the collection itself is public).
- **Region tags for games** — NTSC-U/C, NTSC-J, PAL, or Region-Free as a field on games, shown on the card and searchable. Requires `region-migration.sql` on existing projects.
- **Physical/Digital tag** — a "Copy" field (Physical or Digital) works on any item type, shows as a badge on the card, and has its own dashboard filter. Requires `copytype-migration.sql` on existing projects.
- **Check eBay price** — a button in the item form looks up current eBay (US) listings for that item and shows the low/average/high asking price, which then shows on the card too. This is *current active listings*, not confirmed sale prices — eBay doesn't offer free public access to sold-listing data anymore, so this is "what it's going for right now" rather than a guaranteed resale value. On-demand only (you click Check, it doesn't run automatically), free with an eBay developer account. A "Refresh all prices" button on the dashboard runs the same check across your whole collection in one go (skipping sold items), with a progress readout and a Stop button since a big collection takes a bit. Requires `ebayprice-migration.sql` on existing projects, plus `EBAY_CLIENT_ID`/`EBAY_CLIENT_SECRET` in `.env.local` (see README).
- **Completeness field for games** — Loose (cart/disc only), CIB (complete in box), or Box only (no manual), as its own field separate from general condition. Feeds directly into the eBay price check too — completeness swings a game's real resale value a lot (a loose cart vs. a CIB copy of the same game can be several times apart in price), so the price lookup now searches with the right term included instead of averaging loose and CIB listings together. Requires `completeness-migration.sql` on existing projects.
- **CSV/spreadsheet import** — an "Import CSV" button on the dashboard bulk-adds items from a spreadsheet instead of one at a time. Includes a downloadable template with the right columns and an example row for every item type. Shows a preview of what's about to be added plus any warnings (unrecognized item type, bad date, etc. — those rows still import with sensible defaults rather than failing outright), then imports in batches with a progress readout.
- **"100% complete" tag** — a checkbox on any item ("all extras/achievements done, a full series or set collected, etc.") beyond the existing play-status/condition fields. Shows as a badge on the card, has its own dashboard filter, and is importable via CSV. Requires `fullycompleted-migration.sql` on existing projects.
- **Collection value over time chart** — a mini chart on the dashboard tracking your estimated collection value (last "Check eBay price" result where you've checked one, purchase price otherwise) across snapshots in time. A snapshot is recorded automatically every time "Refresh all prices" finishes, or manually via a "Record snapshot" button — needs at least two snapshots before a trend line shows up. eBay prices are always USD, so if your display currency isn't USD the total is a mix and only approximate. Requires `valuesnapshots-migration.sql` on existing projects.
- **SEO/discoverability basics** — a real `sitemap.xml` (home, Find Collectors, Leaderboard, and every public profile) and `robots.txt` (keeps the login-gated dashboard and auth pages out of search, points crawlers at the sitemap), a branded social-share preview image that shows up when a link gets posted to Discord/Reddit/iMessage/etc., and proper page titles/descriptions site-wide including per-collector ones on public profiles (e.g. "See Alex's collection — 214 items and counting"). None of this needs anything from you to keep working — it updates itself as people join and make their profiles public.
- **First-time onboarding panel** — a brand-new, empty collection now shows a real welcome panel instead of a bare "No items yet" line: what Shelf Life is, and three clear ways to add a first item (by hand, barcode scan, or CSV import), plus a link to browse other collectors for inspiration. Matters more now that search/shared links might bring in strangers, not just friends who already know how the site works.
- **Fixed silent failures on follow/comment/delete** — found during a pass on loading/error states: following/unfollowing, posting a comment, and deleting an item all failed with zero feedback if something went wrong (and Follow's button could get visually stuck as "Following" even when the database call actually failed). All three now show a small error message (a new toast in the bottom-left corner) instead of quietly doing nothing.
- **Mobile pass: nav, tap targets, toolbar** — the navbar collapses behind a hamburger menu on phones instead of wrapping into 2-3 messy lines; buttons and form fields got a 44px minimum tap height (were as small as ~34px); the dashboard's filter toolbar stacks full-width on narrow screens instead of wrapping into a ragged grid.
- **Consolidated dashboard filters** — the 6 filter dropdowns (Type, Status, Copy, Completeness, Platform, Play status) are now tucked behind a single "Filters" button instead of always taking up a row, with a count badge when any are active and a "Clear all filters" shortcut.
- **Share your shelf** — a "Share my shelf" button on your own public profile. On a phone it opens the native share sheet (Messages, WhatsApp, Mail, etc.); on desktop it copies your profile link to the clipboard.
- **Profile showcase** — a "Manage showcase" button right on your own public profile lets you pin up to 5 favorite items, in whatever order you like, to a dedicated Showcase section at the top — above the full collection grid, styled distinctly (gold border, "Featured" flag) so it stands out, laid out 5 to a row. Requires `showcase-migration.sql` on existing projects.
- **Activity feed** — a new `/feed` page (linked from the navbar as "Feed") shows what the collectors you follow have recently added, finished, or rated — plus a "What's New" sidebar listing recent site updates. Only fires for real moments (a first add, newly completing something, rating for the first time or changing your rating), not every incidental edit, and only for people whose profile is public (same privacy rule as everything else). Requires `activity-migration.sql` on existing projects.
- **Restored the inline "Select items" toolbar button** — after the Tools-panel consolidation moved it a click deeper (flagged as a "worth watching" item), it's back as its own button next to Tools on the dashboard toolbar, same as before that consolidation. The "Views" section inside the Tools panel (formerly "Selection & views") no longer duplicates it.
- **"New" indicator on the Tools button** — a small red dot appears on the Tools button the first time there's an actual recommendation waiting (Play Next, Recommended for you, etc.), so it's discoverable without permanently re-expanding those sections. Clears the first time you open Tools, and remembers per-device (via `localStorage`) so it doesn't reappear every visit for the same recommendation.
- **"On this day" now includes completions and ratings, not just adds** — previously only reflected `games.created_at`, so finishing or rating something a year ago today never surfaced, only the original add date. Now also checks `activity_events` for 'completed'/'rated' rows on this date and labels each entry with what kind of moment it was.
- **`/api/igdb-franchise`'s franchise and collection lookups now run concurrently** — closes a "worth watching" item flagged right after the fetch-timeout sweep: `getFranchiseGames()` could chain up to 4 sequential 8-second-timeout fetches after the initial search, which could in theory outrun the route's 20-second ceiling on an unlucky request. The franchise lookup (count check, then full games) and the collections lookup no longer depend on each other, so they now run side by side instead of one after the other — caps the worst case at whichever leg is slower instead of the sum of both.
- **Bulk actions now say when part of a selection was skipped** — closes the "worth watching" item flagged while building gift-priority bulk edit: "Set platform on 6 items" and "Set gift priority on 6 items" used to go quiet about any selected items that didn't qualify (no platform field, not on the wishlist) — now the toast adds ", skipped 2 not on the wishlist" (or the platform equivalent) whenever a selection was a genuine mix, instead of only reporting the count that succeeded.
- **CSV import now explains what currency `price` and `price_alert_threshold` are in** — closes the "worth watching" item flagged while wiring gift priority into CSV export/import: both columns are bare numbers with no currency of their own, always read in whatever currency Profile Settings is set to. That was invisible to anyone editing the raw CSV by hand, so the Import CSV dialog now says so directly rather than leaving it undocumented outside this changelog.
- **Public lists directory (`/lists`)** — from an Aug 2026 competitor pass (see `ROADMAP.md`): Backloggd and Grouvee both lean hard on "browse thousands of community-made lists" as a discovery surface, and Shelf Life's own custom lists (Favorites, For sale, etc.) already existed but were only ever visible on their owner's own profile. A new `/lists` page (linked from the navbar) shows every public, non-empty custom list site-wide — cover, owner, item count — and clicking one opens a full `/lists/[id]` page with everything on it, reusing the same card grid the profile page's own list sections use. Built entirely on the existing `custom_lists`/`custom_list_items` RLS (readable if the owner is public, same rule as everything else) via a new `public_lists` view; no new table. Individual list pages are also in `sitemap.xml` now, same reasoning as public profiles — a browsable directory only pulls its weight as a discovery surface if search engines can actually find what's in it. Requires `public-lists-migration.sql` on existing projects.
- **Aggregate rating now shows on search/browse tiles, not just after a click** — also from the Aug 2026 competitor pass: Backloggd shows a crowd rating right on every browse card. Shelf Life's collectible detail page has had an avg-rating stat since it shipped, but you had to open a title to see it — the Search page's collectible tiles now show it too (e.g. "4.5★"), computed from the same rated-rows-only rule `lib/collectibleDetail.js` already uses so the two numbers can't drift apart. No migration needed, reads the same `games.rating` column already fetched.
- **"Collectors you might like" on the dashboard** — closes the last item from the Aug 2026 competitor pass, the one buildable one of the two remaining "Later"-tier ideas: reuses the exact shared-high-rating signal that already powers "Recommended for you" (other public collectors who rated the same titles 4-5 stars as you), turned around to suggest *people* worth following instead of items worth owning, right alongside it. Excludes anyone already followed. New `recommend_collectors` SQL function, same non-security-definer reasoning as the existing `recommend_games`. Requires `recommend-collectors-migration.sql` on existing projects.
- **Fixed: master set still hid a variant tile you'd actually logged** — root-caused with real debug logging against a live request: TCGdex (the free community database "See master set" is built on) hadn't finished tagging variant data for a set that had only released days earlier, so it reported `reverse: false` for a card that genuinely does have a reverse holo print, even though a real copy of it was already logged. The master set now trusts a print you've actually logged over an unconfirmed "false" from TCGdex — it shows both what TCGdex already knows about *and* whatever you've told Shelf Life directly, instead of only ever trusting TCGdex.
- **Recommended for you** — a panel on the dashboard suggests titles you don't already own, based on what other public collectors who rated the same things highly as you also rated highly (a lightweight "people with similar taste to you" match on shared 4-5 star ratings). Click a recommendation and it opens Add Item pre-filled with the title, type, and cover art. Needs enough shared rating data across the site to surface anything — shows a "not enough data yet" message otherwise. Requires `recommend-games-migration.sql` on existing projects.
- **Steam account integration** — "Log in with Steam" (in Profile Settings) verifies your Steam account, then "Import from Steam" (in the dashboard toolbar, once connected) pulls in your owned PC games automatically as owned/digital items — pick which ones from a checklist, already-imported games are skipped. Requires your Steam profile's "Game details" privacy setting to be Public (Steam has no way to grant per-app access to a private list, login or not). Requires `steam-connect-migration.sql` on existing projects, plus a free `STEAM_API_KEY` (see README).
- **"What should I play next?"** — a widget on the dashboard suggests one thing from your backlog (or wishlist, if nothing's backlogged yet) to play next, weighted toward the genres/platforms you've already rated 4-5 stars. "Try another" re-rolls without repeating the last pick.
- **Xbox/PlayStation trophy & achievement fields** — two new fields on games (a "Platinum'd" checkbox and a completion % field) for tracking your *real* console trophy/achievement progress — kept clearly separate from Shelf Life's own collection-milestone trophy case, both in the field labels and visually. Shows on the card and rolls up into a stats panel (Platinums, average completion, games tracked) on your public profile once you've used it on at least one game. Manual entry only — neither platform has an API this site can use for personal projects. Requires `trophy-completion-migration.sql` on existing projects.
- **Export your collection** — an "Export CSV" button next to Import CSV downloads your whole collection as a spreadsheet at any time, using the same column layout as the import template, so it doubles as a backup you can re-import later if needed.
- **Slimmed down the Play Next and Recommended for you panels** — Play Next now shows a compact thumbnail + title/meta row instead of a full trading-card-style preview; Recommended for you scrolls horizontally in a single row instead of wrapping into multiple rows, so neither panel's height grows out of proportion with the rest of the dashboard.
- **Minimize dashboard panels** — Play Next, Recommended for you, and Collection value over time can each be collapsed down to just their title bar via a "Hide"/"Show" toggle in the header. Remembered per-visitor, so a minimized panel stays minimized next time.
- **Half-star ratings** — personal ratings now go in 0.5 steps (3.5, 4.5, etc.) instead of only whole stars. Click the left half of a star for a half rating. Applies everywhere a rating shows up — item cards, the activity feed, recommendations, "Play next" weighting. Requires `halfstar-ratings-migration.sql` on existing projects.
- **Trophies show up in the activity feed** — landing a Shelf Life trophy now posts to `/feed` for your followers to see, alongside adds/completions/ratings, with a small tier-colored badge (bronze/silver/gold/platinum). Requires `activity-trophies-migration.sql` on existing projects.
- **Chat-style quick add** — closes the last speculative-but-doable item off `ROADMAP.md`: a new "Quick add (type it)" option in the dashboard's "+ Add Item" menu opens a single-field modal where you type something like "fifa 06 on ps2 CIB $8 today" and it parses out a title, price, purchase date, platform, and completeness live as you type, showing a preview of what it understood before you commit. No AI/language API involved — there's no vision/language API key configured anywhere in this project (the same gap `ROADMAP.md`'s AI-photo-import note documents), so `lib/quickAddParse.js` is a plain regex/heuristic parser instead: strips a trailing "today"/"yesterday" into an ISO date, a "$N"/"$N.NN" price, a leading verb phrase ("logged"/"got"/"bought"/"picked up"/"grabbed"/"snagged"/"found", optionally preceded by "I"/"just" and followed by "a"/"an"/"the"), and quantity prefixes ("2 copies of"/"3x") — deliberately scoped to one item per parse (quantity phrasing is stripped, not multiplied into separate items), since "Quick add (search)"'s queue already covers real bulk adding. Parsing feeds into the normal Add Item form via `GameModal.jsx`'s existing `duplicateOf` prefill mechanism (the same path `handleAddFromRecommendation` already uses) rather than saving anything directly, so a wrong guess is just a field to notice and fix before hitting Save, never a silent bad save.
  **Fixed before this had even shipped**: the first pass only extracted title/price/date, so a real test — "fifa 06 on ps2 CIB" — left "on ps2 CIB" stuck inside the title instead of routing it anywhere useful, since there was nowhere else for it to go. `lib/quickAddParse.js` now also recognizes a platform mention ("on ps2", "for Nintendo Switch" — matched against a ~70-alias table of common shorthand, mirroring the canonical names `lib/consoleList.js` already uses, only when it directly follows "on"/"for" so a bare word like "DS" can't be mistaken for part of a title that never mentioned a platform) and a completeness mention ("CIB", "loose", "box only", "missing manual" — the same four values as `GameModal.jsx`'s Completeness dropdown), feeding both into the Add form's Platforms/Completeness fields via the same `duplicateOf` prefill instead of leaving them as noise in the title. A platform or completeness match also now overrides whatever item type was last used when picking the prefill's `item_type` — those are strong, game-specific signals, and the Platforms/Completeness fields only even show up on the form when the item type is a game (or console), so without this override a "fifa 06 on ps2 CIB" typed right after logging a trading card would have silently dropped both fields on the floor. A `tidyPunctuation()` helper also cleans up the orphaned commas that extracting a mid-sentence chunk out of comma-separated phrasing ("Fifa 06, PS2, CIB, $8") would otherwise leave behind. Covered by 17 tests total in `lib/quickAddParse.test.js` now (the original 10 — canonical shape, verb variants, decimal price, no-verb/no-price/no-date cases, quantity stripping, yesterday-date resolution, a title that happens to contain the word "Today" mid-string, blank/all-filler input, trailing punctuation — plus 7 new ones for platform/completeness extraction, including a direct regression test on the exact reported input). Ships as its own dynamically-imported chunk (`QuickAddTextModal.jsx`, code-split the same way `BulkSearchAddModal` already is) — confirmed via a real `next build` that the dashboard route only grew from 20.5kB to 20.6kB. Honest caveat: parse accuracy on real, varied phrasing hasn't been measured at scale — this covers the shapes exercised in the test suite, not every way someone might phrase a pickup.
- **Leaderboard redesign** — replaced the old 4-column grid of plain text rows with tabs (Trophy case, Biggest collections, Most-owned, Trending — one at a time instead of all cramped together) and a gold/silver/bronze **podium** for the top 3 in each, with cover art or avatars sized up and a colored glow per medal. Also added the **Trophy case** ranking itself: public collectors by Shelf Life trophies earned. (Along the way, fixed two bugs: `supabase-schema.sql` never actually included the trophy case tables, so a brand-new project's trophies silently didn't work until `achievements-migration.sql` was run by hand — folded into the main schema file now; and the page briefly 500'd in production from an event handler on an image that isn't allowed inside a server-rendered page — moved into its own component.) Requires `trophy-leaderboard-migration.sql` on existing projects.
- **Duplicate/near-duplicate warning** — adding (or editing) an item now checks it against your own collection as you type, and shows a soft heads-up ("You might already have this: *Chrono Trigger*") if something with a matching or near-matching title and the same item type is already there. Doesn't block the save — a second platform's copy, or replacing a lost one, are both legitimate reasons to "duplicate" something.
- **Automatic Steam achievement % sync** — a "Sync achievements from Steam" button (in Profile settings, once Steam's connected) walks every Steam-imported game and pulls its real achievement completion % straight from Steam's API, filling in the same Platinum/completion fields that were previously manual-entry-only for console games. Skips games with no achievements or a private profile rather than erroring. Uses the same `STEAM_API_KEY` already set up for Steam import — nothing new to configure.
- **Compare collections** — a "Compare collections" button on another collector's public profile (once you're logged in) opens a side-by-side view: what you both own, what only you own, what only they own, plus a Shelf Life trophy/platinum comparison. Matching uses the same near-duplicate title logic as the add-item warning above, so slightly different title formatting still counts as the same item.
- **Collection tools moved into Profile settings** — Import CSV, Export CSV, Import from Steam, and Sync achievements from Steam moved off the dashboard toolbar and into Profile settings under a new "Collection tools" section, keeping the toolbar itself down to the things you reach for constantly (Refresh all prices, Add Item).
- **Filter/sort by trophy completion %** — a "Highest Trophy Completion %" sort option, plus a filter (Platinum only / 75%+ / 50-74% / under 50% / not tracked yet) for the real Xbox/PlayStation completion field — find what's closest to platinum in your backlog, or see everything you haven't touched yet.
- **10 more Shelf Life trophies** — Window Shopping (10 wishlist items), Renaissance Collector (5 item types), Card Shark (25 trading cards), Crate Digger (10 vinyl records), Curator (fill all 5 showcase slots), Social Butterfly (follow 10 collectors), Popular Shelf (10 comments received), Platform Hopper (10 platforms), Half a Thousand (500 items), and First Platinum (your first real console Platinum tracked) — 28 trophies total now, up from 18. Requires `more-trophies-migration.sql` on existing projects; nothing to backfill by hand, everyone's next trophy check picks up the new ones automatically.
- **Custom lists** — curated sub-lists beyond the 5-item showcase (Favorites, For sale, Currently replaying, or whatever's useful) — create as many as you like from a new "Manage lists" button next to Manage showcase on your own public profile, and any list with items in it shows up on your profile alongside the showcase. Requires `customlists-migration.sql` on existing projects.
- **In-app notifications** — a bell in the navbar for follows, comments on your profile, and new trophies, so those moments don't only exist as an in-the-moment toast you might have missed. Opening the bell marks everything read; the unread count refreshes automatically about once a minute. Requires `notifications-migration.sql` on existing projects.
- **eBay price checks now use your local eBay site** — based on your display currency (Profile settings), GBP searches ebay.co.uk, EUR searches ebay.de, CAD searches ebay.ca, AUD searches ebay.com.au, CHF searches ebay.ch, and everything else still searches ebay.com. Prices shown now use whatever currency symbol that site actually returned, instead of always assuming $. This is a rough proxy based on currency, not a real location lookup — there's no separate country field, so EUR in particular can't tell which Eurozone country you're actually in.
- **Fixed "Collection value" not moving after a price check** — the number at the top of the dashboard only ever used purchase price, so checking an item's eBay price and saving it didn't change the total (even though the value-over-time chart below it already blended the two correctly). Now they're consistent.
- **Smarter eBay searches, and digital items skipped entirely** — price checks for games now also factor in the Condition field (not just Completeness), and "Check eBay price" no longer shows at all for anything marked Digital, since a digital code/download has no eBay resale market. A hint appears if Completeness/Condition are still blank on a physical game, since those two swing the accuracy of the result a lot.
- **"Hide digital items" toggle** — a checkbox in the dashboard's Filters panel that, unlike the existing Physical/Digital dropdown (which resets every visit), stays remembered on your device — for anyone who never wants their digital library cluttering the grid.
- **Fixed stale market values on Digital items** — switching an item's Copy field to Digital hid the price-check button but left a previously-checked value sitting there uncleared. Saving now clears it, since there's no eBay resale market for a digital copy.
- **Fixed mismatched currency symbols between item cards and the collection total** — item cards always showed a hardcoded \$, even after the site started checking prices from other eBay regions; the dashboard total already used your actual display currency, so the two could visibly disagree. Cards now remember and show whichever currency their price was actually checked in. Requires `marketpricecurrency-migration.sql` on existing projects (safe to run — backfills existing prices as USD, since every price checked before this shipped was USD).
- **Digital items no longer count toward Collection value** — the total previously fell back to an item's manually-entered purchase price whenever it had no eBay market price, which still included digital items (only the eBay side was excluded for those). Digital items are now excluded from the total outright, since there's no resale market for them at all.
- **More accurate, less inflated eBay price estimates** — obviously-mismatched listings (lots, bundles, graded/slabbed copies, "bonus disc" bundles) are now filtered out before pricing, and the "typical" figure is a median instead of a plain average, so a couple of outlier listings can't drag a small sample way up. The eBay search itself also got more careful: it now sticks to your currency where possible (falling back to whatever's actually listed, correctly labeled, rather than silently blending currencies together) and no longer appends a Condition word (like "good" or "fair") to the search — those almost never appear verbatim in a real listing title and were quietly zeroing out results for anything without heavy inventory. If the full, more specific search comes up empty, it now automatically retries with just the bare title before giving up.
- **"Refresh all prices" no longer fails silently** — a network hiccup, a genuinely empty result, or a database error used to just skip an item with zero feedback. It now retries once on a failed request, and shows a summary toast when the run finishes (refreshed all, or which items were skipped and why).
- **Consoles** — track physical hardware (Nintendo Switch, PS5, Xbox, retro consoles, etc.) as an eighth collectible type, right alongside games/comics/cards/vinyl/media. Manufacturer, storage/variant, special edition, region, and grade fields (reusing the same underlying columns comics/cards/vinyl already share — no new columns needed), plus Condition/Completeness, tags, barcode scanning, and eBay price checks, all work the same way. Copy (Physical/Digital) doesn't apply — a console is always a physical object you own — so that field is hidden for this type. Requires `consoles-migration.sql` on existing projects.
- **Collection value on public profiles** — a "Collection value" stat now shows on `/u/username` alongside Total items/Owned/Completed, same blend the dashboard already uses. Individual items' market prices already showed on the card itself.
- **Refresh other collectors' prices** — a "Refresh prices" button on someone else's public profile (once you're signed in) re-checks eBay for their whole shelf, the same way "Refresh all prices" works on your own dashboard. Writes through a new `refresh_item_market_price` database function that only ever touches the three market-price columns on a public collection's items — nothing else about someone else's items becomes editable. Requires `publicprice-refresh-migration.sql` on existing projects.
- **Funko Pops** — track figures as a ninth collectible type. Series/line, Pop! #, character, exclusive-to (Hot Topic/SDCC/GameStop/Funko Shop/etc.), grade (PPJoe/GalaxyPop/Raw), and a Chase/special-variant flag — reusing the same underlying columns trading cards already use (card_set/card_number/player_name/publisher/grade/is_variant), no new columns needed. Condition, tags, barcode scanning, and eBay price checks (searching by title + Pop! # for accuracy) all work the same as everywhere else. Copy (Physical/Digital) doesn't apply, same reasoning as consoles. Requires `funko-migration.sql` on existing projects.
- **"Most valuable" leaderboard** — a new leaderboard tab ranks public collectors by estimated collection value, same blend already used on the dashboard/profile (market price where checked, purchase price otherwise). Shown in each collector's own currency — totals aren't converted between currencies, so it's a fair ranking within the same currency but not a precise one across different ones (same known limitation as the rest of the site's currency handling). Requires `valuable-leaderboard-migration.sql` on existing projects.
- **Mobile reorganization: slide-in drawers instead of a stacked page** — the phone nav went from a dropdown that pushed the page down to a proper slide-in side drawer with a dimmed backdrop, same feel as a native app's hamburger menu. On the dashboard specifically, Play Next / Recommended / Value chart — three full panels that used to stack one after another above your collection — now live behind a "Tools & insights" button that opens the same kind of slide-in drawer, so the item grid is the first thing you see under the stats bar instead of the last. The stats bar, item cards, and grid spacing are also tightened up (smaller numbers, tighter card padding, a bit more breathing room per row) so more fits without feeling cramped. None of this touches desktop — it's all scoped to the existing mobile breakpoint.

- **Loan tracker** — mark any owned item as "Loaned to ___" (with a date) so a lent-out book/game/disc doesn't quietly disappear from memory. Shows as a distinctly colored row right at the top of the item's card (never hidden even in the mobile grid's compact view), has its own dashboard filter (Currently on loan / Not on loan), and is included in CSV exports as a backup. Clears automatically if you change an item's ownership away from Owned, or clear the loaned-to name. Requires `loan-tracker-migration.sql` on existing projects.
- **Saved filter views** — a new "Views" button next to Filters on the dashboard lets you save the current search/filters/sort as a named shortcut (e.g. "PS5 backlog," "Comics worth price-checking") and reapply it with one click later, instead of re-picking the same dropdowns every visit. Stored per-device (localStorage), same as the "Hide digital items" preference.
- **Undo on delete** — deleting an item no longer needs a confirm dialog. It disappears immediately, but a "Deleted 'X' — Undo" bar sits in the bottom-right for 6 seconds before it's actually removed from the database, so an accidental click (or a change of mind) is recoverable instead of permanent. Deleting a second item while one is still pending finalizes the first rather than juggling multiple undos.
- **Auto-search for Books and Consoles** — two more collectible types get a "Search" button next to Title, same pattern as games/trading cards. Books search Open Library (free, no signup needed — same source barcode scanning already used, just extended from ISBN-only to a plain title search) and fill in cover, author, and publisher. Consoles match against a built-in list of ~40 common systems (Switch, PlayStation, Xbox, Sega, Atari, Steam Deck, etc.) — there's no good free public console database the way there is for games/books, so this is a curated list rather than a live API, and fills in Manufacturer + Genre (Home console/Handheld) when you pick one. Comics, Vinyl, DVDs, CDs, and Funko Pops still don't have auto-fill — noted in `ROADMAP.md`.
- **Friends-only leaderboard** — a new "Friends" tab on the leaderboard, scoped to just the public collectors you follow — a smaller, more personal "who's ahead" than the site-wide rankings. Built as database views that check who's asking (`auth.uid()`) at query time rather than a fixed list, so results are automatically different (and correctly private) per viewer, including showing empty for a signed-out visitor. Requires `friends-leaderboard-migration.sql` on existing projects.
- **Friends leaderboard: all 5 categories, not just trophies** — the Friends tab now has its own row of category pills (Trophies / Most valuable / Biggest / Most-owned / Trending), same categories as the site-wide leaderboard, all scoped to just your friends. Requires `friends-leaderboard-expanded-migration.sql` on existing projects (on top of `friends-leaderboard-migration.sql`).
- **Next.js upgraded off the vulnerable pinned version** — 14.2.5 → 14.2.35, a patch-only bump within the same minor line (no API changes, nothing to retest behaviorally). Clears the critical/high-severity advisories `npm install` had been warning about (cache poisoning, DoS via Server Actions/Image Optimization, a middleware auth-bypass class of bug, several others) — all fixed upstream by Vercel within 14.2.x, so no need to jump to the breaking Next 16 line to get them. A handful of lower-severity advisories remain (an auth-js path-routing issue, a cookie edge case, postcss) that only resolve via major-version bumps of their own packages — left alone for now rather than bundled into this pass; worth a deliberate look later.
- **Self-service account deletion — built to satisfy Apple's App Store Guideline 5.1.1(v), needs a real live test before you trust it** — a "Danger zone" section at the bottom of Profile settings, gated behind typing your own username to confirm (no accidental single-click delete). The actual deletion happens server-side (`app/api/account/delete`), using only the caller's own verified session — never a client-supplied id, so there's no way to point it at someone else's account. It wipes their Storage files (avatars + item/condition photos, recursively across every item) and then deletes the `auth.users` row via the admin API, which cascades through every table that references `profiles(id) on delete cascade` per `supabase-schema.sql` — comments, follows, achievements, activity, notifications, passkeys, custom lists, everything. **This has not been exercised against a real Supabase project** — this sandbox has no live credentials, and deletion is far too destructive to fake-test against real data anyway. Before trusting this: sign up a disposable test account, add an item, upload an avatar, delete the account, then confirm in the Supabase dashboard that the profile row, its games, and its Storage files are all actually gone. Move to the section above once confirmed working.
- **Storage cleanup on item delete and avatar replace** — deleting an item (single or bulk) now removes its condition photos from the `item-photos` bucket first instead of leaving them orphaned; uploading a new avatar in a different file format now removes the old one instead of leaving it behind (the upload path is keyed by extension, so a .jpg replaced by a .png used to just add a second file rather than overwrite); clicking Remove on your avatar now actually deletes the file instead of only clearing the field. All best-effort — a Storage hiccup here never blocks the delete/upload/remove itself. New shared helper: `lib/itemPhotoCleanup.js`, also used by the existing single-photo remove button in the item form (previously had its own copy of the same logic).
- **"What's new" entries are clickable** — each entry in the feed sidebar now expands in place to show a bit more detail on what actually shipped, instead of just the one-line teaser. New `detail` field on every entry in `lib/whatsNew.js` (still written in plain, visitor-facing language, not `CHANGELOG.md`'s technical detail) and a small client component (`components/WhatsNewList.jsx`) handling the expand/collapse.

**Closed bugs:**
- *App Store rejection, Guideline 4.1(a) Copycats: "screenshots includes references to Pokemon"* — traced to the logged-out home page hero (`app/page.js`), which pulled a real, live-fetched Charizard VMAX card into its trading-card showcase slot via `lib/showcaseCovers.js`'s `fetchPokemonCardCover()`, publisher field literally reading "Pokémon" — the most likely source of the flagged screenshot, since it's the first thing anyone (including a reviewer) sees signed out. That slot now uses fully generic, made-up card details ("Rare Holo," "First Edition," "Trading Card Co.") and fetches no image at all, same as any item with no cover art elsewhere on the site; `fetchPokemonCardCover()` itself was removed as dead code (it also called the old `pokemontcg.io` endpoint, already replaced by TCGdex for the real card-search feature — see the Pokémon auto-fill fix above). Also swapped the one Pokémon-referencing row in the downloadable CSV import template to the same generic details. **This alone doesn't clear the rejection** — Apple reviewed actual screenshot images already uploaded to App Store Connect, which live outside this codebase entirely; see the top of `ROADMAP.md` for what still needs doing there before resubmitting.
- *Collection value chart: min-value label collided with the date row beneath it* — `ValueChart`'s bottom-left "lowest value" label and the "first date" label sat only 8px apart (baseline-to-baseline) on an 11px font, so their glyphs visually overlapped — worse with longer currency-formatted values eating more of that cramped strip. Gave the two rows their own clearly separated bands instead of a single shared padding value.
- *Item cards: long stat labels could push the value off to zero width, and — found on a second pass after the first fix — most cards were truncating far more than they should even at full desktop width* — on the grid's narrower cards, a longer label ("Storage / variant," "Special edition," "Market value," etc.) was locked at `flex: none`, so it never gave up any width — the value next to it got squeezed to 0px and vanished instead of truncating with "…". Fixed that part first (labels wrap onto a second line when they need to, values keep a minimum sliver of width), but a live check afterward showed near-everything with 2+ words was still getting cut off, even in a full-width browser window. Turned out the card grid's `auto-fill` column sizing means a wider window just packs in *more* narrow cards rather than making each one wider — so cards were sitting close to the 180px floor no matter how wide the screen was, leaving very little room for any stat row regardless of the earlier fix. Shortened the wordiest labels ("Storage / variant" → "Storage," "Special edition" → "Edition," "Series / line" → "Series," "Box condition" → "Condition," "Market value" → "Value") to free up space directly, gave the value a bigger guaranteed share of each row (was capping the label at up to 62% of the width, now 50%), and raised the grid's minimum card width from 180px to 200px so there's more room across the board. Ellipsis truncation still happens for genuinely long values (a game owned on four platforms, a long publisher name) — that's expected single-line behavior, not a bug — but the common case no longer clips unnecessarily.
- *Email verification link went to localhost* — fixed via Supabase Auth → URL Configuration (Site URL/Redirect URLs updated to the real site).
- *Cover images: .png didn't load, .jpg did* — no repro ever turned up (no broken URL or screenshot), most likely a one-off hotlink block on a specific image host rather than a site-wide bug. Reopen if it happens again with a URL to check.
- *Game search/auto-fill not working* — not actually a bug: needed a Twitch developer app for IGDB credentials, which was blocked on Twitch's 2FA requirement. Resolved once Taylor set up the Twitch dev account and `IGDB_CLIENT_ID`/`IGDB_CLIENT_SECRET` were added to Vercel.
- *What's New panel doesn't load on mobile* — a stray leftover mobile override for `.feed-layout`/`.feed-sidebar` sat *earlier* in `globals.css` than the base (non-mobile) rules for those same classes. Since both had equal CSS specificity, the base rules — declared later in the file — always won the cascade, on every screen size. So on mobile the sidebar stayed in its desktop 300px grid track instead of stacking below the feed, pushing it off-screen to the right with nothing clipping the overflow. Fixed by moving the mobile override to after the base rules, where it actually takes effect.
- *Changing your currency doesn't change an existing item's currency* — not actually a bug in the sense of a reactivity failure (purchase prices already reformat live everywhere the moment you change currency in Settings — no reload needed). What was actually happening: an item's *Market value* (an eBay price snapshot, not the purchase price) has never converted between currencies — by design, there's no live conversion anywhere on the site — so it silently kept showing whatever currency it was checked in even after you changed your profile currency, which read as a bug rather than a stale snapshot. Item cards now show a `(USD)`-style qualifier next to Market value whenever it doesn't match your current currency, so it's clear it's a snapshot rather than broken.
- *"trophys" on the leaderboard* — the trophy-count text built its plural by just appending "s" to "trophy," which gives "trophys" for any count other than 1 instead of "trophies." Fixed.
- *Supabase Security Advisor: "Security Definer View" on every leaderboard view* — Postgres views run with the view owner's permissions by default, which is what the advisor flags. Every leaderboard view now explicitly sets `security_invoker = true` so it runs with the querying user's own permissions/RLS instead — checked first that the underlying table policies (profiles/games/follows/achievements) already allow exactly what these views need, so this only tightens enforcement, it doesn't change what shows up. Requires `security-definer-views-migration.sql` on existing projects.
- *Trading card search sometimes came back empty for multi-word names* (e.g. "Floette ex") — the Pokémon TCG API's exact-phrase query occasionally missed cards it should've matched. Search now tries a phrase match, then a looser unquoted match, then a first-word prefix match, using whichever one finds something first.
- *Supabase Security Advisor: "Function Search Path Mutable"* on `handle_new_user`, `set_updated_at`, and `recommend_games` — these three didn't pin `search_path` the way the project's other functions already do, leaving them (especially `handle_new_user`, which runs with elevated privileges to create your profile row on signup) theoretically open to a schema-resolution trick. Pinned to `public`, matching the existing pattern; only function metadata changed, not behavior. Requires `function-search-path-migration.sql` on existing projects.
- *Supabase Security Advisor: broad SELECT policy on the avatars storage bucket* — the "Avatar images are publicly readable" policy let any client list every file in the bucket (filenames, upload times, sizes), which the app never actually needed: the bucket is already public, so avatar images display fine via their direct URL with no SELECT policy involved at all. Removed the policy; avatars keep displaying exactly the same. Requires `avatar-storage-policy-migration.sql` on existing projects.
- *Supabase Security Advisor: SECURITY DEFINER functions callable by signed-out visitors* — `award_achievements_for`, `check_and_award_achievements`, `backfill_all_achievements`, and `refresh_item_market_price` were never explicitly locked down, so Postgres's default (EXECUTE granted to everyone, including anon) quietly applied — meaning any of them could technically be called straight from a signed-out browser via the API, bypassing the app entirely. Also found and fixed a real bug while auditing this: `check_and_award_achievements`'s "is this really you" check used `<>` against `auth.uid()`, which silently evaluates to neither-true-nor-false (and gets skipped) when `auth.uid()` is null — i.e. exactly for a signed-out caller — so an anonymous visitor could actually still call it with someone else's user id and force their trophies to recompute. Locked all four down to only the roles that should ever call them, and fixed the comparison to handle nulls correctly. Requires `revoke-internal-function-access-migration.sql` on existing projects.
- *Supabase Security Advisor: same warning on `handle_new_user`* — missed in the first pass since it's a trigger function (can't actually be invoked any other way — Postgres blocks that), but the advisor flags the exposed grant regardless. Revoked. Requires `handle-new-user-revoke-migration.sql` on existing projects.
- *Supabase Security Advisor: previous revoke fix didn't actually clear the warnings* — turns out Supabase grants EXECUTE on every new function directly to `anon`/`authenticated` at creation time, through its own default-privilege setup — a separate mechanism from plain Postgres's generic `PUBLIC` default, which is what the earlier fix revoked. Re-fixed all five functions with an explicit revoke from `anon, authenticated` (not just `public`), and added a project-wide `alter default privileges` so this can't quietly happen again for any function added later. Requires `revoke-anon-authenticated-function-access-migration.sql` on existing projects.
- **Real branding** — a proper logo, replacing the plain "S" badge in the navbar and browser tab. Favicon, Apple touch icon, and the social-share preview image (Discord/Reddit/iMessage link previews) all now show the actual Shelf Life icon instead of placeholder text. No migration needed — this is a frontend-only, static-asset change.
- **Home page: real screenshots instead of a mockup, live stats** — the hero's sample cards and the "Earn real trophies" section now render the actual `GameCard`/`TrophyCase` components with sample data, not a hand-drawn imitation of them — what you see is pixel-for-pixel what your own dashboard looks like. Added a real, live "X items catalogued / X public collectors" stat next to the main CTA — genuinely queried from the database each time, not a made-up number, and the whole stat line just doesn't show if the counts are too small yet to say anything meaningful. The "most-owned" example in the leaderboard section does the same: it swaps in real top-3 data once there's enough of it, labeled as an illustrative example until then. No migration needed. Worth knowing: the home page went from a static prerendered page to a per-request one (same as `/leaderboard` and `/players` already were), since it now queries Supabase on every load — totally fine at current traffic, just flagging the change in kind.
- **Home page fixes: layout, real cover art** — the hero's 3 sample cards now always sit side by side instead of wrapping to a second row (the grid split was too narrow for them at full width — rebalanced it and made the cards shrink together instead of ever wrapping; below 640px they scroll horizontally instead of shrinking illegibly small). The 3 demo cover images went from flat color-plus-text to actual illustrative artwork. The small leaderboard preview also got real styling for the first time (it turns out those row/rank CSS classes were never actually written after the real `/leaderboard` page moved to a podium layout) plus cover thumbnails per row.
- **Home page hero: swapped trading card + vinyl for game + book, real photos** — the hero cards briefly pulled a real person's actual collection item per type, but a real entry can have any number of optional fields filled in (region, condition, market value...), which made the 3 cards wildly different heights and truncated real long field values in the narrow hero layout — reverted that part. The 3 cards use curated, consistent field values again, but the photo itself is still genuinely real: game fetches a real live cover from IGDB, same as the real "Search" button does for that type. No migration needed, no new env vars.
- **Home page hero: comic swapped for trading card** — comic never had a real, free cover-art source, so it was always showing generated illustrative art no matter how many times that art got redrawn. Swapped it for trading card, which does have one — the Pokémon TCG API, completely free and keyless, the same source the real trading-card "Search" button already uses. All 3 hero cards (game, trading card, book) now show a genuinely real photo rather than any generated artwork.
- **Shelf mosaic** — a poster of your real cover art arranged like items actually standing on a shelf, one wood-plank row per category (Games, Comics, Trading Cards, etc.). Two forms: a live, browsable page at `/u/[username]/mosaic` (hover or tap a tile for its name, type, and price), and a downloadable/shareable PNG at the same URL plus `-image` generated with the same `next/og` `ImageResponse` mechanism as the site's social-share preview image. You pick what's included — the Whole Shelf, just your Showcase picks, one category, one year you added things in, or your Most Valuable items — and covers get two independent accent badges rather than a plain grid: a star for anything in your Showcase, a price tag for your 5 highest-value items currently in view. Broken or missing cover URLs fall back to a colored initial tile instead of silently dropping the item or breaking the whole image. Folds in and replaces two ideas that were sitting unbuilt in `ROADMAP.md` ("Shareable yearly recap" and "Auto-generated stats card image") rather than building all three separately. No migration needed — reads existing columns only (`item_type`, `cover`, `showcase_order`, `market_price`/`price`, `created_at`).
- **Shelf mosaic redesign** — the first version read too much like a screenshot of the site (same navy chrome, thin plank line, one full-width row per category regardless of how few items were in it, so a small collection looked mostly empty). Reworked into a genuinely different-looking poster: a warm wood/brass palette instead of the site's navy, thicker shelf boards with real highlight/shadow and a brass category tag, items sit with a slight organic tilt and a drop shadow instead of a rigid grid, and rows now pack continuously across category boundaries instead of each category getting its own row no matter how small — a shelf with 2 comics and 3 records now shares one full row instead of two nearly-empty ones. Same underlying data and accent badges as before, purely a visual rebuild (`lib/mosaicRender.js`, shared by the PNG route and mirrored in the live page's CSS).
- **Fewer buttons on screen at once** — the dashboard header, profile page action row, and the item edit modal's footer each had 3-5 buttons sitting side by side competing for attention. Consolidated the secondary/infrequent ones into a new "⋯ More actions" dropdown (`components/ActionMenu.jsx`): dashboard keeps "+ Add Item" up front and tucks "Refresh all prices"/"Profile settings" away; a collector's profile keeps Follow (or Share/Edit profile if it's your own) up front and tucks Compare/Shelf mosaic/Showcase/Custom lists/Refresh prices away; the item modal keeps Cancel/Save Item up front and tucks Duplicate/Delete away. Existing buttons and their click handlers are unchanged — only where they live changed. Followed up by tucking the Settings panel's "Collection tools" row into the same dropdown too — Import CSV and Export CSV stay visible, Import from Steam and Sync achievements moved into "More collection tools" (the live sync-progress/Stop control still shows in place while a sync is running). Surveyed the rest of the app for the same pattern — dashboard's Filters/Views toggles, the platform filter chips, and modal reorder/remove icon rows (Showcase manager, Custom lists) were deliberately left alone since those are toggles or frequently-used per-row controls, not secondary one-off actions, and hiding them behind a click would hurt usability rather than help it.
- **Shelf mosaic fixes** — three issues from a closer look at the feature: price tags only ever showed on the 5 highest-value items in whatever was currently in view, so most priced items looked untracked — now every item with a tracked price shows its tag. "Most Valuable" could surface digital items even though they have no resale value (same rule the dashboard's "Collection value" stat already follows) — now excluded from that sort entirely. And filtering down to a single type used to cap out at one row (~10 items) with everything else counted as a "+N" overflow tile, because the per-category item cap was sized for a multi-category "Whole Shelf" view, not a filter that only ever has one category to draw from — row-packing is now computed once in `lib/mosaicData.js` and shared by both the PNG poster and the live page (previously only the PNG packed rows across category boundaries), so a single-type shelf now spreads across as many full rows as it has items for, up to 80 shown at once.
- **Feedback** (`/feedback`, linked from the footer) — a bug report / issue / feature suggestion form, no account required to use it. Every submission saves to a new `feedback` table first (so nothing's ever lost even if email delivery has a hiccup), then best-effort emails a notification via Resend if `RESEND_API_KEY`/`RESEND_FROM_EMAIL`/`ADMIN_EMAIL` are configured — without them, submissions still save, just without the email. Needs `feedback-migration.sql` on existing projects.
- **Bulk edit** — a "Select" toggle on the dashboard turns on checkboxes across the grid; a bulk-action bar lets you set ownership status or platform, add a tag, or delete, across everything selected at once instead of opening each item individually. Platform bulk-set only touches games/consoles in the selection (the only types that field applies to) and silently skips the rest; tags merge into each item's own existing tags rather than overwriting them, since tags differ per item and can't be a single uniform update. No migration needed.
- **Collection Insights** (`/dashboard/insights`, linked from the dashboard's "More actions" menu) — a stats page beyond the value chart: item counts by type/platform/genre, tracked value by type (digital items excluded, same rule as the Collection value stat), spending by month, and which calendar month you tend to add the most items in across every year. All derived from data already in the `games` table, no new inputs needed, rendered as simple CSS bar lists rather than pulling in a charting library (same "no library needed" call `ValueChart` already made). Scoped to owned items only. No migration needed.
- **Condition photos** — attach up to 4 real photos to a physical item (separate from cover art), for high-value collectibles where actual condition or grading matters. Uploads straight to a new `item-photos` Storage bucket, same folder-scoped-by-user-id pattern as avatar upload, plus a `condition_photos` array column on `games`. Only available once an item's been saved at least once (the storage path needs the item's id). Needs `condition-photos-migration.sql` and `item-photos-storage-migration.sql` on existing projects.
- **New-feature newsletter** — an opt-in checkbox in Profile Settings ("Email me when something new ships," off by default) and a private `/admin/newsletter` page (gated to whichever email matches the `ADMIN_EMAIL` env var — a 404 for anyone else, not just an "access denied") to write and manually send an update to everyone opted in. One-shot send, not scheduled or automated — nothing goes out until you write it and hit Send, and recipients go in Resend's `bcc` so they can't see each other's addresses. Reading opted-in collectors' actual email addresses needs `auth.users`, which the app's normal Supabase client can't see by design — added a service-role client (`lib/supabaseAdmin.js`, `SUPABASE_SERVICE_ROLE_KEY`) used only by this one feature, clearly commented as to why it's the sole place in the codebase that bypasses Row Level Security. Needs `newsletter-optin-migration.sql` on existing projects. See README.md step 7 for the full Resend + admin setup. Also added `FEEDBACK_NOTIFY_EMAIL` as an optional override so feedback notifications can go to a different inbox than the one `ADMIN_EMAIL` gates — useful if you'd rather keep admin login and feedback triage on separate addresses.
- **Feedback replies go to the actual sender** — the notification email didn't set a reply-to, so hitting "Reply" went back to the site's own send-from address instead of whoever submitted the form. `lib/resend.js` now supports `replyTo`, and the feedback route sets it to the submitter's email whenever they left one.
- **Combined search, and collectible detail pages** — the old "Find Collectors" page (`/players`, now just "Search" in the nav) searches collectors and collectible titles at once: usernames/display names as before, plus any title logged across public collections, deduped into one tile per title with a collector count. Clicking a collectible opens a detail page (`/collectible`) aggregating what every collector who owns it entered — platform/genre for games, series/issue/publisher for comics, and so on per type — plus how many own it, how many rated it, and the average rating, with an "Owned by" list linking to their profiles. Games get two outbound links, "Check time to beat" (HowLongToBeat) and "Find a guide" (GameFAQs) — links out rather than embedding, since neither has a free public API and scraping isn't something this app does. No migration needed, reads existing `games` columns only.
- **Search surfaces games nobody's collected yet** — the search page's title search now also runs the same IGDB lookup GameModal's "Search" button uses when adding an item, so typing a game title that's real but not yet in anyone's collection still returns a result (tagged "Not yet collected") instead of only ever matching what's already logged. Clicking one opens the same `/collectible` detail page, built from IGDB's platform/genre/release-year data instead of collector rows, still with the time-to-beat/guide links and a nudge to add it to your own shelf. IGDB search logic moved into a shared `lib/igdbSearch.js` so the API route and this new server-side fallback use the same code (and token cache) rather than duplicating it.
- **Collectible detail page cleanup** — reworked to use the same visual language as the rest of the site (stat tiles, section headings, card/border tokens) instead of ad hoc inline styling from the first pass.
- **Fixed: "Could not find the 'loaned_at' column" error when adding an item** — the Loan tracker feature's two columns (`loaned_to`, `loaned_at`) were written into `supabase-schema.sql` and `loan-tracker-migration.sql` when that feature originally shipped, but the migration itself was never actually run against the live database, so every save hit a schema-cache miss. Applied directly to production; no code changes needed since the app already expected these columns to exist.
- **Vinyl/CD auto-search** — a "Search" button next to Title for Vinyl and CD items, backed by MusicBrainz (free, no signup, unlike ComicVine/TMDb which the remaining Comics/DVD auto-search would need). Fills in artist, label, and format; cover art stays a manual paste since MusicBrainz's Cover Art Archive only covers a fraction of releases.
- **Scan multiple** (dashboard "More actions") — pick an item type, then scan barcodes back-to-back; each one looks up and inserts a new Owned item directly, without closing/reopening the Add form between scans. Reuses `BarcodeScanner` unchanged (remounts it via a key change after each scan) rather than modifying its one-shot detect-then-close behavior.
- **Reactions on the feed** — a 👍 toggle on each `/feed` entry (one per person per event), notifying the event's owner when someone else reacts. Requires `activity-reactions-migration.sql` on existing projects; also widened the `notifications.type` check constraint to add `reaction` and `price_drop`.
- **Trophy rarity** — each trophy in the Trophy Case now shows "X% of collectors have this," from a new `trophy_rarity()` SQL function computed live across every profile (not security-definer — everything it reads is already publicly selectable). Requires `trophy-rarity-migration.sql` on existing projects.
- **Collector level** — trophies are now also worth points (10/25/50/200 for bronze/silver/gold/platinum), rolled into a "Level" badge shown above the Trophy Case. Computed purely client-side from trophy defs + earned keys already being passed to `TrophyCase.jsx` — no schema change needed.
- **Comment rate limiting** — a `before insert` trigger on `comments` caps posting at 5 per author per 5 minutes, enforced in Postgres so it can't be bypassed by calling the API directly (client-side throttling alone wouldn't have covered that). Requires `comment-rate-limit-migration.sql` on existing projects.
- **Installable app** — `app/manifest.js` (Next's manifest file convention, reuses the existing `/icon.png`/`/apple-icon.png`) plus a deliberately minimal service worker (`public/sw.js`) makes the site installable to a phone/desktop home screen, with a cached `public/offline.html` fallback for when there's no connection. The service worker only caches static assets and the offline page — it never touches Supabase or `/api/*` requests, so installed users always see live collection data, never something stale.
- **Price-drop alerts** — an optional "Notify me if price drops below" field on wishlist items; a new Vercel Cron job (`app/api/cron/price-drop-check`, daily, `vercel.json`) checks current eBay listings and sends an in-app notification the first time the price dips at or below the threshold — tracked with a simple active/inactive flag per item so it only re-notifies after a real second dip, not every day it stays low. Needs a signed-in-user-less service-role client (the same one the newsletter uses) since a cron job has nobody to run as. Requires `price-drop-alerts-migration.sql` on existing projects, plus `CRON_SECRET` (and `SUPABASE_SERVICE_ROLE_KEY`) set — see README.md step 8.
- **Split the "Box only" completeness option into two levels** — it was previously one value labeled "Box only (no manual)," which conflated two very different states: literally having just the box, and having everything except the manual. Now two separate options: "CIB minus manual" (game + box, missing only the manual) and "Box only" (just the box, nothing else). Old CSV exports/imports using the previous `box` value are still read correctly (mapped to the new "CIB minus manual" value, which is what it actually meant). Requires `completeness-split-migration.sql` on existing projects — a data-only migration, no schema change (the column has no CHECK constraint).
- **Fixed: "Add to your shelf" did nothing for uncollected games** — the CTA on a game's detail page (shown when nobody's added that title yet, via the IGDB-backed search) linked to a bare `/dashboard` with no data attached, and the dashboard had no code path that opened the Add form on arrival — so clicking it silently landed on an unchanged dashboard. Now the link passes the title and cover through the URL, and the dashboard opens the Add form pre-filled with them on load.
- **Removed real emoji from the UI** — audited the whole codebase; most of what looked like emoji at a glance (✕, ☰, ★, →, ↑↓, ✎) turned out to already be plain typographic symbols serving as the site's icon system, not emoji, so those stayed. The three genuine emoji — 🧰 on the dashboard's "Tools & insights" button, 🔔 on the notification bell, 👍 on feed reactions — are now plain text (toolbox) or small inline SVG icons (bell, thumbs-up) instead.
- **Fixed: shelf mosaic "Download PNG" not working on mobile** — it was a plain `<a href download>` pointing straight at the image route, which iOS Safari mostly ignores for a real navigation (it just opens the PNG full-screen instead of saving it — reads as "nothing happened"). Now fetches the image as a blob and hands it to the native Share sheet (with a real "Save Image" option) where `navigator.share` supports files, falling back to a same-origin blob: URL download link everywhere else — which, unlike the old direct-navigation link, actually triggers a save reliably, including on desktop.
- **App Store prep** — Apple Developer Program approved; started getting Shelf Life ready for a real iOS App Store submission (wrapping the existing PWA via PWABuilder, building/signing through Xcode on a Mac). So far: a proper 1024×1024 App Store icon (flattened, no alpha, no baked-in rounded corners — required by Apple, generated from the same 512px source the rest of the site's icons use), safe-area-inset and overscroll-bounce handling so the wrapped app doesn't feel like a browser tab on notched iPhones, and a full checklist (`app-store-checklist.md`) covering screenshots, listing text, the new 2025/2026 age-rating questionnaire, the privacy "nutrition label," and App Review notes. Confirmed along the way that Sign in with Apple isn't actually required here (Guideline 4.8 only kicks in if a third-party login can authenticate your *primary* account — Steam login only links an already-signed-in account, it never creates or authenticates one) and that real push notifications aren't feasible inside the wrapped app at all (WKWebView, which is what the iOS wrapper uses, doesn't support the Web Push API — only true native APNs push would work, which needs actual Swift code once the Xcode project exists). Face ID/Touch ID passkey sign-in is still open — deliberately not rushed, since it touches Supabase session-minting directly and deserves real device testing rather than a same-session build.
- **VHS, and DVD/Blu-ray auto-search** — added VHS as a tenth item type, sharing all of DVD's fields (Director, Studio, Format, Edition). Both now have a "Search" button next to Title, backed by Apple's iTunes Search API — turns out it covers the same ground (title, cover art, director, studio, genre, year) as TMDb without needing a signup/API key, so DVD auto-search shipped too, not just VHS's. One movie search serves both formats since it's the same underlying title either way. Requires `vhs-migration.sql` on existing projects.
- **Button shape consistency** — audited every button on the site for the reported squared-vs-rounded mismatch; found one genuine case, the feed's reaction button, which used a full pill shape (`border-radius: 999px`) like a static tag instead of matching every other real action button's 8px radius. Fixed. Everything else that looked inconsistent turned out to be a deliberate distinction already in place — pill-shaped elements (badges, chips, category tags) are static labels, not buttons, so they intentionally look different from something you click.
- **Fixed: home page example cards not fitting on mobile (logged out)** — turned out not to be an overflow bug: the 3 sample collection cards were already a deliberate horizontal swipe row on narrow screens (full-size cards read better than 3 illegibly shrunk ones), but the third card got cut off cleanly at the frame edge with no visual cue that it was scrollable, which reads as broken. Added a right-edge fade-gradient hint, visible only in that mobile swipe mode, so it reads as "swipe for more" instead of "cut off." Also tightened up general overflow safety while investigating: `overflow-x: hidden` on the page body (a safety net, not a fix in itself — every intentionally-scrolling row already scrolls in its own inner container) and a `min-width: 0` fix on the hero showcase's wrapper, since a flex row that's also a CSS grid item can get stuck at its content's full natural width otherwise.
- **Fixed: leaderboard podium wrapping oddly on mobile** — found during a fresh mobile audit: the top-3 podium (Trophy case and Most valuable tabs) sizes each place at a fixed 140px with 20px gaps, 460px total — wider than a phone's content width, so 3rd place silently wrapped onto its own line below the other two instead of staying in the row. Shrunk podium widths/gaps/avatar sizes under the mobile breakpoint so all 3 stay side by side.
- **Xcode / App Store Connect walkthrough** (`app-store-xcode-walkthrough.md`) — a literal step-by-step for the part of App Store submission that has to happen on a Mac and can't be done from this repo: generating the iOS project with PWABuilder, opening/signing it in Xcode, taking the required 6.9" screenshots via the Simulator, creating the App Store Connect listing, archiving/uploading the build, TestFlight testing, and submitting for review — plus what to do if it comes back with a Guideline 4.2 rejection. Complements `app-store-checklist.md` (the assets/listing text to have ready) rather than replacing it.
- **Fixed 4 PWABuilder manifest warnings** — found by actually running the site through PWABuilder: missing `id` (added, matching `start_url` so browsers/OSes recognize repeat installs as the same app rather than a new one), missing `orientation` (added `portrait-primary` — the whole app is built and tested phone-width only, no landscape treatment exists anywhere, so this avoids ever landing in an untested layout), missing manifest `screenshots` (added two real captures of the live site — the dashboard and the shelf mosaic — not mockups), and an undetected service worker. That last one was already registering and working for real users, but only from inside a React `useEffect`, which fires after the whole JS bundle hydrates — late enough that PWABuilder's scan can miss it. Switched `PwaRegister` to a plain inline script (`next/script`, `afterInteractive`) so registration fires as soon as the page is interactive, independent of hydration timing.
- **Face ID / Touch ID passkey sign-in** — a "Sign in with a passkey" button on the login page and an "Add a passkey" section in Profile Settings, using real WebAuthn (`@simplewebauthn/server`/`@simplewebauthn/browser`), not a fake biometric-looking UI. Registration and sign-in are both fully server-verified (challenge/response, origin, RP ID) before anything is trusted — the browser's Face ID/Touch ID/screen-lock prompt is the actual authenticator, this app never sees or stores anything biometric, only a public key. The one genuinely tricky part: Supabase has no native "sign in with an already-verified credential" API, so once a passkey checks out server-side, the service-role client mints a one-time Supabase token (`auth.admin.generateLink`) which the browser then redeems itself (`auth.verifyOtp`) to actually get signed in — no email is sent, this is purely a token hand-off. Both buttons feature-detect and quietly hide themselves on browsers/devices without platform authenticator support, same graceful-degradation pattern used everywhere else on the site. Requires `passkey-migration.sql` on existing projects. Confirmed working end-to-end on a real Mac with Touch ID — add a passkey from Settings, then sign in with it from `/login`, no password typed either time.
- **Addressed the rest of PWABuilder's manifest suggestions** — after the 4 warnings above, PWABuilder's remaining list was ~20 lower-priority "info"/"enhancement" items. Most (widgets, Edge side panel, windows-control-overlay, tabbed mode, notes-app registration, background sync, push-via-service-worker, file/protocol handlers, share_target) are Windows-desktop-specific or don't fit how this app is actually used — skipped on purpose rather than adding manifest fields with no real behavior behind them. Added the ones that were genuinely quick and useful: `scope`, `display_override` (standalone → minimal-ui fallback), `lang`/`dir`, `categories` (lifestyle/utilities/entertainment), a `shortcuts` list (Add an item, My Collection, Leaderboard — long-press/right-click jump list, reusing the existing `?add=1` deep link), and a `wide` (1280×800, desktop) screenshot alongside the two `narrow` ones already added. Two items need Taylor directly rather than being something to build: an IARC age rating (requires going through Global Ratings' own free questionnaire to get a real rating ID — not something to fabricate), and `related_applications`/`prefer_related_applications` (needs the real Apple App Store ID, which doesn't exist until the app is actually submitted and live — a natural follow-up once that happens, not before).
- **Accessibility pass: modals and dropdowns keyboard-operable** — every full-screen modal (`GameModal`, `ImportCsvModal`, `SteamImportModal`, `ShowcaseManagerModal`, `CustomListsModal`, `BarcodeScanner`) was openable but not really usable by keyboard alone: no Escape to close, focus stayed wherever it was when the modal opened instead of moving into it, and Tab could walk straight through to the dashboard grid sitting behind the overlay. Built one shared hook (`lib/useModalA11y.js`) and applied it everywhere: Escape closes the modal, focus moves to the first field on open, Tab/Shift+Tab are trapped inside while it's open, and focus returns to whatever button opened it once it closes. Handles nested modals correctly too — opening the barcode scanner from inside the item form and pressing Escape closes just the scanner, not both. Every modal now also has proper `role="dialog"`/`aria-modal`/`aria-labelledby` so screen readers announce it as a dialog with a real title instead of an unlabeled block of content. The "⋯ More actions" menu (`ActionMenu`) and the notifications bell dropdown got the same Escape-to-close-and-return-focus treatment plus `aria-haspopup`, since neither had any keyboard way to dismiss them before. No visual changes, no migration needed — purely keyboard/screen-reader behavior.
- **New-signup email notification** — every new signup now sends an email so Taylor knows when someone joins, reusing the existing Resend setup (same one behind the feedback-form notification).
- **Fixed Add Item modal overlapping the mobile header** — the modal's z-index sat below the mobile nav bar's own stacking context (needed for the slide-in drawer), so on phones the "Add Item" heading rendered behind "Shelf Life" in the header instead of over it. Modal now always wins.
- **Mobile card grid: four across, poster-style** — collection cards on phones now lay out 4-per-row instead of 2, with the detailed stat block (platform/genre/grade/etc.) hidden at that size in favor of just cover art + title — a dense, scannable grid closer to a phone home screen than the desktop card layout. Tap a card for the full detail view same as before.
- **"See full series" completion view** — tap any game, comic, trading card, or Funko Pop and hit "See full series" to see the whole set/franchise, with anything not in the collection greyed out. Games use IGDB's franchise data; comics/trading cards/Funko Pops use whatever other Shelf Life users have logged for that same series/set (crowdsourced from the app's own data, same privacy rules as everywhere else). Works from Edit Item and from clicking a game/item on any profile (yours or someone else's), read-only on profiles.
- **Newsletter: send-to-everyone option + opt-in by default for new accounts** — the admin newsletter tool can send a one-off announcement to every account, not just opted-in ones (for the initial "everything that's shipped" email). New signups now start opted in to occasional feature-update emails by default (toggle-off available any time in Settings) — existing accounts are untouched either way.
- **Newsletter: HTML mode** — the admin newsletter tool can send a full HTML email (images, formatting) instead of just plain text, with a live preview before sending.
- **Fixed: mouse wheel didn't scroll horizontal rows on PC** — "Recommended for you," "Browse by system," and the shelf mosaic's rows only scrolled via trackpad or touch; a plain vertical mouse wheel did nothing since browsers don't automatically redirect vertical scroll into a sideways-scrolling container. These rows now forward vertical wheel movement into horizontal scroll for mouse users specifically (trackpad/touch behavior unchanged).
- **Fixed: eBay price check returning way-too-low results for consoles** — a search like "Nintendo Switch 2" was getting flooded with cheap accessory listings (cases, skins, screen protectors, docks, grips) that sellers tag with the console's full name, dragging the median down to around $11. Added accessory terms to the existing mismatch filter and doubled the raw listing sample size so real console listings aren't crowded out. (Later reworked further — the first pass of terms was still too narrow to reliably catch real accessory listing titles.)
- **Removed the loan tracker** — the "Loaned to ___" field, its dashboard filter, and the two `loaned_to`/`loaned_at` columns are gone (dropped from the live database too, no accounts had anything actively loaned out at the time). Not a feature worth keeping around.
- **Custom-selection shelf mosaic mode** — a new "Custom" tab alongside Whole Shelf/Showcase/By Type/By Year/Most Valuable lets you hand-pick exactly which items go into a mosaic instead of only using a computed rule, for e.g. a themed one-off ("just my Zelda stuff") that doesn't map to any existing filter. Picking opens a searchable grid with the same checkbox-select UI bulk edit already uses; the picks are remembered while you switch tabs, and an "Edit selection" button reopens the picker to change them. Works the same on both the live page and the downloadable/shareable PNG.
- **Click-to-edit profile showcase** — the 5 featured items pinned at the top of your public profile are now clickable: you click straight into the same picker the existing "Manage showcase" button opens (no separate step to find it in the menu), while a visitor clicks into the same read-only series view the rest of the collection grid already supports. The "Manage showcase" button itself is unchanged, still there for when the showcase is empty and there's nothing yet to click.
- **A real home page for signed-in visitors** — "/" used to show the exact same "Sign up free" marketing landing page to everyone, logged in or not, which meant clicking the navbar logo (or opening the installed app — its start_url is "/") dropped an already-signed-in collector back onto a page asking them to sign up. There's now a proper logged-in home: a greeting, your real stats at a glance, one-tap shortcuts into Add Item/My Collection/My Profile/Feed/Search/Leaderboard/Shelf mosaic, a peek at recent activity from collectors you follow, and a "What's new" teaser — distinct from /dashboard (collection management) and /feed (full activity stream), not a duplicate of either. Logged-out visitors still see the original marketing page unchanged. The navbar logo now always links to "/" for everyone instead of skipping straight to /dashboard when signed in, so this is actually reachable via normal navigation. No migration needed — reads existing columns only.
- **Reviews & Articles, and a home page that's actually distinct from the dashboard** — the home page's stats bar and 7-tile shortcut grid turned out to just duplicate /dashboard (which already has its own stats bar) and the navbar (which already links Search/Leaderboard/Feed/My Collection/My Profile) — home had no real reason to exist next to either. Reworked it: the stats bar is gone, the shortcut grid is down to just the two things that weren't already a tap away (Add an item, Shelf mosaic), and in their place is a new "Reviews & Articles" section — genuine editorial content, not collection data — with a new `/articles` index and `/articles/[slug]` detail pages. First piece is a real review (Deus Ex: Mankind Divided, 4/5). Content lives in `lib/articles.js` for now, same static-array pattern as `lib/whatsNew.js` — no database table or admin UI yet, just add an entry to publish a new one. "Articles" is also now a navbar link, visible whether signed in or not. Recent-activity and What's new stay as they were. No migration needed.
- **Community article/review submissions, with an approval queue** — anyone signed in can now write their own review or article at `/articles/submit` (title, one-line summary, body, and a star rating if it's a review) instead of Reviews & Articles being Shelf Life-team-only. Submissions land as "pending" and stay invisible to everyone but their author until approved from a new admin-only queue at `/admin/articles`, where each one can be read in full and Approved (publishes immediately under the submitter's name, alongside the built-in pieces) or Rejected. Submitters see their own pending/published/rejected status on the submit page. An email notifies Taylor whenever something new comes in. Requires a new `article_submissions` table (already applied to production) with RLS so a submission is only ever visible to its author or an approved/admin viewer.
- **Home page: dropped the recent-activity feed, fixed two mobile bugs** — the home page's "Recent activity" column (a preview of followed collectors' latest adds/completions) was cut — it was still just a smaller copy of /feed rather than something the home page uniquely offered, same complaint that drove the rest of the redesign. "What's new" now runs full-width in its place. Also fixed: the two-column layout's collapse-to-one-column breakpoint was 360px, far narrower than any real phone (375-430px), so it never actually collapsed and content ran off the right edge of the screen; and the greeting heading had no mobile-specific size, rendering oversized at the browser default. Both now collapse/scale correctly at 640px, matching the rest of the site's mobile breakpoints.
- **Home page: removed every button/link that duplicated the navbar** — the "My Collection (count)" tile went straight to /dashboard, exactly what the navbar's own "My Collection" link already does; "See all" under Reviews & Articles and "See all updates" under What's new both just pointed at pages (/articles, /feed) the navbar already links directly. All three are gone — home now only has links to places you can't already reach in one tap from the nav (Add an item, Shelf mosaic, Submit yours, and the article/update items themselves).
- **Home page: two Backloggd-inspired discovery rows** — "Recently added" (a live, horizontally-scrolling wall of cover art from what public collectors across the whole site are adding right now, not just people you follow) and "Recent ratings" (real 4-5 star ratings paired with real notes, shown as a card strip with the rater's name). Both are genuinely site-wide, not personalized, and both use the same horizontal-scroll layout at every screen width on purpose — that's what makes them work on mobile without a separate layout, narrower screens just show fewer cards before the edge. Cards link to `/collectible`, the same title+type page search results already use. No migration needed — reads existing `games` columns only.
- **Home page: decorative top banner** — a wide auto-scrolling band of real cover art (highly-rated public items, a different slice than the "Recently added" row below so the two don't just repeat each other) now runs across the top of the page, above the greeting. Purely visual — not clickable, no text — the loop is seamless (renders the cover list twice and scrolls exactly halfway) and pauses automatically for anyone with reduced-motion enabled. Only shows once there's enough real cover art to loop (6+), so it never renders sparse. Runs full-bleed edge-to-edge rather than boxed inside the usual page width, so it actually reads as a banner instead of another card.
- **Fixed Recent ratings cover thumbnails getting stretched/cropped** — flex's default cross-axis stretch was pulling the small cover image up to match the height of the taller text next to it, squashing the artwork past its real proportions.
- **Widened the page sitewide** — the shared page width went from 1200px to 1440px, so grid-heavy pages (dashboard, leaderboard, players, mosaic) use more of a big monitor instead of leaving space on either side. Text-heavy pages are unaffected — article bodies and forms already have their own tighter width caps that don't depend on this one.
- **Fixed a real security bug: any signed-in user could overwrite any public collector's item price** — `refresh_item_market_price()` (the function behind "Check eBay price") wrongly reused the *read* authorization rule (owner OR public profile) for a *write*. In practice that meant anyone could call it directly with another public collector's item ID and set their market price to anything, which would also skew the "Most valuable" leaderboard. Tightened to owner-only, found and fixed in a pre-launch audit. Also revoked the direct-API-call grant on `enforce_comment_rate_limit()`, a trigger-only function that never needed public EXECUTE access in the first place (same pattern already used for `handle_new_user()`).
- **robots.txt now excludes `/admin`** — those pages already 404 for everyone but the site owner (enforced server-side), so this isn't a security fix, just stops search engines from indexing a dead result.
- **Home banner respects safe-area insets** — the full-bleed top banner now pads for `env(safe-area-inset-left/right)`, matching the same handling the rest of the page already has on `body`, so a landscape notch or rounded display corner can't clip a cover right at the true screen edge.
- **Removed dead CSS left over from the cut "Recent activity" home section** — `.home-split`, `.home-activity-row`, `.home-activity-avatar`, `.home-activity-text`, and `.home-split-empty` were still sitting in globals.css with nothing referencing them anymore.
- **Rate-limited article/review submissions** — `/api/articles/submit` had no cap; a signed-in-only trigger now blocks more than 5 submissions per person per 5 minutes, same shape as the existing comment rate limit, with a clean "you're submitting too fast" message on the form instead of a raw 500. Requires `article-submission-rate-limit-migration.sql` on existing projects (already applied to production).
- **Fixed inconsistent button shapes (squared vs. rounded)** — the root cause: `border-radius: 8px` was only ever set on the bare `button` element selector, so `.btn-primary`/`.btn-ghost`/`.btn-icon`/`.btn-danger` only got rounded corners when applied to an actual `<button>`. Every place those same classes were used on a `<Link>`/`<a>`/`<label>` instead (nav CTAs on the homepage, "Edit profile," "Upload image," "Log in with Steam," the collectible page's external links, and more) rendered dead square right next to real buttons using the identical class — not actually two competing styles, just one bug. Moved `border-radius: 8px` onto the classes themselves so it applies regardless of element type. Also brought the tiny condition-photo remove button in from 5px to the standard 8px. Pills, badges, avatars, circular icon controls, and the dropdown-menu/tab-underline variants were all already deliberate and left as-is.
- **Dashboard bundle size: code-split the heavy, click-only pieces** — `DashboardClient` statically imported `GameModal` (1,430 lines, and the thing that pulls in the `@zxing` barcode-scanning libraries via `BarcodeScanner`), `ImportCsvModal`, `SteamImportModal`, `BulkScanSession`, and `PasskeyManager` even though every one of them is already conditionally rendered (modal closed, settings panel collapsed, etc.) — meaning their code shipped to every dashboard visit regardless of whether they were ever opened. Switched all five to `next/dynamic` with `ssr: false`, and made the CSV-export `papaparse` import lazy too (it was the only other static import only needed inside one click handler). Confirmed with a real production build: dashboard page-specific JS went from 158kB to 15.2kB, first load from 298kB to 149kB — no longer the outlier the roadmap flagged, now in line with the rest of the site's heavier pages. No behavior change, same components, just loaded on demand instead of upfront.
- **RLS performance cleanup** — fixed everything Supabase's own performance advisor flagged: 39 row-level-security policies (across `profiles`, `games`, `follows`, `comments`, `custom_lists`/`custom_list_items`, `value_snapshots`, `activity_events`/`activity_reactions`, `notifications`, `passkey_credentials`, `article_submissions`, and storage) were calling `auth.uid()` directly, which Postgres re-evaluates per row instead of once per query — all rewrapped as `(select auth.uid())`. Also folded `article_submissions`' two overlapping SELECT policies into one. No behavior change, same access rules, cheaper at scale. Checked the two indexes the advisor flagged as unused (`games_title_idx`, `custom_list_items_list_id_idx`) and left both in place — they back real lookups (case-insensitive title matching, ordered list items) that just haven't been exercised yet at current data volume. Requires `rls-performance-migration.sql` on existing projects (already applied to production); `supabase-schema.sql`, `storage-setup.sql`, and `item-photos-storage-migration.sql` also updated so new projects start with the efficient version.
- **"Most valuable" leaderboard now accounts for currency — confirmed live and working** — the ranking used to compare raw numbers across currencies as if they were the same unit (a €5,000 collection and a ¥5,000,000 collection aren't actually close, but sorted like they were). Both `leaderboard_most_valuable` and `leaderboard_friends_most_valuable` now convert every collection's total to USD behind the scenes (a new `currency_rates_to_usd` table, ~15 hand-set approximate rates) purely to decide sort order — what actually displays on the leaderboard is unchanged, still each collector's own currency and own raw total. These are static rates with no update mechanism, so they'll drift out of accuracy over time — flagged in `ROADMAP.md` as worth automating via a free rates API down the line. **This was flagged as broken after shipping — the actual bug was that the migration had never been applied to the live database**, so the site was still running the old raw-number-sorted view. While applying it directly, also caught a real bug in the migration SQL itself: Postgres's `create or replace view` refuses to insert a new column in the middle of an existing view's column list (only appending at the end is allowed) — `total_value_usd` had to move after `priced_count` instead of before it. Fixed in `currency-aware-valuable-leaderboard-migration.sql` and `supabase-schema.sql`, applied directly to production, and confirmed against real data (a collector with a higher raw AUD number now correctly ranks below a collector with a lower raw GBP number, once both convert to USD).
- **Public "What's New" page** (`/whats-new`, linked from the footer) — the site's list of recent updates used to live only on `/feed`, which redirects straight to `/login` for anyone signed out, so the exact people it'd be most useful for persuading to sign up never saw it. Same content, same expand-for-detail behavior as the `/feed` sidebar, just reachable without an account. Included in the sitemap. No migration needed — reuses the existing `lib/whatsNew.js` list.
- **Account deletion grace period — schema now live, cron flow still needs a real end-to-end test** — deleting your account no longer happens instantly. Confirming in the "Danger zone" now just timestamps the request (`deletion_requested_at`); a dashboard banner shows the account is scheduled for deletion, with a one-click "Cancel deletion" that clears it if you sign back in during the window. The actual, irreversible cleanup (Storage files + `auth.users` row, same cascade as before) now runs from a new daily Vercel Cron job (`app/api/cron/process-account-deletions`) once 48 hours have passed since the request — the original instant-delete route (`app/api/account/delete`) is now just the scheduling step. **Found while investigating the leaderboard report: this migration had never been applied either** — the `deletion_requested_at` column didn't exist on the live `profiles` table, meaning "Delete my account" would have errored instead of scheduling anything. Applied `account-deletion-grace-migration.sql` directly to production and confirmed the column now exists. Still genuinely unverified: whether the `vercel.json` cron entry is actually registered and firing, and whether `CRON_SECRET` is set (same one `price-drop-check` uses) — couldn't check either from here (Vercel's runtime-log retention on the current plan is 1 hour, too short to see a daily cron's history). Do one real end-to-end run on a disposable test account before trusting this fully. Move to the closed-bugs/shipped section above once confirmed.
- **Free leaked-password check at signup, no Supabase Pro needed** — the roadmap previously described Supabase's own "Leaked password protection" toggle as a "2-minute manual step"; that was wrong — checking the actual docs, it's gated to the Pro plan and above, not worth paying for at current traffic. Built the same practical protection for free instead: signup now checks the chosen password against Have I Been Pwned's breach database (`lib/pwnedPassword.js`, `app/api/check-password`) using the k-anonymity range API — only a 5-character hash prefix is ever sent over the network, the real password never leaves the server as plaintext. A password that's shown up in known breaches is rejected with a clear message instead of silently accepted; if the HIBP check itself fails or times out, signup proceeds anyway rather than blocking someone over a third-party outage. Worth knowing: the site has no password-change/reset flow yet, so signup is the only place this runs today.
- **Fixed: "What's new" wasn't clickable on the logged-in home page** — the click-to-expand behavior (`components/WhatsNewList.jsx`) was built for the `/feed` sidebar and the new `/whats-new` page, but the home page had its own separate, older copy of this section — a plain static list of titles with no click behavior and no way to open, left over from before that component existed. Home now renders the same `WhatsNewList` component the other two places use, so its 4 entries expand in place too, plus a "See all updates" link next to the heading pointing at `/whats-new` (previously only reachable from the footer, which is what prompted noticing this in the first place). Removed the now-unused `.home-whatsnew-row` CSS.
- **Caught up the visitor-facing "What's New" list — it had drifted ~6 days and ~90 commits behind what actually shipped** — `lib/whatsNew.js` (the list real visitors see on `/feed`, `/whats-new`, and the home page) hadn't been touched since the Aug 6 home page redesign, even though a lot of real, user-facing work shipped after that: the signed-in home page and Reviews & Articles, community article submissions, the "See full series" completion view, Face ID/Touch ID sign-in, the shelf mosaic, VHS support, combined search, the feedback form, bulk edit, Collection Insights, condition photos, comics auto-fill, the Pokémon search fix, the currency-aware leaderboard, and the account deletion grace period. Added 17 entries covering all of it, using real commit dates, written in the same plain, visitor-facing language as the rest of the list — deliberately left out internal-only work (security/performance fixes, admin tooling, routine bug fixes) the same way the existing list already did.
- **Added an automatic staleness check so this doesn't happen again** — `scripts/check-whats-new-freshness.js` runs automatically before every build (wired as npm's `prebuild` step, so it fires on every local build and every Vercel deploy) and prints a warning to the build log if `lib/whatsNew.js`'s newest entry is more than 5 days old. Never blocks or fails the build — it's a nudge in the log, not a gate. Can also be run directly with `npm run check:whatsnew`. Doesn't fix the underlying discipline problem on its own (nothing can auto-write good visitor-facing copy from a technical changelog entry — that still takes a human/Claude pass), but it does mean it can't silently drift unnoticed for a week again the way it just did.
- **Collection cards: view first, Edit as its own explicit action** — clicking a card in the dashboard grid used to jump straight into the edit form; it now opens a new read-only `ItemDetailModal` instead, showing the same stat rows the card itself does (pulled out of `GameCard` into an exported `getStatRows()` so the two can never quietly drift apart) plus the fields that only ever lived inside the edit form before — notes, purchase price/date, variant details, condition photos. Getting into the actual edit form is now a deliberate, separate click: a small ✎ button overlaid on the card's own cover art (`.card-edit-btn`, hidden while in bulk-select mode), or an "Edit" button inside the new detail view itself. Scoped to the dashboard's own collection grid only — public/read-only views of someone else's cards (profile pages, showcase, compare) are unaffected, since `onEdit` is only ever passed from `DashboardClient`. No migration needed.
- **Full series view: a missing entry links straight to an eBay price check** — the "See full series" grid (in `GameModal`, editing your own item) greyed out what you didn't own but gave no way to act on that; clicking a greyed-out entry now opens a fresh Add Item form pre-filled from that entry (title, and set/issue-or-card-number for comics/cards/Funko Pops, franchise title for games) via the same `duplicateOf` prefill mechanism "Recommended for you" already uses — landing directly on a form that already has its own "Check eBay price" button, so spotting a gap turns straight into "what would this cost me." `normalizeSeriesResponse()` now carries each entry's raw title/number through (previously discarded once the display label was built) so there's enough to build a sensible prefill; a new `prefillFromSeriesEntry()` in `lib/seriesLookup.js` does the per-type mapping. `SeriesModal` (the read-only view on public profiles, where there's no add-item flow to hand off to) is unaffected — it simply never passes the new `onSelectMissing` handler, so those grids stay exactly as greyed-out-but-inert as before. No migration needed.
- **WebAuthn/passkey API routes are now rate-limited** — `register-options`, `register-verify`, `login-options`, and `login-verify` (`app/api/webauthn/*`) had no request cap at all, unlike comments/articles which each got a trigger-based limit earlier. Couldn't reuse that same pattern here: `login-options` doesn't insert anything (it just mints a challenge), and none of the four routes have a signed-in `user_id` to key on the way comments/articles do — login is exactly the case where there's no user yet. Built a dedicated `webauthn_rate_limit_events` table instead, keyed by client IP (`x-forwarded-for`/`x-real-ip`), checked and recorded from the route handlers themselves via a new `lib/webauthnRateLimit.js` — one shared budget of 20 requests per 5 minutes across all four routes (well above comments/articles' 5-per-5-minutes, since a real sign-in naturally uses two of these per attempt and a device switch or a retried Face ID prompt can easily add a few more). Over the limit returns a 429 with a clear message; both client-side buttons (`PasskeyManager`, `PasskeyLoginButton`) now surface it instead of a generic "could not start" message. Opportunistic cleanup (a small random chance per call sweeps rows older than a day) instead of a dedicated cron job, to avoid the table growing forever without adding a new scheduled route just for housekeeping. Requires `webauthn-rate-limit-migration.sql` on existing projects (already applied to production).
- **Fixed: clicking a missing series entry looked like it silently changed the item you were editing** — reported directly after the series→eBay-check feature above shipped: the click reuses the same open `GameModal` instance and switches it from "Edit Item" into a fresh "Add Item" draft in place (same mechanism the existing "Duplicate" button already used), which is deliberate — the original item is never touched in the database — but with nothing on screen calling that out, the same modal quietly showing different content read as your item having changed. `GameModal` now takes a `duplicateSource` prop (`'copy' | 'recommendation' | 'series'`, set by whichever of `handleDuplicate`/`handleAddFromRecommendation`/`handleAddFromSeries` triggered it) and, for the series case specifically, spells it out: the heading reads "Add Item — from the series" instead of just "Add Item," and the subtext explicitly says this is a new item, the one you were editing is untouched, and any unsaved edits to it were discarded (true — the form fully re-derives from the new source, same as it always has for Duplicate). Also fixed a related latent bug while in this code: the insert branch of `handleSave` cleared `modalGame` on a successful save but never `duplicateOf` (or now `duplicateSource`) — harmless in isolation, but it meant a stale prefill could silently resurface the next time the genuinely-blank "+ Add Item" button was clicked. No migration needed.
- **Missing-series-entry-to-eBay-check now also works from your own profile page, not just the dashboard's edit form** — reported right after the fix above: the feature had only ever been wired into `GameModal` (editing an item on `/dashboard`); `SeriesModal`, the read-only counterpart opened by clicking a game/comic/card/Funko Pop on a profile page, never got it at all, so a missing entry there did nothing. Fixed by passing a new `isOwnProfile` prop through from `ProfileTabs` — only actionable on your *own* profile, since "missing from `items`" only means "missing from your own collection" in that case (on someone else's shelf it just means missing from theirs, which says nothing about whether you already have it). There's no Add Item form to open in place on a profile page, so clicking hands off to `/dashboard` instead, with the prefill flattened into query params (`?addFromSeries=1&item_type=...&title=...`) — same pattern the collectible detail page's existing `?add=1` "Add to your shelf" link already uses, and it runs through the exact same `handleAddFromSeries` the in-dashboard click does, so the landing form gets identical "from the series" messaging either way. **While fixing this, found and fixed a real pre-existing bug in the code being touched**: `?add=1` passes `modalGame` a real-but-unsaved object (`{item_type, title, cover}`, no `id`) — several places in `GameModal` (the heading, and the footer's Duplicate/Delete menu) checked bare `game` truthiness instead of `game?.id`, so arriving via that link showed "Edit Item" instead of "Add Item," and offered a Delete button that would have called `onDelete(undefined)`. All three spots now correctly check `game?.id`. No migration needed.
- **"See full series" moved onto the read-only card detail view, not just the edit form** — requested directly: checking series completion from the card you clicked had nothing to do with editing anything, so burying it behind Edit was the wrong place for it. The new `ItemDetailModal` (the read-only view opened by clicking a card in your own dashboard grid) now has its own "See full series" toggle in the footer, positioned before the Edit button, with the same inline series grid and missing-entry click-through `GameModal`'s edit-mode version already has, without ever opening Edit first. No migration needed.
- **Clicking a missing series entry now opens the actual listing on eBay and CeX, instead of routing through an Add Item form first** — reported directly after the version above shipped: pre-filling an Add Item form (so its existing "Check eBay price" button could be used) was an extra, unwanted step when the whole point of the click was just to see what filling the gap would cost. Clicking a greyed-out entry anywhere the series grid is shown (`GameModal`, `ItemDetailModal`, and `SeriesModal` on your own profile) now opens two new tabs straight away — an eBay search for that title/set/number, and the same search on CeX (the UK-rooted used-games/media chain, via its `webuy.com` search) — no API keys or extra click required, since these are just the same public listing-search pages you'd get typing into either site yourself. New `lib/externalListings.js` builds both URLs, reusing the same per-type search-term logic (`lib/marketPrice.js`) the in-form "Check eBay price" button already uses, and picks the right country site for eBay (matching your display currency, same mapping the price checker uses) and for CeX where it has one (UK/Ireland/Australia; anywhere else falls back to the UK site, since CeX doesn't operate in the US). This fully replaces the earlier version of this feature, which opened a pre-filled Add Item form instead — that flow (and the "Add Item — from the series" messaging that went with it) is gone, along with the `/dashboard?addFromSeries=1` deep link `SeriesModal` used to redirect through. No migration needed.
- **That same click now checks eBay before deciding which tab to open, instead of always opening both** — asked for directly right after the version above shipped, originally as "check CeX first": not actually buildable — eBay has a real, official, already-integrated API (the same one the in-form "Check eBay price" button uses) to check whether it has listings, but CeX has no public API at all; the endpoint their own site uses internally 403s anything that isn't their own frontend. So it works the other way around: clicking a missing entry now checks eBay first, opens the eBay tab if it found anything, and only opens the CeX search tab instead when eBay comes up empty (or the check itself couldn't run — API not configured, network hiccup). One tab now instead of two, most of the time. `lib/externalListings.js`'s `openBestListingTab()` opens a blank tab immediately (so the redirect once the check resolves doesn't get caught by a browser's popup blocker) and severs its `opener` reference for the same reason any "open in new tab" link should. No migration needed.
- **"Forgot password?" flow** — a real gap flagged in `ROADMAP.md`: there was no way back into an account if you forgot your password, short of asking Taylor directly, and passkey sign-in (a real alternative, but still a minority of accounts) isn't offered at signup. `/forgot-password` collects an email and calls Supabase's own `resetPasswordForEmail()`, which sends a real reset email through Supabase's mailer — no new email provider needed, and deliberately shows the same "check your email" result whether or not the address has an account, since that's Supabase's own anti-enumeration behavior, not something faked here. The link lands on `/reset-password`, which handles both of Supabase's possible reset-link shapes (a `?code=` query param under the PKCE flow, or session tokens straight in the URL hash under the older implicit flow) so it works regardless of which one the project has configured, then calls `updateUser({ password })` once a session's established. Reuses the same free Have I Been Pwned breach check signup already runs (`lib/pwnedPassword.js`) — a reset is exactly the moment someone might reuse an already-compromised password. A "Forgot password?" link now sits under the password field on `/login`; both new pages are excluded from `robots.js` the same way login/signup already are. No migration needed. Built and compiles clean, but — like everything else in this pass — couldn't be exercised against a real password-reset email from this sandbox (no live Supabase project access to trigger one); worth a real click-through (request a reset, follow the email, confirm the new password actually signs you in) before trusting it fully.
- **Feedback form is now rate-limited** — `/api/feedback` was the last of the site's public write endpoints (comments, articles, and the four WebAuthn routes already had one) with no cap at all, and arguably the most exposed: no sign-in required at all, and every accepted submission fires a real notification email via Resend on top of the database insert, so a burst of junk wouldn't just fill the `feedback` table, it'd flood Taylor's actual inbox too. Couldn't reuse the trigger-on-table pattern comments/articles use, same reasoning as the WebAuthn routes before it: most feedback submitters aren't signed in, so there's no `user_id` to key a trigger on. Built a dedicated `feedback_rate_limit_events` table instead, keyed by client IP, checked and recorded from the route handler via a new `lib/feedbackRateLimit.js` — 5 submissions per 10 minutes per IP, looser than comments/articles' 5-per-5-minutes since a real person might legitimately resubmit once after fixing a validation error. Over the limit returns a 429 with the same "you're submitting too fast" message the other rate-limited forms already show; `FeedbackForm.jsx` needed no changes since it already surfaces any non-OK response's `error` field. Requires `feedback-rate-limit-migration.sql` on existing projects — applied directly to production and confirmed the table exists.
- **Account deletion now sends a confirmation email** — closes a real gap flagged in `ROADMAP.md`: since the 48-hour grace period shipped, the in-app "scheduled for deletion" banner was the *only* signal a request had gone through — someone who clicked it by mistake, or on someone else's behalf, and didn't happen to sign back in within 48 hours had no other way to find out or cancel. `app/api/account/delete` now sends a real email (via the same `lib/resend.js` every other notification uses) right after scheduling the deletion, spelling out the actual deletion date/time and linking back to the dashboard's "Cancel deletion" button — best-effort, same as every other notification email on the site, so a Resend hiccup can't turn a successful request into an error response for whoever just clicked delete. No migration needed — reuses the `RESEND_API_KEY`/`RESEND_FROM_EMAIL` already required for feedback notifications.
- **"Sign out of all other devices"** — a real gap flagged in `ROADMAP.md`: resetting a password because you suspect someone else has it didn't actually do anything about a session that was already signed in elsewhere (Supabase's JWTs are verified statelessly, so an old session stays "authenticated" until its token naturally expires, not until the moment you'd expect). A new "Account security" section in Profile Settings (right above Danger zone) has a "Sign out of all other devices" button using Supabase Auth's own `signOut({ scope: 'others' })` — no service-role key or new backend route needed, since it only ever acts on the caller's own sessions. Also now fires automatically (best-effort, non-blocking) right after a successful `/reset-password`, so recovering a compromised password actually closes out whatever session had it, not just the browser doing the resetting. No migration needed.
- **`/admin/stats`** — a private, unlinked admin page (same 404-not-"unauthorized" gating as `/admin/articles` and `/admin/newsletter`) showing total users, public-profile count, new signups this week/month, items logged site-wide, activity events in the last 7 days, and the 10 most active public collectors over the last 30 days. Closes a real gap flagged in `ROADMAP.md`: the only admin-facing pages before this were the article-review queue and the newsletter composer — nothing that just showed the numbers, even though this is a real launched product now. "Most active" is aggregated in plain JS from `activity_events` rather than a new SQL view — at Shelf Life's current scale that's a handful of rows, and it meant this shipped with no migration at all. Public profiles only, same privacy rule the real leaderboard already applies.
- **Currency rates now auto-refresh weekly** — closes a real gap flagged in `ROADMAP.md`: the `currency_rates_to_usd` table that powers the "Most valuable" leaderboard's ranking (see below) was a one-time hand-typed snapshot with no update mechanism and nothing to flag when it went stale. A new weekly Vercel Cron job (`app/api/cron/refresh-currency-rates`, `vercel.json`) pulls fresh rates from `frankfurter.app` — free, no API key, and covers every currency `lib/currency.js` supports — and upserts the table. Weekly rather than daily on purpose: this only feeds a ranking, not anything conversion-critical, so to-the-minute accuracy isn't the goal, just not going stale for months. Reuses the same `CRON_SECRET`/`SUPABASE_SERVICE_ROLE_KEY` the price-drop cron already needs — no new setup for existing projects that already have those configured. Also fixed a stale README claim while in this area: the "Leaderboard" bullet still said "Most valuable" wasn't currency-converted at all, which stopped being true once the currency-aware ranking shipped — updated to describe what it actually does now (ranks by a USD conversion, displays each collector's own raw currency, unchanged). Separately, `currency-aware-valuable-leaderboard-migration.sql` had also never made it into the README's existing-projects migration checklist — added it there too. No new migration needed for this cron itself, just the existing table.
- **Cron jobs now email Taylor when they actually fail** — closes a real gap flagged in `ROADMAP.md`: the only record of a cron breaking was a `console.error` landing in Vercel's runtime logs, and per this file's own currency-rates entry above, retention on the current plan is 1 hour — nowhere near long enough to notice an overnight failure. A new `lib/cronAlert.js` (`notifyCronFailure()`) sends a plain email to `ADMIN_EMAIL` via the same `lib/resend.js` every other notification already uses, wired into the real failure paths of all four cron routes: `price-drop-check`, `process-account-deletions`, `refresh-currency-rates`, and the new `weekly-stats-digest` below — a query failing, a fetch failing, `SUPABASE_SERVICE_ROLE_KEY` going missing, or (for account deletions specifically, since those are irreversible) an individual account failing to actually delete. Deliberately doesn't fire on a stray 401 (someone/something hitting the URL without the right `CRON_SECRET`, not a real job failure) and doesn't try to email itself if the alert's own send fails, to avoid a retry loop. Silently a no-op if `ADMIN_EMAIL` isn't configured — same fallback behavior as every other admin-facing feature on the site. No migration needed.
- **Weekly stats digest email** — closes a real gap flagged in `ROADMAP.md`: `/admin/stats` only ever showed anything if Taylor remembered to visit it — pull, not push. A new Sunday-morning Vercel Cron job (`app/api/cron/weekly-stats-digest`) emails the exact same numbers straight to `ADMIN_EMAIL` instead. Pulled `/admin/stats`'s query logic out into a shared `lib/siteStats.js` (`getSiteStats()`) so the page and the email read from one place and can never quietly drift apart — the admin page itself is unchanged, just now calls the shared function instead of running its own copy of the same queries. Needs `ADMIN_EMAIL` and Resend set up (see README step 7) on top of the `CRON_SECRET`/`SUPABASE_SERVICE_ROLE_KEY` the other crons already require; skips sending (503) rather than erroring if `ADMIN_EMAIL` isn't set. No migration needed.
- **Trophies earned added to the logged-out home page's trust stats** — closes a real gap flagged in `ROADMAP.md`: the home page already showed real, live "items catalogued" and "public collectors" counts to a first-time visitor, but not the third number the roadmap specifically called out — trophies earned site-wide. Now a third stat pulls a live count from `user_achievements` (a fully public-readable table, so no admin client needed, same trust rationale the other two counts already use) and shows once there are at least 10, same "don't show an unimpressive number" threshold pattern the existing two stats use. No migration needed.
- **Report a comment or a profile** — closes a real gap flagged in `ROADMAP.md`: this is now a public, search-indexed site with strangers leaving comments on each other's profiles, and there was no lever over bad content beyond Taylor finding it manually. A "Report" link now sits next to every comment (`app/u/[username]/CommentSection.jsx`) and a "Report profile" option lives in the "More actions" menu on public profiles (`components/ReportProfileButton.jsx`) — both one click, no reason text required, signed-in users only. Files into a new `reports` table via `/api/reports`, which also emails `ADMIN_EMAIL` a heads-up (best-effort, same as every other notification). A new private, unlinked `/admin/reports` queue (same 404-not-"unauthorized" gating as the other admin pages) shows each report with the actual comment text or profile it points at — resolved defensively, since `target_id` has no foreign key and the underlying comment/profile can be deleted out from under a report — with "Mark actioned" / "Dismiss" buttons. Server-enforced rate limit (5 reports per 5 minutes per person), same shape comments already use, so the report system itself can't become a new way to spam Taylor's inbox. Requires `report-migration.sql` on existing projects — applied directly to production and confirmed the table exists.
- **"Download my data"** — closes a real gap flagged in `ROADMAP.md`: the existing "Export CSV" only ever covered collection items, nothing else the site stores about someone. A new "Download my data" button next to Export CSV (`app/api/account/export`) downloads a full JSON export of everything else: profile, comments written and received, follows (both directions), activity feed entries, and trophies. Every table queried is already scoped to the caller's own `user_id`/`author_id` by existing RLS policies, so this runs entirely through the normal signed-in session — no service-role client needed, unlike most of the site's other admin-ish routes. The natural companion to self-service account deletion (a "here's a copy before it's all gone" step). No migration needed.
- **"What's new" teaser on the logged-out home page** — closes a real gap flagged in `ROADMAP.md`: `/whats-new` was only ever reachable from a small footer link, so a first-time signed-out visitor deciding whether to sign up had no way to see the site is actively maintained. The home page now shows the newest 3 entries (reusing the same `WHATS_NEW` data and `WhatsNewList` component the logged-in home page and `/whats-new` itself already use) with a "See all updates" link through to the full page. No migration needed.
- **Roadmap correction: two "new ideas" from last round turned out to already be built** — caught while re-reviewing the roadmap for this round's priority ranking (see below): "Sitewide search" was already live at `/players` (search collectors *and* collectibles, with an IGDB fallback for titles nobody's logged yet), and "Duplicate detection when adding an item" was already live too (the soft "You might already have this" heads-up, shipped well before this round). Both were added to `ROADMAP.md` without checking closely enough against what's actually in the codebase — removed rather than built. Worth mentioning so it's not a mystery why 2 fewer "new ideas" show up this round than usual.
- **`ROADMAP.md` restructured to rank by priority** — previously organized by where an idea came from (requested by Taylor, outside feedback, Claude's own ideas, accessibility, "later," infrastructure); now organized by actual priority tier, highest first, per Taylor's request, with a dedicated "Needs a quick action from you" tier for the handful of items that aren't really buildable from here (icon art, a Supabase dashboard toggle, a domain name decision, Xcode-side passkey setup) so they stop competing for attention against genuinely build-ready items.
- **Cron job visibility on `/admin/stats`** — closes half of a real gap flagged in `ROADMAP.md`: `lib/cronAlert.js` already emails Taylor when a cron job runs and errors, but nothing caught a cron that never ran at all. A new `cron_runs` table (`job_name`, `last_run_at`, `last_success_at`, `last_status`) gets upserted by all four cron routes (`price-drop-check`, `process-account-deletions`, `refresh-currency-rates`, `weekly-stats-digest`) on both their success and error paths, via a new shared `lib/cronHeartbeat.js`. `/admin/stats` (and `lib/siteStats.js`, shared with the weekly digest email) now has a "Cron jobs" section showing each job's schedule and last run time/status, or "Never run yet." This is the buildable half only — a job that stops firing *entirely* (not just erroring) still won't show up any differently here, since nothing calls the heartbeat if the job never runs. The other half (an external dead-man's-switch service) is now a "Needs a quick action from you" item in `ROADMAP.md` — needs Taylor to sign up for a free service like healthchecks.io or Cronitor. Requires `cron-heartbeat-migration.sql` on existing projects — applied directly to production and confirmed the table exists.
- **Accessibility: skip-to-content link, toast region announced to screen readers, and a missing focus indicator fixed** — first slice of the "Accessibility essentials" item in `ROADMAP.md`. Added a skip-to-content link (`app/layout.js`, visually hidden until keyboard-focused, jumps past the navbar straight to `#main-content`) for keyboard/screen-reader users who'd otherwise have to tab through the whole navbar on every single page. The toast notifications (`components/ToastListener.jsx`) now carry `role="status" aria-live="polite"`, so a screen reader announces a toast ("Comment posted," an error, etc.) the moment it appears instead of it being silently invisible outside of sighted use. And while auditing every `outline: none` in `app/globals.css` for real focus-visibility gaps: `.chip-input input` suppressed the browser's default focus outline with nothing replacing it — genuinely unusable for keyboard-only navigation, since there was no visual sign the field was focused at all — now gets a visible focus ring (`.chip-input:focus-within`). (`.mosaic-tile`'s own `outline: none` was checked too and left alone — it already has a proper `:focus-visible` replacement, so it wasn't actually a gap.) Still open: the site-wide `aria-label` audit, real `<label for="">`/`id` pairing on form fields, meaningful `alt` text, and `aria-live` for import progress specifically — see `ROADMAP.md`.
- **Confirmed working against real production data: trading card search, comics auto-search, and "See full series"** — these three shipped in earlier rounds and compiled clean, but were built without outbound access to their third-party APIs from this sandbox, so they'd never actually been exercised against real results. Taylor clicked through all three live and confirmed they work: trading card search (TCGdex, since the switch off the paywalled Pokémon TCG API), comics auto-search (Comic Vine), and the "See full series" completion view across games/comics/trading cards/Funko Pops. Removed from `ROADMAP.md`'s "Needs a quick action from you" list — nothing left open on any of the three.
- **Fixed: trading card search was silently dropping real matches** — reported directly: searching "Beldum" showed other Beldum cards but not the specific one Taylor actually owns. Root cause was two stacked, overly-tight limits in `app/api/card-search/route.js` — TCGdex's own search was capped at 8 results (`pagination:itemsPerPage=8`), then the code cut that down to 6 again on top of that — so a card that's been reprinted across more than a handful of sets (which is most of them) could easily have its specific print pushed off the end before ever reaching the results list, even though it was a genuine match. Raised TCGdex's cap to 48 and removed the redundant second cut entirely; raised the Magic/Scryfall side's cap from 5 to 30 for the same reason, since it has the same shape of bug. Not literally unbounded — a name shared across hundreds of prints (Pikachu, for instance) would make an uncapped search both slow (one detail-fetch per result, all in parallel) and a dropdown nobody could usefully scroll — but 48/30 comfortably covers real per-card reprint counts for the vast majority of searches. The results list itself already scrolls (`components/GameModal.jsx`), so no UI changes were needed, just showing what was already being silently thrown away. Couldn't be verified against a live Beldum search from this sandbox (TCGdex's API is outside its network allowlist) — worth a real re-search to confirm before considering this fully closed.
- **"See master set" for Pokémon trading cards** — requested directly: the trading-card "See full series" view only ever knew about card numbers other Shelf Life users happened to have logged (the same crowdsourced approach comics/Funko Pops still use), so a set with just one or two collectors on the whole site showed a mostly-empty, mostly-useless grid. Trading cards now hit a real backend instead (`lib/tcgdexSetLookup.js`, `app/api/pokemon-master-set/route.js`): TCGdex's actual per-set card list, including every real print variant (Normal, Reverse Holo, Holo, 1st Edition, W Promo) — a genuine master-set checklist, not a guess. A card with two prints (say, Normal and Reverse Holo) now shows as two separate checkable tiles instead of one, which is the real definition of a "master set" versus a plain base-set list. The button relabels to "See master set" specifically for trading cards (still "See full series" everywhere else). Matching an owned card against the right tile relies on the existing "This is a parallel/insert/special version" checkbox + Details text field (shared with comics, not a new field) — added a hint under that field explaining which keywords (reverse holo/holo/1st edition/promo) the matcher actually looks for, since freeform notes only match if they're recognizable. Also fixed a latent bug while building this: card numbers like "4/102" were being run through the same normalizer used for game/comic titles, which strips all punctuation and mangled "4/102" into "4102" instead of "4" — added a dedicated `normalizeCardNumber()` so a card added via search (which fills in the "/total" suffix) still matches correctly. Magic stays on the old crowdsourced version for now — added to `ROADMAP.md` as its own item, since Scryfall's variant data (foil/borderless/showcase) isn't shaped the same way TCGdex's is. Couldn't be exercised against live TCGdex data from this sandbox (same network-allowlist limitation as every other TCGdex integration here) — genuinely worth a real click-through (search a set with a known variant card, confirm it shows as two tiles and that logging the reverse holo print actually checks off the right one) before trusting this fully.
- **Fixed: "See master set" never showed reverse holo/holo/1st edition tiles at all** — reported directly, right after the feature above shipped: a real reverse holo copy was logged correctly, but the master set grid only ever showed a single "Normal" tile for that card, no second tile to check off. Root cause, confirmed against TCGdex's own docs: the per-set card list endpoint (`GET /sets/{id}`) only ever returns the "brief" shape (id/localId/name/image) — it never included variant info, for any card, regardless of what that specific card actually has. Real variant data (`reverse`/`holo`/`firstEdition`/`wPromo`) only exists on a card's own individual detail endpoint, one fetch per card, and there's no bulk version. Fetching full detail for every card in a set wasn't a safe fix on its own — real Pokémon sets commonly run 100-250+ cards, and firing that many parallel requests on one click risks both a Vercel function timeout and hammering a free community API. Fixed by making the detail fetch targeted instead of exhaustive: `lib/seriesLookup.js`'s new `variantCardNumbersFor()` tells the master-set endpoint exactly which card numbers the requester has already logged a variant copy of (within that same set), and only those specific cards get a real per-card detail fetch — everything else stays a single Normal tile, capped at 25 detail fetches per request as a backstop. Net effect: a card you've logged a reverse holo/holo/1st edition/promo copy of now correctly shows both tiles and checks off the right one; a card you haven't logged an extra print of yet still only shows one tile even if it technically has other real-world variants — this is "confirm the different prints I already have," not yet "discover every variant a set offers." Worth another real click-through to confirm the fix actually works, since this still couldn't be exercised against live TCGdex data from this sandbox.
- **Account deletion: investigated whether a deleted account's sessions stay "authenticated" — found the existing design already closes it** — real question from `ROADMAP.md`, not a superficial checkbox: traced through what Supabase's stateless, short-lived access tokens actually mean for a just-deleted account's still-open session on a second device. Two things already close the practical gap: Supabase Auth rejects a refresh attempt for a deleted user immediately (it checks the user still exists, not just that the token's unexpired — confirmed against a real GoTrue bug report hitting the same check), and every table a still-live token's RLS scope could reach (`user_id references profiles(id) on delete cascade`, and `profiles.id references auth.users(id) on delete cascade` in turn) is already gone in the same cascade the moment deletion runs, so there's nothing left for a stale token to actually read or write. Documented the full trace in `lib/accountDeletion.js` rather than bolting on speculative code for a gap that isn't really open. The one true residual — an already-issued access token stays cryptographically "valid" for up to its own TTL, since verification is stateless — has nothing left to act on given the schema above; if it's ever worth tightening further anyway, Supabase Dashboard → Authentication → Sessions → "Access token (JWT) expiry" (default 3600s) can be lowered project-wide, no code needed.
- **Accessibility: icon-button labels, form field/label pairing, image alt text, import-progress `aria-live`** — the four remaining items from "Accessibility essentials." Every icon-only button (chip remove, reaction like, list/showcase rename-delete-remove, etc.) that only had a bare symbol now has a specific `aria-label` describing the action, not just "close" or "remove." Every real `<label>`+field pair across the site's forms (the big one: `components/GameModal.jsx`'s ~50 fields across every item-type branch) now has matching `id`/`htmlFor` instead of being visually adjacent but structurally unconnected — each id kept unique even across the form's mutually-exclusive type branches. Cover art/avatar images with placeholder or missing `alt` now describe what's actually shown; genuinely decorative images (a small logo already sitting next to visible text) were deliberately left `alt=""`. CSV import's and Steam import's "Importing… X/Y" progress readouts, plus the Steam achievement sync readout, now announce updates via `role="status" aria-live="polite"` as they happen, same treatment the toast system already had — instead of a screen reader only reading the count once on manual re-navigation.
- **SEO landing pages** — four dedicated pages, each targeting a real search term with genuinely different content for that audience rather than one template reskinned four times: `/video-game-collection-tracker` (completeness-aware pricing, region tags, trophies), `/comic-collection-app` (issue numbers, variant covers, series completion), `/collectible-database` (all nine item types, real Pokémon master-set tracking, Funko chase variants), `/retro-game-inventory` (condition/completeness/region for import collecting). Each includes an FAQ section with real `FAQPage` structured data (a shot at a Google FAQ rich result, for free, from content already being written and shown). Cross-linked from the site footer and from each other, and now listed in `sitemap.js` too (`robots.js` already allowed them by default, no change needed there) — the one remaining step is verifying the site in Google Search Console and submitting the sitemap, see `ROADMAP.md`.
- **Fixed: mobile pages looked slightly zoomed in, with room to scroll sideways that shouldn't exist** — reported directly. Root cause: `app/layout.js`'s custom `viewport` export only ever set `themeColor` and `viewportFit: 'cover'` — defining a custom `viewport` export at all replaces Next.js's own sensible default rather than merging with it, so the page had no explicit `width`/`initialScale` at all. Without those, a mobile browser can lay the page out at a wider virtual width than the actual screen and scale the whole thing down to fit, which reads as both "everything's slightly zoomed in" and "there's room to pan sideways" simultaneously. Added `width: 'device-width', initialScale: 1` to the same export. (`app/globals.css` already had `overflow-x: hidden` on `html`/`body` as a safety net — the fact that horizontal scroll was still possible despite that net being in place points at the viewport-scaling explanation specifically, rather than one particular element overflowing, so no page-by-page hunt was needed on top of this.)
- **Adding a new item now defaults to whatever type you added last** — reported directly: the blank "Add Item" form always started on Video Game, so anyone mostly logging trading cards (or any non-game type) had to reselect the dropdown every single time. `components/GameModal.jsx` now remembers the item type of the last item you successfully *added* (not edits — editing an existing item's type doesn't change the default) in `localStorage`, and a fresh blank Add form starts there instead. Falls back to Video Game the first time, or if storage is unavailable (private browsing, etc.) — nothing new to configure, no migration needed.
- **CSV import now flags likely duplicates against your existing collection** — noticed while working near `components/ImportCsvModal.jsx`: manually adding one item already warns "You might already have this" (`lib/duplicateCheck.js`, same item type + matching/near-matching title), but bulk CSV import had no equivalent, so a spreadsheet that overlapped with what's already logged created silent duplicate rows. Import now runs every parsed row through the same title-matching rule against your existing collection, shows a "Skip N rows that look like something you already have" checkbox (checked by default, with a list of exactly which rows and what they matched), and the Import button's count updates live as you toggle it. Doesn't touch the existing per-item warning logic at all — same rule, reused, not rebuilt.
- **Fixed: mobile card grid left visible gaps of empty space under shorter titles** — reported directly. `app/globals.css`'s mobile card layout hides everything but the cover and title bar (`.card-body { display: none; }`) to fit 4 tiles across, but the title bar's height still varied with however many lines a given title wrapped to — a one-word title and a long one sitting in the same grid row meant CSS Grid sized that whole row to the taller one, leaving a slab of blank space under the shorter card's title, not just a shorter card. Clamped `.card-title` to exactly 2 lines on mobile (`-webkit-line-clamp`, with an ellipsis for anything longer) so every card in the grid is the same height regardless of title length — rows now pack evenly instead of leaving gaps. Desktop's title bar (which has room to wrap freely, and the visible stat block below it already absorbs any slack via `flex: 1`) was left alone — this was specifically a mobile-grid issue.
- **"Scan multiple" now checks for duplicates against your existing collection too** — closes the gap flagged in `ROADMAP.md` right after the same fix shipped for CSV import: `components/BulkScanSession.jsx` inserted straight to the database on every barcode with no check at all, so re-scanning something already logged (easy to do without realizing, especially working through a shelf) created a silent duplicate. A likely match now pauses the scanner — camera swaps out for a "You might already have this: *Title* looks like *existing title*" prompt with Add anyway / Skip buttons — instead of the single-item form's checkbox pattern, since there's no form sitting open here to attach one to and scanning keeps moving either way. Same `lib/duplicateCheck.js` rule all three entry points (single-item, CSV, and now bulk scan) share.
- **Tags and custom lists can now filter the dashboard, not just search-match or show on your public profile** — closes two gaps flagged in `ROADMAP.md`. Click any tag badge on a card (owner's own dashboard only — public/read-only card views keep plain, non-interactive tags) and the grid filters to just that tag, same spirit as the existing "Browse by system" platform tiles; a matching dropdown was also added to the Filters panel for browsing by tag without an example card already in view. Separately, `components/CustomListsModal.jsx`'s lists (Favorites, For sale, Currently replaying, etc. — previously visible only on your public profile) now load read-only into the dashboard too, with their own Filters-panel dropdown, so a list built for profile browsing doubles as a saved view where you're actually managing the collection. List membership itself is still edited from "Manage lists" on the profile — added to `ROADMAP.md` as a small follow-up (a bulk-edit "add to list" action) rather than folded in here.
- **Fixed properly this time: mobile card titles, after two rounds of guessing a line-clamp count (2, then 3 lines) that both still cut off real titles** — reported directly, twice. Both previous attempts picked a fixed number of lines as "probably enough room," and any fixed N > 1 has the same two-sided problem: some real title is always longer than N lines and gets cut off anyway, while shorter titles still fall short of N lines and leave the exact whitespace gap the original fix was for (CSS Grid sizes every row to its tallest card, so any per-card height mismatch shows up as dead space under the shorter ones). Switched `.card-title` to a single truncated line instead — the only value that isn't a guess, since it's the natural minimum every title box already needs, so every card is guaranteed the same title height regardless of content, not just "usually." Also matches how dense poster/tile grids handle this everywhere else (Netflix, Steam, the App Store) — one line under the art, full title one tap away in the detail view. Desktop's title bar (full-width, wraps freely, no row-height coupling since `.card-body` is visible there and absorbs slack via `flex: 1`) was never affected by any of this — still untouched.
- **Comic "See full series" upgraded to real per-series data (Comic Vine), same upgrade the Pokémon side already got** — closes the top item flagged in `ROADMAP.md`. Comics used to share the same generic crowdsourced-from-Shelf-Life's-own-data approach Funko Pops and (still) Magic use — only ever able to know about issue numbers someone here happened to have logged, not a real "here's every issue this run actually had" checklist. New `lib/comicVineSeriesLookup.js` / `app/api/comic-master-set/route.js` hit Comic Vine's real volume/issue data (the same free API already powering comic search/auto-fill) — a genuine per-series completion checklist instead of a guess built from whatever's already in the database. Deliberately shaped to match `/api/series-lookup`'s existing response instead of inventing a new one (comics don't have TCGdex's "variants" complication — one entry per issue, full stop — so it flows through the same `normalizeSeriesResponse()` path everything else already uses, unlike the Pokémon master set which needed its own pre-shaped response). The "See full series" button now reads "See master set" for comics too, same as trading cards. Capped at 300 issues per series (Comic Vine paginates 100 at a time; 3-page safety backstop against a Vercel timeout) — flagged as a known gap in `ROADMAP.md` for the handful of 1000+-issue ongoing titles that exceed it. Couldn't be exercised against live Comic Vine data from this sandbox (same network-allowlist limitation as every other external API integration here) — worth a real click-through before trusting this fully, same caveat the Pokémon master set carried until Taylor confirmed it live.
- **Bulk edit (dashboard "Select" mode) can now add selected items to a custom list** — closes the other item flagged in `ROADMAP.md` right after lists were wired into the dashboard's Filters panel. Multi-select was already the natural way to build up a big list ("For sale," say) instead of adding items one at a time from "Manage lists" on the profile page — now it actually can. Items already on the chosen list are skipped rather than erroring on the duplicate membership row; the toast says how many were added and how many were already there. Removing from a list via bulk edit is the natural next step — added to `ROADMAP.md`.
- **CSV import's duplicate check now catches repeats within the same file, not just matches against your existing collection** — closes the other item flagged in `ROADMAP.md` right after that check first shipped. A spreadsheet listing the same item twice (a copy-paste mistake, a re-exported file merged with an older one) used to import both rows with zero warning, since the check only ever compared against what was already saved. Now runs a second pass against earlier rows in the same file — only the second-and-later occurrence of a repeat gets flagged (never the first), so the default "skip flagged rows" keeps exactly one copy instead of dropping every copy of it. Same unified "Skip N likely duplicates" checkbox as before; each flagged row now says *why* — "you already have X" or "duplicate of row N in this file."
- **Notifications: per-category mute** — closes the item flagged in `ROADMAP.md`. A new "Notifications" section in Profile Settings lists all 5 notification types (new followers, comments, trophies earned, reactions, wishlist price drops) as individually checkable — uncheck any one and the bell stops surfacing it, without touching the others. Enforced entirely at read time in `components/NotificationBell.jsx` (both the unread badge count and the dropdown list now exclude muted types), not by changing any of the 5 places that actually write a notification row (follow/comment/trophy/reaction/price-drop) — so muting is purely a display filter: rows for a muted type still get written, they just stay hidden until (if ever) you turn that type back on, meaning nothing earned while muted is lost. New `profiles.muted_notification_types` column (`text[]`, defaults to empty — nobody's notifications change until they actually touch the new checkboxes). Requires `notification-mute-migration.sql` on existing projects.
- **Bulk edit can now remove selected items from a custom list, not just add to one** — closes the item flagged in `ROADMAP.md` right after "add to list" shipped. Same list-picker dropdown in the dashboard's Select-mode bulk bar now has a second button next to "Add to list": pick a list, select some items, "Remove from list" takes just the selected ones off it (items not currently on that list are silently skipped rather than erroring). No new table or migration — `custom_list_items` already had a delete policy scoped to the list's owner.
- **Shareable gift list link** — closes the item flagged in `ROADMAP.md`. A new `/u/[username]/wishlist` page shows just the wishlist — nothing else from the collection — meant for sending to family/friends around birthdays or holidays instead of handing them the full profile (and everything already owned) to dig through. Reachable from a new "Gift list" entry in the profile's "More actions" menu (shown to viewers only when there's actually something on the list; always shown to the owner, same as Showcase/Manage lists), plus its own "Share gift list" button using the same copy-link/native-share behavior `ShareProfileButton` already provides everywhere else. Same visibility rule as the rest of the profile (public, or the owner themselves) — this doesn't add a separate privacy toggle, just a narrower view of data a viewer could already see on the full profile; flagged in `ROADMAP.md` as a real follow-up gap for anyone who wants the gift list shareable without making their whole collection public.
- **Gift list can now be shared without making the whole profile public** — closes the item flagged in `ROADMAP.md` right after the gift list link itself shipped. A new "Share my gift list even when my profile is private" toggle in Profile settings (`profiles.wishlist_public`, defaults off) makes `/u/[username]/wishlist` visible independently of the main "Make my profile and collection public" toggle. The settings checkbox alone wasn't enough to actually do anything, though — the real access boundary is the `games` table's row-level security policy, not app code, so the existing "Games readable if profile is public or owner" policy got a third clause: also readable if the row's `ownership = 'wishlist'` and the owning profile has `wishlist_public` set. Scoped narrowly on purpose (wishlist rows only, nothing else) — audited the two other views that already query another user's `games` (`/compare/[username]`, the shelf mosaic) to confirm neither one leaks anything as a result; both already gate on `is_public` before querying at all, so they're unaffected. Flagged as a residual consideration in `ROADMAP.md` for any *future* games-querying view. Requires `wishlist-public-migration.sql` on existing projects.
- **Comic master set no longer hard-truncates at 300 issues** — closes the item flagged in `ROADMAP.md` right after the Comic Vine-backed master set itself shipped. The flat 3-page/300-issue cap was a simple but blunt safety backstop against a Vercel function timeout — simple, but it truncated the handful of genuinely 1000+-issue ongoing titles (Action Comics, Detective Comics, and similar) at exactly the same spot regardless of how much time was actually left. `lib/comicVineSeriesLookup.js` now paginates against a real 45-second time budget instead (with a 50-page/5,000-issue hard cap left in only as a backstop against a runaway loop, not an expected ceiling), and `app/api/comic-master-set/route.js` got its `maxDuration` raised to 60s to give that budget real room to work with — the same value the existing cron routes already use. A series that fits in the time budget (nearly all of them; even a 1000-issue run is only 10 pages) now gets its complete issue list. Couldn't be exercised against live Comic Vine data from this sandbox, same caveat as the original feature — worth confirming a genuinely long-running series (Action Comics, say) actually comes back complete now.
- **Old notification rows now get cleaned up automatically** — closes the item flagged in `ROADMAP.md` right after per-category mute shipped: muting only ever hid a type in the bell, it never stopped the row from being written, so a heavily-muted (or just old) account had no cleanup path at all — and a muted type's rows specifically never get marked read by anyone, since they're excluded from the bell's own queries entirely, so a simple "delete old read notifications" rule wouldn't have caught them. New `lib/notificationCleanup.js` purges anything older than 180 days regardless of read state, called once daily from the existing `process-account-deletions` cron rather than adding a fifth cron job just for this — both are quiet daily housekeeping that touches no one's actual collection data. `/admin/stats`'s "Cron jobs" section label updated to "Account deletions + notification cleanup" so it's not a surprise what that job actually does now.
- **Full notification history page** — closes the item flagged in `ROADMAP.md` right after the 180-day cleanup shipped: the bell dropdown only ever shows the most recent 30 rows, and now anything past the retention window is gone for good too, so there was no way to look something up from further back. A new `/notifications` page lists everything still in the table, 50 at a time via a plain `?before=<timestamp>` link (no client-side pagination needed for a simple chronological list), respects the same per-category mute the bell already does, and marks whatever's shown as read the same way opening the bell does. The row-rendering logic (type → display text/link) moved out of `components/NotificationBell.jsx` into a new shared `describeNotification()` in `lib/notificationTypes.js` so the dropdown and the new page can't drift into two different wordings for the same notification type. The bell dropdown got a "See all notifications" link at the bottom pointing at the new page. No migration needed — reads the same `notifications` table everything else already uses.
- **`SeriesGrid` now paginates instead of rendering every entry at once** — closes the item flagged in `ROADMAP.md` right after the comic master set's flat 300-issue cap was replaced with a real time budget: that cap had incidentally also been capping how many tiles `components/SeriesGrid.jsx` ever had to render in one pass. Now shows the first 100 entries with a "Show 100 more (N left)" button underneath for anything past that, matching the "first 8, then N more" pattern `ImportCsvModal.jsx` already uses elsewhere — no new dependency, no virtualization library. Resets back to the first 100 whenever the underlying series changes (neither `GameModal.jsx` nor `ItemDetailModal.jsx` remounts this component via a `key` when the selected item changes, so without an explicit reset a leftover page count from a previous, larger series could make a smaller one look like it's missing entries that were just never re-shown). Pokémon/Comic Vine master sets and the crowdsourced fallback all go through this same component, so all three benefit.
- **Funko Pops: checked for a free per-line database, none exists** — a documented investigation, not a shipped feature (see `lib/seriesCrowdsource.js`'s module comment for the full writeup). The one real open dataset, `kennymkchan/funko-pop-data` (~23,000 entries), is explicitly marked deprecated by its own author and hasn't been updated since January 2021; its named successor, popiq.dev, now requires payment (a 402 on every page checked, including its docs) rather than being free. No other free, live, actively-maintained option turned up. Funko Pops stay on the existing crowdsourced-from-Shelf-Life's-own-data path (same as before), and the "worth a look before assuming buildable" line in `ROADMAP.md` was removed rather than left as a recurring open question — nothing here changes unless a genuinely new free option shows up later, since Funko itself has no public API.
- **Dashboard: Filters/Select/Views/insights consolidated into one "Tools" panel** — closes the item flagged in `ROADMAP.md`, confirmed against a working mockup with Taylor first (a toggleable current-vs-proposed comparison built from the real theme tokens). These used to each open a different way — Filters as its own inline panel, Select and Views as separate toggles, and Play Next/Recommended for you/the value chart/Browse by system stacked above the toolbar expanded by default. All of it now lives in one slide-in panel instead, opened by a single "Tools" button next to Search and Sort (which stayed in the open toolbar since those get used on nearly every visit). This reuses `components/DashboardClient.jsx`'s `dash-tools` drawer, which already existed but was mobile-only scaffolding — it now runs at every screen width instead of being a separate desktop-only mechanism, and the CSS driving it moved out of the 640px media query into unconditional rules in `app/globals.css`. The four insight panels default to collapsed now (they used to default to expanded); each is still individually collapsible and the choice is still remembered per-device, same as before. The bulk-edit action bar that appears once Select mode is on stays visible above the grid exactly as before — only the toggle to enter/exit Select mode moved into the panel, since the bar itself needs to stay visible while picking items.
- **Profile Settings split into tabs** — closes the item flagged in `ROADMAP.md`. What used to be one continuous scroll of roughly ten unrelated settings (display name/currency, avatar, bio, three visibility/newsletter toggles, notification mute, passkeys, Steam, collection tools, sign-out-everywhere, account deletion) is now four tabs — Profile, Notifications, Security, and Data & danger zone — inside the same settings panel. Nothing moved out of Settings or changed behavior; "Save settings" still only applies to the Profile and Notifications tabs (the two that write through the existing settings form), since everything on Security and Data & danger zone already saves or acts immediately on its own button, same as before this was split up.
- **`price_drop` notifications now link straight to the item** — closes the item flagged in `ROADMAP.md` right after `describeNotification()` was pulled out into `lib/notificationTypes.js`: every other notification type already deep-linked somewhere specific, but a price drop just sent you to a bare `/dashboard` and left you to find the item yourself — more of a problem now that `/notifications` can surface a row from well before what fits on the default dashboard view. `notifications.game_id` was already stored on the row (used for the join, not previously used for a link), so this needed no schema change — just a new `?item=<id>` deep link on the dashboard (same pattern `?settings=1` already used) that opens straight to that item's detail view, and pointing the price-drop link at it. Silently does nothing if the item's since been deleted or sold off the wishlist.
- **Crowdsourced series matching now normalizes text before comparing** — closes the item flagged in `ROADMAP.md`. `lib/seriesCrowdsource.js` (the fallback backend Funko Pops still use) used to require an exact case-insensitive match on the series text, so "Marvel Series 1" and "Marvel - Series 1" would silently end up as two separate series that never saw each other's cards, even though they're clearly the same set to a person reading them. A new `normalizeSeriesText()` strips everything but letters/numbers down to single spaces before comparing, so punctuation and spacing differences merge instead of splitting. Doing that comparison in JS instead of a Postgres `ilike` meant switching from an exact-match server-side filter to fetching every row of the item type and filtering client-side — noted as worth watching in `ROADMAP.md` if this backend ever needs to scale past Funko Pops.
- **`/notifications` can now be filtered to one notification type** — closes the item flagged in `ROADMAP.md` right after the page itself shipped: the bell dropdown is short enough that one list is fine, but the full history page can run to hundreds of rows across months with no way to narrow it down. A row of plain `?type=` links (reusing `NOTIFICATION_TYPES` from `lib/notificationTypes.js`) sits above the list now, same non-JS-link pattern "Load more" already used — muting still applies regardless of which type is picked, same as everywhere else notifications are read.
- **Profile Settings can now be deep-linked to a specific tab** — closes the item flagged in `ROADMAP.md` right after Settings was split into tabs: `?settingsTab=<profile|notifications|security|data>` (alongside the existing `?settings=1`, or on its own — either opens the panel) jumps straight to that tab instead of always landing on Profile. Nothing uses this yet from elsewhere in the app — it's the plumbing a future nudge or support reply can point at.
- **`/notifications`' type filter now explains a muted-type empty state, and finally puts the `?settingsTab=` deep link to use** — closes both items flagged in `ROADMAP.md`. Filtering to a type you've muted used to look identical to never having gotten one of those notifications at all — now the empty state says plainly "You've muted [type] — that's why nothing shows up here," with a "Manage what notifies you" link straight to `?settingsTab=notifications`, the first real use of the deep link built last round. No schema change — reads the same `profile.muted_notification_types` the filtering logic already checks.
- **Gift list priority** — closes the item flagged in `ROADMAP.md`. A new "Gift priority" field (High / Medium / Low, or unset) shows up in the item form only for wishlist items, right alongside the existing price-drop alert threshold. `/u/[username]/wishlist` now sorts by priority first (unranked items still sort last, by title, same as before) instead of showing every item with equal weight, so a family member shopping from the list sees "get this one first" items up top. The priority also shows as a colored badge on the item's card everywhere a wishlist item renders — the dashboard grid included, not just the public gift list page. Requires `wishlist-priority-migration.sql` on existing projects. Cleared automatically if an item's ownership changes away from Wishlist, same rule the price-alert threshold already follows. Doesn't round-trip through CSV export/import yet — flagged in `ROADMAP.md`, same pre-existing gap the price-alert field already had.
- **"On this day" collection memories** — closes the item flagged in `ROADMAP.md`. A new panel in the dashboard's Insights section surfaces what you added on today's date in a previous year ("2 years ago today"), grouped by year, click a title to open its detail view — only shows up at all when there's actually something to show, no empty accordion taking up space most days. Reads `games.created_at` directly rather than the activity feed, entirely client-side over data already loaded for the grid — no new query, no migration, and it works all the way back to someone's very first item rather than only from whenever the activity feed happened to ship. Only reflects when something was *added*, not completed or rated on this date — flagged in `ROADMAP.md` as a real follow-up if that turns out to matter.
- **Fixed: Pokémon (and Magic) card search "randomly" failing to find cards** — reported live. Traced through production logs to a real bug, not a fluke: `/api/card-search` (and `/api/pokemon-master-set`, same root cause) fetch TCGdex/Scryfall with no timeout at all, so on TCGdex's own occasional slow patch, one hanging request could stall the *entire* search until Vercel's platform-level ceiling killed the function — 300 seconds later, as a 504 with a body the client can't even parse as JSON, which is what actually reached you as "can't find Pokémon cards." Both routes' TCGdex/Scryfall fetches now abort and fail fast (8 seconds) instead of hanging indefinitely, and both routes got an explicit `maxDuration = 20` as a backstop against any other unexpected hang. A single slow card now degrades gracefully (skips that one card, or falls back to name/image-only if just its detail fetch was the slow one) instead of taking the whole search down with it. No env var or migration needed — deploy only.
- **Book search "can't find Pokeology" — most likely a real Open Library coverage gap, not a bug, plus one real bug found and fixed while checking** — reported live right after the card-search fix above. Couldn't confirm Open Library's actual response from this sandbox (network restrictions), but the book itself ("Pokeology"/"Pokécology: An Illustrated Guide to Pokémon Ecology," released around April 2026) doesn't turn up on openlibrary.org in a plain web search either, which is the strongest signal available that it simply isn't catalogued there yet — Open Library depends on library/publisher records, and a book that recent, especially one with a US/UK spelling split ("Pokécology" vs. "Pokeology"), commonly lags behind. The "No matches found" message now says as much and suggests entering it by hand instead of just going quiet. Separately, found and fixed the same missing-timeout bug as the card-search fix above in two more routes that hit Open Library: `/api/book-search` (this one) and `/api/barcode-lookup` (used for every barcode scan, any item type — the biggest blast radius of the three). Neither was the cause of this specific report, but both had the identical exposure, so both got the same `fetchWithTimeout`/`maxDuration = 20` fix while already in this code. `/api/ebay-price`, `/api/igdb-search`, `/api/igdb-franchise`, `/api/movie-search`, `/api/music-search`, `/api/comic-search`, and `/api/comic-detail` all share the same no-timeout pattern and weren't touched this round — flagged in `ROADMAP.md` as worth a dedicated sweep rather than doing it piecemeal, reactively, one live bug report at a time.
- **`/api/card-search` now logs when a Pokémon/Magic lookup actually fails, instead of silently returning zero results either way** — reported live right after the timeout fix above: a specific card ("Poltchageist," from `Twilight Masquerade`) still wasn't turning up in search after that fix shipped. Confirmed directly against TCGdex's own source data that the card genuinely exists there (exact name match, two prints) — so a real, thrown failure (a network error, an unexpected response shape, TCGdex itself returning a non-2xx) was the leading suspect, but the code had no way to tell that apart from TCGdex legitimately returning zero matches: both silently became `results: []`, and nothing was ever logged either way. Added `console.error` at each of those failure points so the next occurrence is actually visible in production logs instead of a dead end. Investigation ongoing — this is diagnostics, not a confirmed fix yet.
- **Fixed two real bugs behind "some series collections are inaccurate, show far too many duplicates, or don't show the whole series"** — reported live. Two separate, genuine problems, not one:
  1. **Every non-Pokémon trading card (Magic, mainly) was silently routed to TCGdex, a Pokémon-only database, and got nothing back.** `lib/useSeriesLookup.js` sends every `trading_card` "See master set" click to `/api/pokemon-master-set` regardless of which TCG the card is actually from — `ROADMAP.md`'s own "Magic master sets" note assumed Magic was still on the older crowdsourced path, but it wasn't; that was stale documentation, not actual behavior. A Magic card's set name (e.g. "Foundations") essentially never matches a Pokémon set on TCGdex, so the lookup dead-ended with a Pokémon-specific "couldn't find a matching set" error — read from the outside as a series that "just doesn't show." Now a trading-card `no_series` from TCGdex automatically falls back to the same crowdsourced backend Funko Pops use (comics got the identical fallback against Comic Vine, for the same reason, while in this code) instead of stopping there.
  2. **The crowdsourced backend's own duplicate-collapsing was keying on the raw card/issue number text, not the same normalized form every other "do these numbers match" check in the app already uses.** Two collectors logging the same card as "20," "#20," and "020" used to land in three separate entries in the series grid instead of merging into one — every formatting difference in how people happened to type a number showed up as an extra, duplicate-looking row. `lib/seriesCrowdsource.js` now dedupes through the same `normalizeCardNumber()` (`lib/seriesLookup.js`) everything else already relies on. This affects Funko Pops directly and, now, anything landing on the crowdsourced fallback per fix 1 above.
  
  No migration needed for either — deploy only. The remaining, separate "inaccurate" report (Pokémon master sets only expanding a card's real variant tiles once you've logged one yourself) is a known, deliberate tradeoff already flagged in `ROADMAP.md`, not touched here.
- **Fixed the actual bug behind "inaccurate/far too many duplicates/doesn't show the whole series" — that report was about game franchises, not trading cards.** The two fixes directly above address a real, separate trading-card/comic bug, but the live report was about the Games "Series" section (IGDB franchises, e.g. searching from a Grand Theft Auto or Zelda game). Root cause: neither of the two Apicalypse queries in `lib/igdbSearch.js`'s `getFranchiseGames()` that fetch a franchise's games specified a `limit`, and IGDB's documented default when it's omitted is 10 rows (max is 500) — so every real franchise (more than a couple of platform re-releases deep) was silently capped at whatever 10 rows IGDB happened to return, in no particular order. Two symptoms from that one cause: the franchise-size comparison used to pick the right franchise when a game carries more than one tag topped out at "10" for any franchise that actually had 10+ games, so ties got broken arbitrarily instead of by real size (the "inaccurate" report); and the final 10-row slice was often dominated by several editions/re-releases of the same title or two (the "far too many and duplicates" report) while genuinely different games in the series never made it into the sample at all (the "doesn't show the whole series" report). Both queries now ask for the real maximum (500), so the existing per-title dedup is working from the actual full roster instead of an arbitrary 10-row sample of it. No migration needed — deploy only.
- **`lib/igdbSearch.js` now logs what it actually matched/picked for a game's series** — reported live right after the fix above: "Viva Piñata: Limited Edition" only shows one other game, which the `limit` fix doesn't explain, since that franchise was never anywhere near the old 10-row cap to begin with — the likely cause is upstream of that, a "Limited Edition"-style title exact-matching a separate, sparser IGDB entry for that specific retail SKU instead of the base game, which may simply carry a thinner franchise tag. Added `console.error` logging of the matched IGDB game, the franchise picked (and any other franchise tags on that game), and the raw game count returned, so the next occurrence is diagnosable from production logs instead of guessed at. Investigation ongoing — this is diagnostics, not a confirmed fix yet.
- **"Viva Piñata: Limited Edition only shows one other game" — confirmed via the new logging above: this one really is IGDB's own data, not a Shelf Life bug.** Production logs show the matched game ("Viva Piñata: Trouble in Paradise") carries exactly one franchise tag, and IGDB's own "Viva Piñata" franchise (id 1967) genuinely only has 2 games attached to it — confirmed straight from the log line, not guessed at — even though the real series has at least 4 (Viva Piñata, Trouble in Paradise, Party Animals, Pocket Paradise). Nothing left to fix in Shelf Life's own code here: the `limit` fix above is correctly pulling everything IGDB actually has tagged to that franchise, IGDB just hasn't tagged this one completely, and this game has no second franchise tag to fall back to either. Flagged in `ROADMAP.md` as worth trying IGDB's separate `collections` field as a fallback for cases like this, once that's actually been checked against IGDB's data rather than assumed to help.
- **Game series now also pull from IGDB's `collections` tag, not just `franchises`** — the "worth trying" idea from directly above, now built. `franchises` is IGDB's broad brand/universe grouping (Mario spans platformers, karts, party games...); `collections` is IGDB's narrower, more series-specific one — and IGDB itself has been actively moving that direction, having deprecated the old singular `collection` field in favor of the current plural `collections`. `lib/igdbSearch.js`'s `getFranchiseGames()` now fetches a game's `collections` tags alongside its picked franchise and merges both games lists together (the existing per-title dedup already handles any overlap between the two, e.g. a game tagged to both). Deliberately not symmetric with how multiple *franchise* tags are handled — still just the single biggest one there, not merged — since two franchise tags on a crossover game can be genuinely unrelated universes, which is exactly the "far too many, not really this series" clutter the `limit` fix above already dealt with; collections don't carry that same risk. Confirmed against production logs right after deploy: for "Viva Piñata: Trouble in Paradise," the `franchises` tag still only carries the same 2 games as before, but the `collections` tag ("Viva Pinata") carries 5 — exactly the gap this was meant to close, confirmed from real data rather than assumed. No migration needed — deploy only.
- **Closed the remaining missing-fetch-timeout gap across every external-API route** — closes the item flagged in `ROADMAP.md` right after four routes got this same fix while chasing the live Pokémon-card report above: `/api/ebay-price`, `/api/igdb-search`, `/api/igdb-franchise` (and the `lib/igdbSearch.js` logic behind both), `/api/movie-search`, `/api/music-search`, `/api/comic-search`, and `/api/comic-detail` (and `lib/comicVineSearch.js` behind both) all used to fetch their upstream API (eBay, IGDB/Twitch, iTunes, MusicBrainz, Comic Vine) with nothing capping how long a slow response could hang the request — same exposure as the four routes fixed earlier, just not yet reached by a live report. Every fetch call across all seven routes (thirteen call sites in total, including ones nested two API calls deep like eBay's OAuth token exchange or Comic Vine's issue→volume lookup) now aborts after 8 seconds via the same `fetchWithTimeout` pattern, and each route got an explicit `maxDuration = 20` backstop. No live bug behind this one — a proactive sweep rather than a reactive fix, closing the last item on that list. No migration needed — deploy only.
- **CSV export/import now carries gift priority and the price-drop alert threshold** — closes the item flagged in `ROADMAP.md` right after wishlist priority shipped. Both fields are wishlist-only (same rule GameModal's own form already enforces — cleared unless the item's ownership is actually `wishlist`), and neither round-tripped through "Export CSV"/CSV import before, a gap that quietly existed for the price-alert field even before wishlist priority did. `lib/csvExport.js` now includes both columns; `lib/csvImport.js` parses and validates `wishlist_priority` against the same 1/2/3 (High/Medium/Low) the database's check constraint enforces, dropping anything else with a warning rather than letting a bad value hit the insert and fail. The downloadable template (`public/shelf-life-import-template.csv`) got both new columns plus a 12th example row (a wishlist item with a priority and a threshold set) so the two new fields are actually demonstrated, not just present as blank columns. No migration needed — deploy only.
- **Mobile fix: search-result tiles no longer stretch their grid row** — a concrete instance found via a code-based mobile audit (no live browser access this session, see `ROADMAP.md`'s "Mobile layout feels disorganized in places"). `components/CollectibleCard.jsx`'s Search page tiles inherited the dashboard's forced 4-column mobile grid but, unlike `GameCard.jsx`, had no matching mobile text-truncation treatment — the type/collector-count/rating meta line could wrap to 2-3 lines on an ~80px-wide card, and since CSS Grid rows size to their tallest cell, that stretched every card in the same row. Gave it dedicated `.collectible-card-body`/`.collectible-card-title`/`.collectible-card-meta` classes with base and mobile-breakpoint rules, mirroring `GameCard`'s established pattern. No migration needed — deploy only.
- **"For sale" flag on owned items, shown on your profile** — closes the item flagged in `ROADMAP.md`. Distinct from the wishlist-only gift list: a checkbox + asking price, settable only while an item's ownership is `owned` (same 3-step gated-field pattern `GameModal.jsx` already uses for wishlist priority and the price-alert threshold), shows a green "For sale" badge with the asking price on the card wherever it renders — including the public profile, since it reuses the same `GameCard` component every profile tab already renders through. Requires `forsale-migration.sql` on existing projects.
- **Yearly "Wrapped" recap** — closes the item flagged in `ROADMAP.md`. A new `/dashboard/wrapped` page (linked from "More actions" next to Collection Insights) builds a shareable, screenshot-styled end-of-year card entirely from data already being tracked: items added and busiest month from `games.created_at` (accurate back to day one), completed/rated counts and your top-rated completion of the year from the activity feed, and an estimated collection-value change from value snapshots — each of the latter three skipped outright rather than shown as zero/misleading when there's nothing to base them on. A year picker steps back through your full history. Since the activity feed and value snapshots only started being recorded once those features shipped, the page says so directly under the card rather than presenting an older year's numbers as complete. No migration needed — deploy only.
- **Bulk edit can now set gift priority across a whole selection at once** — closes the item flagged in `ROADMAP.md` right after per-item priority shipped. The bulk-action bar already handled ownership/platform/tags/lists across a multi-item selection; priority used to mean opening each wishlist item's edit form one at a time. A new priority dropdown (High/Medium/Low/No priority) plus a "Set gift priority" button in `app/dashboard/DashboardClient.jsx`'s bulk bar applies to whichever selected items are actually on the wishlist and silently skips the rest — same "skip what doesn't apply" pattern the existing platform bulk-action already uses for item types with no platform field. "No priority" is a real, pickable option here too (not just a disabled/blank state), so bulk-clearing priority off a batch is exactly as valid an action as bulk-setting one. No migration needed — deploy only.
- **Accessibility pass, round 2** — closes several items from `ROADMAP.md`'s "Rest of the Accessibility checklist." Real keyboard support for the star rating: arrow keys now step the rating up/down half a star at a time from wherever focus sits in the group, on top of the existing per-star buttons' native Enter/Space support (`components/StarRating.jsx`). `TrophyToastListener` gets the same `role="status" aria-live="polite"` the regular toast stack already had — trophy-earned popups were the one toast stack on the site that never announced themselves. The Undo-delete toast now pauses its 6-second countdown entirely on hover or keyboard focus and restarts a full fresh window when you look away, instead of a flat window that could run out while you were still reading it (`app/dashboard/DashboardClient.jsx`). Site-wide `prefers-reduced-motion` support (`app/globals.css`) — the well-known "near-zero animation/transition duration" catch-all, which covers the trophy-toast slide-in animation and every hover-lift `transition: transform` rule on cards and buttons, on top of the home-page banner marquee's existing dedicated rule. And a new `/accessibility` page (linked in the footer, honest-status format like `/privacy`) listing what's covered and what's still open, rather than a formal conformance claim. No migration needed — deploy only.
- **Collection appraisal export** — closes the item flagged in `ROADMAP.md`. A new `/dashboard/appraisal` page (linked from "More actions") builds a polished, itemized document of your owned items — grouped by type with subtotals and a grand total, each row showing its details and whether its value comes from a purchase price or a checked eBay price (and when) — distinct from the existing raw CSV export. No new PDF library: it's a print-optimized page (`app/globals.css`'s new `@media print` rules hide the navbar/footer/buttons and reformat the document for paper), so the browser's own "Save as PDF" print target produces a real, clean PDF with zero new dependencies. Includes an explicit footnote that eBay-sourced values are current-listing prices, not a professional appraisal. No migration needed — deploy only.
- **Affiliate "buy it" links on the gift list** — closes the item flagged in `ROADMAP.md`. `/u/[username]/wishlist` now shows "Buy on eBay" / "Search Amazon" links under each item — real, working search links either way (built the same way "Check eBay price"'s search term already is, via `lib/marketPrice.js`), with optional eBay Partner Network / Amazon Associates affiliate tracking layered on top via two new `NEXT_PUBLIC_`-prefixed env vars (see README step 9) — a family member or friend shopping the list gets a direct path to actually buy something instead of routing through this app first. Hidden below the mobile breakpoint on purpose: the dense 4-per-row mobile grid has no room for a second line under each card without reintroducing the same row-height-mismatch bug already fixed once for search-result tiles, and every card already links through to the full item on tap regardless. No migration needed — deploy only.
- **"Quick add (search)"** — requested directly: a way to bulk-add items entirely in-app, no spreadsheet and no barcode required. Distinct from both existing bulk paths: CSV import needs a file, and "Scan multiple" (`BulkScanSession.jsx`) needs a physical barcode and inserts each one immediately as it's scanned. This new `BulkSearchAddModal.jsx` (a "Quick add (search)" entry next to "Scan multiple" in "More actions") instead reuses the exact same auto-fill search the single-item Add form already has per type — IGDB, TCGdex/Scryfall, Comic Vine, Open Library, iTunes, MusicBrainz, or the local consoles list — so you search a title, pick the right result, and it joins a running batch instead of saving right away; search again for the next one, review the batch (each row removable before committing), then "Add N items" inserts everything at once in batches of 100, the same chunking `ImportCsvModal.jsx` already uses. Field-mapping per result type mirrors `GameModal.jsx`'s `applySearchResult()` exactly, including the async Comic Vine detail fetch for writer/artist/publisher. Duplicate checking runs against both your existing collection and anything already sitting in the current batch, same "duplicate within this batch" rule CSV import already added for repeats within one file. Funko Pops aren't offered here, same reason the single-item form has no Search button for that type either (no live per-line database exists — see `ROADMAP.md`). Reuses `handleImported` (the same post-insert handler CSV import already calls), so newly bulk-added items correctly trigger a trophy re-check too. No migration needed — deploy only.
- **"+ Add Item" is now a chooser, not a single button — "Quick add (search)" moved out of "More actions"** — requested directly: Quick add (search) shipped a round ago but was buried a menu-click deep, when it's arguably the thing most people reach for right after signing up. `components/ActionMenu.jsx` gained optional `trigger`/`triggerClassName` props (defaults to the existing icon-only "⋯" behavior, so the two other call sites — the dashboard's "More actions" and the profile action row's "More collection tools" — are unaffected) so the same dropdown/positioning/click-outside/Escape logic could be reused for a second, visibly-labeled control instead of duplicating it. The dashboard header's old plain "+ Add Item" button is now a "+ Add Item ▾" dropdown with two entries: "Add one item" (opens the same single-item `GameModal` as before) and "Quick add (search)" (moved here from "More actions", which no longer has it). `WelcomePanel.jsx` — the empty-collection first-run screen — got the same swap: its "Scan a barcode" step (removed along with barcode scanning generally, see below) was replaced with a "Search and add several at once" step pointing at Quick add (search) instead. No migration needed — deploy only.
- **Barcode scanning removed** — requested directly, now that per-type title search covers auto-fill better than a UPC lookup ever did (see the two landing-page copy updates below): the camera-based scanner (`components/BarcodeScanner.jsx`, built on `@zxing/browser`/`@zxing/library`), the lookup API (`app/api/barcode-lookup/route.js`, which called UPCitemdb and Open Library), the Scan button and `handleBarcodeDetected()` in `GameModal.jsx`, and the entire "Scan multiple" bulk-add feature (`components/BulkScanSession.jsx`, which was built entirely around barcode capture and is superseded by "Quick add (search)" above) are all gone. The `@zxing/browser` dependency was dropped from `package.json` — run `npm install` after pulling to actually remove it from `node_modules`/`package-lock.json`. Deliberately **not** removed: the `games.barcode` database column and the plain "Barcode / UPC" text field in the item form (still there for manual entry or existing data — nobody's stored barcodes disappear), and `barcode` as a CSV import/export column (`lib/csvImport.js`/`lib/csvExport.js`), since those are just a stored field, not the scanning feature. Updated copy wherever the scanner was mentioned: `WelcomePanel.jsx` (see above), the privacy policy's third-party-services list (dropped UPCitemdb), two SEO landing-page feature bullets (`lib/landingPages.js`, now pitching search auto-fill instead — including a rewrite of the retro-game-inventory page's "coverage isn't universal" barcode bullet, since search genuinely does better for older/import titles than a UPC database did), and the App Store keyword list (`app-store-checklist.md`, swapped "barcode scanner" for "inventory app"). No migration needed for the code removal — deploy only; the DB column stays as-is.
- **"Quick add (search)" queue now has ownership/condition/price per item, not just a bare title/cover** — reported directly right after the above shipped: every field a search result actually returns (title, cover, genre, platforms, writer/publisher, etc.) was already flowing through, but ownership, condition, and purchase price never come back from any search source, and the queue had no way to set them — every batch-added item was silently landing as "Owned," no condition, no price, with the only fix being to open and edit each one individually afterward. Asked how much editing this needed (full per-item form vs. a shared batch-wide setting vs. a few key fields per row) and went with the middle ground: each queued row in `components/BulkSearchAddModal.jsx` now has its own small inline Ownership/Condition/Price controls (Condition hidden for comics, same rule `GameModal.jsx`'s own form already follows), defaulting to the same "Owned, no condition, no price" starting point as before so nothing changes if you don't touch them. Price follows the same `'' → null` conversion `GameModal.jsx` already uses on save, since an empty string fails a numeric column outright. Deliberately still not every field the full form has — Platforms already comes back from search for games, and anything else (purchase date, rating, tags, notes) stays reachable via Edit on the item after it's saved, same as before. No migration needed — deploy only.
- **Game search results now show which platform(s) a result covers, in both Quick add (search) and the single-item Add form** — reported directly: an IGDB result's `platforms` array is exactly what lands in the saved item's Platforms field if you click it, but the results dropdown only ever showed a title and a year, with no way to tell which platform(s) a given result actually represents before clicking — a real problem for anything re-released across multiple platforms over the years. Both `components/BulkSearchAddModal.jsx`'s and `components/GameModal.jsx`'s search-result rows now show up to 3 platform names (plus a "+N more" tail for anything with more tagged) alongside the year, via a small `resultMeta()` helper duplicated in both files rather than shared, since they don't otherwise import from each other. No migration needed — deploy only.
- **"Quick add (search)": Completeness replaces Price for games and consoles** — requested directly, right after ownership/condition/price landed in the queue above: for games and consoles specifically, Completeness (Loose/CIB/Box only) is the more useful thing to set at add time than purchase price, same as it's a real field on those two types in `GameModal.jsx`'s own form (and Price still isn't, there — that's `asking_price`/`price` set separately). Each queued row in `components/BulkSearchAddModal.jsx` now shows a Completeness dropdown instead of the Price input specifically when the batch's item type is Games or Consoles (matching the exact `isGame || isConsole` gate `GameModal.jsx` already uses for this field); every other item type in Quick add keeps the Price input, since Completeness isn't a real field for books, vinyl, comics, and the rest. Price itself isn't gone from the row shape — for a games/consoles batch it just isn't shown or set through the queue, and stays reachable afterward via Edit like any other field this round didn't add a control for. No migration needed — deploy only.
- **"Quick add (search)" queue now shows newest-added items at the top** — requested directly: `addToQueue()` used to append, so an item you'd just picked landed at the bottom of the list, below however many were already queued — backwards for a list meant to be skimmed as you go, especially once the ownership/condition/completeness-or-price controls above made each row worth actually glancing at. Now prepends instead. Commit order (and the partial-failure-recovery logic that trims however many rows actually made it into the database off the front of the queue) both still work correctly either way, since neither ever depended on the queue being in any particular chronological order — deploy only, no migration.
- **Multi-platform game results now ask which platform, instead of tagging the item with all of them** — requested directly, right after platform names were added to the results list above: seeing the platforms wasn't the whole fix, since clicking a result still set the item's Platforms field to literally every platform IGDB had tagged that game with — correct if someone genuinely means "I have this on all of these," but wrong for the much more common case of owning one specific platform's copy. Clicking a game result that lists more than one platform (in both `components/GameModal.jsx`'s Add form and `components/BulkSearchAddModal.jsx`'s Quick add) now pauses on a small "Which platform is this copy for?" picker — a button per platform, plus Cancel — instead of applying immediately; a result with only one platform (or none) still applies immediately exactly as before, so most searches see no change at all. Picking a platform re-runs the exact same fill-in logic as before, just with the result's `platforms` array narrowed to the one chosen first. No migration needed — deploy only.
- **Fixed: clicking outside Quick add (search) silently discarded the whole queue** — reported live after exactly that happened: the overlay backdrop's click-to-close handler called `onClose()` directly with zero warning, same as it does on every other modal in the app — reasonable when there's nothing to lose, but Quick add (search) is the one modal here where a single stray click can be minutes of searching-and-queueing work, since nothing in the queue is actually saved until "Add N items" runs. `components/BulkSearchAddModal.jsx` now routes every close path (the backdrop, the Cancel/Close button, and Escape — which this modal, unlike GameModal/ImportCsvModal/SteamImportModal/ShowcaseManagerModal/CustomListsModal, wasn't even wired to handle before this, so it also now gets the same focus-trap/auto-focus `useModalA11y` gives those) through one `requestClose()` function: closes immediately with an empty queue, otherwise shows a "Discard N queued items?" confirmation (Keep editing / Discard) instead of closing outright. Also blocks closing altogether while a commit is actively in flight, rather than tearing the modal down mid-insert. A successful "Add N items" still closes immediately with no prompt, same as before — the confirmation is only for discarding unsaved work, never for saving it. No migration needed — deploy only.
- **Console search now tolerates typos** — asked directly ("is there any way for it to understand slight typos"), and console search was the one search backend actually fixable here: games (IGDB), trading cards (Pokémon TCG/Scryfall), books (Open Library), VHS/DVD/CD/vinyl (iTunes/MusicBrainz), and comics (Comic Vine) all go through third-party APIs that already do their own relevance-ranked matching rather than a literal substring check, so they weren't investigated further this round — nothing here changes those. `lib/consoleList.js`'s `searchConsoles` previously did a plain `.includes()` scan of a 39-entry hardcoded list, so a single dropped or swapped letter ("nintendo swithc", "xbox seris x", "playstaton 5") returned nothing at all. It now falls back to fuzzy matching (a from-scratch Wagner–Fischer edit-distance implementation, no new dependency) whenever the plain substring match comes up empty for a query 3+ characters long: query and console name are each split into words, and every query word needs some name word within a length-scaled edit-distance tolerance (1 char for short words, up to 3 for long ones) — summed distance ranks the results. A second, independent check compares the whole query and name with all spaces/punctuation stripped, to catch word-boundary mismatches a word-by-word comparison misses on its own (e.g. "gameboy advence" vs. "Game Boy Advance"); a name counts as a match if either check succeeds. Substring matches (including plain prefixes like "play") still always rank first and are untouched — this only kicks in for genuine near-misses. Function signature and return shape are unchanged, so no caller changes were needed in `GameModal.jsx` or `BulkSearchAddModal.jsx`. No migration needed — deploy only.
- **"What do you collect?" preferences — a new signup (or an existing account that hasn't answered yet) is asked which of the 10 item types they actually collect, and everything else hides** — requested directly: "when someone registers I want them to be asked questions to tailor shelf life to them... if someone only collects books and comics, hide anything that isn't relevant to them," plus a "welcome back" version for every account that already existed before this shipped. New `profiles.enabled_item_types text[]` (defaults to all 10 types — an existing account's UI looks identical until it actually narrows things down) and `profiles.types_onboarded_at timestamptz` (null until answered or explicitly skipped) — see `collection-types-migration.sql`. A new `components/CollectingPrompt.jsx` modal renders whenever `types_onboarded_at` is null, which covers both audiences with one component and one flag: a genuinely brand-new account (no items yet) sees "What do you collect?" with every type checked by default, while an existing account sees "Welcome back — what do you collect?" pre-checked to whatever types it already actually owns (`lib/mosaicData.js`'s existing `availableTypes()` helper), so someone who's only ever logged games and consoles doesn't have to manually uncheck the other 8. "Skip for now" (or the backdrop/Escape, same as every other skip path in this modal) marks it seen without narrowing anything down — skipping is never destructive, it just stops asking. Enabled types are enforced (fully hidden, not just reordered) in three places: `GameModal.jsx`'s Type dropdown (Add Item and every edit), `BulkSearchAddModal.jsx`'s "Item type" picker (Quick add), and the dashboard's Filters "type" dropdown — each always still shows whatever type is *currently selected* even if it's since been disabled, so editing an old item or an in-progress filter never breaks. Deliberately left untouched: any item already logged of a disabled type stays fully visible everywhere (the grid, "Browse by system," Collection Insights, CSV export) — this only controls what you can newly pick, never what you've already saved; and CSV import still accepts rows of any of the 10 types regardless of what's enabled, so re-importing a mixed spreadsheet never silently drops rows. Always reachable afterward from a new 5th "Collecting" tab in Settings (see below). No caller of `GameModal`/`BulkSearchAddModal` needed changes beyond passing the new `enabledTypes` prop down from `DashboardClient.jsx`. Needs `collection-types-migration.sql` on existing projects.
- **"Profile settings" renamed to "Settings," with a new Collecting tab** — requested directly, alongside the preferences above: "we need a general settings button because some of the profile settings don't actually belong on the profile" — true since the tabs split (Profile/Notifications/Security/Data & danger zone), where three of the four tabs were never really about the profile at all. The dashboard header's "Profile settings" button and the panel's own heading are both now just "Settings" (the underlying `?settingsTab=`/`?settings=1` deep-link params, tab keys, and CSS classes are unchanged, so nothing bookmarked or linked breaks). Every other place in the app that referenced "Profile Settings" by name in user-facing copy — the profile-privacy note on `/privacy`, `WelcomePanel.jsx`'s "make your profile public" tip, `ImportCsvModal.jsx`'s currency note, `SteamImportModal.jsx`'s "connect Steam" error, the account-data-export JSON note, and one SEO landing page's FAQ answer — got the same rename; past `CHANGELOG.md`/`lib/whatsNew.js` entries were left as-written since those are a dated historical record, not live UI text. The new 5th tab, Collecting, holds the same checkbox list `CollectingPrompt.jsx` shows, backed by the same `enabled_item_types` column, and saves immediately on its own button (same pattern as Security/Data & danger zone) rather than through the shared Profile/Notifications save flow, since toggling it visibly changes what several other parts of the dashboard show. Blocks saving with nothing checked ("pick at least one — you can always add more later") rather than silently hiding every type. No migration needed beyond the one above — deploy only.
- **Dashboard "segmented shelf" — real visual identity per collectible type, v1 of the flagship differentiator discussed in chat** — the whole point being that no other site in this space (Collectr, TCGplayer, League of Comic Geeks, Discogs, CLZ/Collectorz) does real per-collector visual identity; everyone shows the same shell to everyone. A new `components/ShelfIdentityHero.jsx` renders at the top of the dashboard (only once there's at least one owned item — `WelcomePanel` still covers the true empty-collection case) with one row per collectible type the account both owns something in and has enabled (respecting `enabled_item_types` from the "What do you collect?" preferences): each row shows real cover art from that type's owned items (newest first), in that type's own accent color and icon, so a comic-heavy account and a vinyl-heavy account genuinely look different at a glance instead of sharing one generic dashboard shell. New `lib/mosaicData.js` exports `TYPE_COLORS`/`TYPE_ICONS` — a hand-picked (not hashed) map since there are only ever exactly 10 known categories, spaced around the color wheel for real visual separation, fed into the same `--tile-color` CSS custom property the existing "Browse by system" tiles already use so hover states get the same `color-mix()` treatment for free. This is also the multi-type answer settled on in chat: rather than picking one "winner" type to represent the whole account or blending several types' colors into one muddy hybrid, every enabled type simply gets its own zone — scales cleanly whether someone collects 1 type or all 10, with no special-casing for "how many is too many to blend." Clicking a row's header filters the dashboard grid straight to that type (reuses the existing Filters `fType` state); clicking a tile opens that item's detail view, same as clicking it in the main grid. Tile rendering reuses the shelf-mosaic feature's existing conventions (real cover art, or a stable `titleColor()`-derived letter tile when there's no usable cover) but is deliberately its own lighter component — the full mosaic (`/u/[username]/mosaic`) is a whole dedicated poster page; this is a compact hero sitting above a dashboard that has plenty else below it. Rows scroll horizontally via the existing `attachHorizontalWheelScroll` helper (`lib/useHorizontalWheelScroll.js`, the non-hook variant already used by the mosaic page's own rows, since the number of shelf rows here varies per account and a hook can't be called a variable number of times per render). Still to come on this front (see `ROADMAP.md`): type-specific "smart" Insights widgets, extending the same color/icon identity to public profiles, and type-aware microcopy/trophy-badge flavor. No migration needed — deploy only.
- **Fixed: Add Item and Quick add no longer restrict which type you can pick — picking a disabled type re-enables it instead** — reported directly right after the shelf hero shipped: the Type dropdown in `GameModal.jsx` and the Item type picker in `BulkSearchAddModal.jsx` had been narrowed down to only Collecting-enabled types the round before, which meant a type you'd turned off in Settings > Collecting became genuinely impossible to add without a trip back to Settings first — too strict for what was meant to be a decluttering preference, not a lock. Both dropdowns now always offer all 10 (well, 9 for Quick add — Funko Pops still have no search backend, unrelated to this) types again, same as before Collecting preferences existed. New behavior on top: saving an item (or committing a Quick add batch) with a type outside the current enabled set now calls a new `DashboardClient.jsx` function, `ensureTypeEnabled()`, which adds that type to `enabled_item_types` automatically — a no-op, no database write at all, for the common case of adding something you already collect. So the type just quietly shows back up everywhere it should (Filters, the shelf hero) the moment you actually use it, rather than needing a manual toggle first. Updated the Collecting settings tab's and `CollectingPrompt.jsx`'s own copy to describe this accurately — both used to (incorrectly, as of this fix) claim Add Item/Quick add respected the preference. No migration needed — deploy only.
- **Type-specific "smart" Insights widgets — not one generic breakdown for everyone** — the next piece of the "visual identity tailored to what someone actually collects" flagship idea (see `ROADMAP.md`), continuing straight on from the shelf hero: `/dashboard/insights` already had type-agnostic breakdowns (by type, platform, genre, value, spend by month); this adds a second `Made for what you collect` section underneath with panels that only make sense for a specific type, and only render at all for types you actually own something in — a games-only account never sees a raw-vs-graded card panel, a trading-card-only account never sees a backlog panel. Games get Backlog health (a `PLAY_STATUS_LABELS`-mapped breakdown of backlog/playing/completed/abandoned via the existing `play_status` field) and Trophy completion (average `trophy_completion` across owned games with a nonzero value). Trading cards get Raw vs. graded (whether `grade` is set) and Top card sets (`card_set`, reusing the same field trading-card master sets already key off of). Comics and Books each get their own Top series panel, both keyed off the shared `series` field per `supabase-schema.sql`'s field-reuse convention. Vinyl+CD (combined, since both are "music" in practice) get Record labels (`publisher`, doubling as label per the same convention) and By format (`format` — LP/45/CD/etc). Consoles get By manufacturer (`publisher`, doubling as manufacturer). DVD+VHS (combined) get By studio (`publisher`, doubling as studio). Funko Pops get By character (`player_name`, doubling as character). Every panel is a plain count-and-sort over data already logged — no new columns, no new API calls, just a new `countBy(items, field)` helper and a `BarList` render per panel, reusing the existing `BarList` component the type-agnostic panels already use. `app/dashboard/insights/page.js`'s server-side select gained the specific columns these panels need (`play_status, trophy_completion, grade, card_set, series, publisher, player_name, format`) alongside what it already fetched. No migration needed — deploy only.
- **Extend the type-driven visual identity to public profiles** — the next piece of the flagship idea (see `ROADMAP.md`), and the one with real growth value: a comic collector's `/u/username` should look different from a vinyl collector's, not the same generic shell everyone else gets. The dashboard's `components/ShelfIdentityHero.jsx` (real cover art, one row per owned+enabled type, each in its own `TYPE_COLORS`/`TYPE_ICONS` accent — see the shelf-hero entry above) needed zero changes to reuse here: it already filters to `ownership === 'owned'` and returns `null` on an empty result internally, so the same component drops straight into `app/u/[username]/ProfileTabs.jsx`'s Collection tab, fed `profile.enabled_item_types` (already selected via that route's existing `select('*')` — no new query). Placed inside the Collection tab specifically, not above the tab bar the way the dashboard does it, since clicking a row or a tile here is a view into the grid directly below it, not the whole page. New interaction, since public profiles have no `ItemDetailModal` the way the dashboard does: clicking a row (or a tile of a type series-view doesn't cover) filters the Collection grid to that type — click the same row again to clear it, or use the new "Showing X · Clear" line above the grid; clicking a tile of a series-supported type (games, comics, trading cards, Funko Pops) instead opens that exact item straight into the existing `SeriesModal`, same as clicking its card in the grid already does. Caught during this round, not shipped as a regression: the "Showing X" indicator's className was originally `"sub"`, which turned out to be scoped-only in `app/globals.css` (`.modal .sub`, `.form-card .sub`, `.welcome-step .sub` — no bare `.sub` rule), the exact same trap the Trophy completion Insights panel hit last round — fixed with an inline `color: var(--text-dim)` instead of relying on a class that would've rendered unstyled default text. No migration needed — deploy only.
- **Emoji removed from the site and app** — requested directly. An audit turned up exactly one place a real emoji was ever used anywhere in `app/`, `components/`, or `lib/`: `lib/mosaicData.js`'s `TYPE_ICONS` map (🎮💥🃏🎵📚📀📼💿🕹️🧸), consumed by exactly one component, `components/ShelfIdentityHero.jsx`'s per-row icon on the shelf hero (both the dashboard's and, as of the round above, public profiles'). Everything else that superficially looked emoji-adjacent in a broad sweep — star ratings (★), close buttons (✕), the mobile nav's hamburger icon (☰), edit pencils (✎), reorder/chevron/back arrows — is plain monochrome Unicode UI iconography, not color emoji, and was left alone. Replaced `TYPE_ICONS` with a new `TYPE_MONOGRAMS` export (two-letter abbreviations — GM/CM/TC/VN/BK/DV/VH/CD/CN/FP — a single initial collides for several types: Comic/CD/Console would all be "C," Vinyl/VHS would both be "V"), rendered in `.shelf-hero-icon` as a small outlined chip using the same `--tile-color`/`color-mix()` treatment `.system-tile` already established, rather than a solid fill — a deliberate choice, not just a style preference: several `TYPE_COLORS` (trading cards' gold especially) are too bright for white text to sit on at a readable contrast ratio, and this sidesteps that entirely by using the accent as text/border color against the existing card background instead. No migration needed — deploy only.
- **Type-aware microcopy and trophy-badge flavor text — the last piece of the visual-identity flagship idea** (see `ROADMAP.md`, now fully shipped): empty states and trophy copy speak a collector's own language instead of one generic voice for every account. New `lib/mosaicData.js` exports: `TYPE_NOUNS` (the word for "where these live" per type — backlog for games, long box for comics, binder for trading cards, crate for vinyl, setup for consoles; books/DVDs/VHS/CDs/Funko Pops share "shelf," which is also the untouched default, so those render identically to unflavored copy on purpose rather than forcing an invented synonym), `TYPE_SINGULAR` (one item of a type, for "every X you want to keep track of"), and `dominantType(items)` (the item_type with the most items in a list, ties broken by `CATEGORY_ORDER`, `null` for an empty list — powers flavor without requiring someone to have narrowed Collecting preferences down to exactly one type). Three surfaces flavored: `components/WelcomePanel.jsx` (a brand-new signup's very first, still-empty dashboard screen) swaps "This is your shelf" for "This is your long box"/binder/crate/backlog/setup when exactly one type is enabled via the "What do you collect?" preferences — the only signal available before any item exists to compute a real dominant type from; `app/u/[username]/ProfileTabs.jsx`'s "No items on this shelf yet." does the same for a still-empty public profile, phrased as "Your long box is empty"/"Sam's crate is empty" depending on whether it's your own profile; and `components/TrophyCase.jsx` gets a new small italic flavor line under four of the cross-type "own N items" milestone trophies (First Pickup, Double Digits, Centurion, Half a Thousand — the ones with no type identity of their own, unlike e.g. "Bookworm" which already names comics directly), shown only once earned and only when a real dominant type exists — "Your binder's getting thick" for a trading-card-heavy account hitting Double Digits, "A long box any shop would envy" for a comics-heavy Centurion, and so on for games/vinyl/consoles. Purely presentational — no new database columns, no change to how any trophy is actually earned, and the home page's hardcoded `TrophyCase` marketing demo (`app/page.js`) is unaffected since it passes no `dominantType` and the flavor line simply doesn't render without one. No migration needed — deploy only.
- **Fixed: footer's last link was unreachable on mobile** — found during a mobile-layout audit prompted by "mobile layout feels disorganized in places" (see `ROADMAP.md`). `.site-footer` in `app/globals.css` was a plain `display: flex` row with no `flex-wrap` — fine back when it only held the copyright line and a couple of links, but the row has since grown to six items (copyright text plus five links, including the newer Collectible Database one), and on a 375px-wide phone the row ran out of horizontal space before all six fit. Instead of wrapping, flex just kept shrinking link text ("Privacy Policy" splitting across two lines on its own) until the last item, "Collectible Database," got pushed straight past the right edge of the viewport — and since `html`/`body` carry a site-wide `overflow-x: hidden` safety net, that link wasn't just visually clipped, it was completely unreachable, not even by scrolling sideways. Added `flex-wrap: wrap` to the base rule (a no-op at any width the footer was already tested at — desktop and tablet both have room to spare in one row) plus a small `@media (max-width: 640px)` override so the wrapped result reads as a centered, intentional block on a narrow screen instead of a ragged left-aligned wrap. Audited the rest of the mobile surface alongside this fix — home, leaderboard, public profiles (game-heavy and comic-heavy), `/lists`, `/whats-new`, `/accessibility`, `/feedback`, `/signup`, `/login`, `/collectible-database`, and `/players` — nothing else turned up a concrete, confirmed layout bug; noted as a broad pass, not an exhaustive one, since the original report was "a general sense," not a specific page. No migration needed — deploy only.
- **Closed out the mobile audit: Trophies/Comments tab toggle and the item-detail series modal, confirmed clean** — the previous round's mobile pass couldn't reach either of these (both are client-side-only interactions with no URL route, and that round's browser tooling could only navigate, not reliably click). With real click access this round, both got tested directly on a 375px viewport on a live profile: the Collection/Trophies/Comments tab toggle switches cleanly with no layout shift, the Trophy Case list and empty Comments state both render correctly at that width, and tapping a collection tile opens the `SeriesModal` (tested on a 230-entry Call of Duty series) with its "N of collectible items" header, tile grid, and "Show 100 more" pagination button all fitting the viewport with no overflow, plus Close working as expected. No bugs found — closes `ROADMAP.md`'s "Mobile layout feels disorganized in places" as a fully-audited item rather than an open one with an untested corner.
- **App Store submission resubmitted with clean screenshots** — the second Guideline 4.1(a) Copycats rejection ("screenshots includes references to Pokemon") had two possible sources: the home page hero's live-fetched Charizard VMAX card (see the fix above this one) and the actual screenshot images already uploaded to App Store Connect, which live outside this codebase and couldn't be checked from here directly. Went through the live submission today: confirmed all 7 iPhone and 6 iPad screenshots are clean of any Pokémon or other third-party-IP references, then found the real reason the submission had sat idle for 15 days — it was never actually stuck in Apple's queue at all. The Aug 12 rejection message left the submission in a dead "Waiting for Review" state; editing metadata/screenshots on an already-submitted version doesn't restart a review pass on its own, so nothing had moved since the rejection regardless of what got fixed. Canceled that stale submission and resubmitted fresh, which is now genuinely queued for a first review pass. Nothing code-side left to do here — see `ROADMAP.md`.
- **Fixed: light-mode contrast on status/tier colors — the first real bug found in the accessibility checklist's contrast-audit item** — computed exact WCAG contrast ratios (from-scratch relative-luminance implementation, not eyeballed) for every status/tier/accent color in `app/globals.css` against both themes' actual backgrounds. Dark mode is fine (worst case `--accent`/`--wishlist` at 3.83:1/3.26:1, both still clearing the 3:1 large-text/UI-component threshold). Light mode had a real, silent bug: `body[data-theme='light']` never redefined `--owned`, `--sold`, `--tier-bronze`, `--tier-silver`, or `--tier-gold` at all, so each one was quietly inheriting its dark-tuned value — as low as 1.58:1 to 2.96:1 against a white card, nowhere near AA's 4.5:1 minimum for text. Concretely: the leaderboard's gold/silver/bronze podium text (`.podium-medal` in `app/leaderboard/LeaderboardClient.jsx`), the "Owned"/"Sold" badge text (`.badge.owned`/`.badge.sold`), and toast-success messages (`.toast-success`) were all effectively low-contrast-grey-on-white in light mode. `--warn` did already have a light override (`#c97a12`) but only measured 3.35:1 — passes the 3:1 UI-component bar its one text-adjacent usage (`.star-rating-fg`) needs, but not full AA text contrast. Added/updated six light-theme overrides — `--warn: #a8660f`, `--owned: #00856a`, `--sold: #eb002a`, `--tier-bronze: #a66728`, `--tier-silver: #6574a0`, `--tier-gold: #937014` — each computed via hue-preserving binary search (HLS lightness only, same hue/saturation as the original) targeting 4.6:1 against `#fff` (a small safety margin over the 4.5:1 minimum, since some usages layer a semi-transparent background under the text). Deliberately left `--accent`, `--accent-2`, `--danger`, `--wishlist`, and `--tier-platinum` untouched this round: `--danger` measures 4.43:1 in light mode (borderline, not a clear failure), `--tier-platinum` is never used as a text color anywhere in the codebase, and `--accent`/`--wishlist` are the site's actual brand purple — changing that felt like a design call worth its own explicit sign-off rather than folding into an autonomous contrast pass. Dark mode's `:root` block is completely untouched. No migration needed — deploy only.
- **Closed out two more accessibility-checklist sub-items, no code change needed** — "differentiate status without relying on color alone": checked the two UI elements that convey meaning primarily through color (the leaderboard podium and trophy tiers) and both already pass. `LeaderboardClient.jsx`'s podium renders literal `1st`/`2nd`/`3rd` text (`.podium-medal`) alongside the gold/silver/bronze styling, not color alone. `TrophyCase.jsx` renders the tier name itself (`Bronze`/`Silver`/`Gold`/`Platinum`, via `TIER_LABEL`) as visible text under every trophy, same as ownership badges already did (`GameCard.jsx`'s `.card-ownership-flag`, confirmed a prior round). Pinch-to-zoom: also already fine — `app/layout.js`'s `viewport` export sets no `maximumScale`/`userScalable: false`, so nothing was ever disabling it. Manual text-size control: investigated and deliberately not attempted this round — `app/globals.css` has hundreds of scattered hardcoded `font-size: NNpx` declarations with no root rem/em scale feeding them, so a root-font-size-based control wouldn't actually cascade to most of the site's text; the CSS `zoom` property would work but felt too risky to ship blind from here with no way to visually verify a site-wide zoom change before it goes out — flagging in `ROADMAP.md` as a scoped, specific blocker rather than a vague "not done yet." Keyboard tab-order: not checked this round, left open in `ROADMAP.md`.
- **Removed the shelf-hero letter-monogram badges entirely — requested directly** ("i also dont like the letters we replaced the emojis with, id rather have neither"). The previous round's fix for "remove emojis" swapped `lib/mosaicData.js`'s emoji `TYPE_ICONS` map for a two-letter `TYPE_MONOGRAMS` one (GM/CM/TC/...), rendered as a small colored chip (`.shelf-hero-icon`) next to each shelf-hero row's label. That chip is gone now, along with `TYPE_MONOGRAMS` itself (confirmed via a full-codebase grep that `components/ShelfIdentityHero.jsx` — used for both the dashboard and public profiles — was its only consumer, so nothing else references it) and the now-unused `.shelf-hero-icon` CSS rule. Each row still identifies its type without any icon at all: the label text, its color, the row's hover underline, and the plank beneath it all already carry that type's `--tile-color` accent (`TYPE_COLORS`, unchanged) — removing the chip loses no information, just the badge itself. `TYPE_COLORS` is untouched and still feeds the rest of the row's styling. No migration needed — deploy only.
- **Fixed: leaderboard podium's keyboard Tab order didn't match its visual layout — closes the last open item on the accessibility checklist** — a code-level audit of every `order:` and `tabIndex` usage site-wide (the general technique for finding this class of bug — CSS `order` moves an element visually without moving it in the DOM, so screen-reader/keyboard focus order can silently drift out of sync with what's on screen) turned up exactly one real hit: `app/leaderboard/LeaderboardClient.jsx`'s podium renders 1st/2nd/3rd place in that DOM order, then `app/globals.css`'s `.podium-place.place-1/2/3` used `order: 2/1/3` to lay them out visually as 2nd (left), 1st (middle), 3rd (right) — the classic podium shape. That meant Tab landed on the visually-*middle* 1st-place card before the visually-*left* 2nd-place one, confirmed live by walking the actual Tab order on `/leaderboard` before touching anything. Fixed by sorting `podiumRows` into the real visual order (`PODIUM_VISUAL_ORDER`, a `{1:2, 2:1, 3:3}` lookup) before mapping, so the DOM order already matches what's rendered — removed the now-redundant CSS `order` rules entirely rather than leaving dead styles behind. `.place-1/2/3`'s other rules (medal color, avatar size, step height) are class-based, not position-based, so nothing else about the podium's look changed. The one other `order:` usage site-wide, the home page's `.value-row.reverse .value-text`, was checked and left alone — that row's `.value-text`/`.value-visual` are both plain text/decorative content with no focusable elements inside on that specific row, so no actual focus-order mismatch exists there to fix. Also confirmed structurally clean (no `order`/positive `tabIndex` at all) on the dashboard grid and search results — everything renders in plain array-map DOM order with no separate visual-reordering mechanism, so their Tab order already matches what's on screen; the one `tabIndex={0}` in the codebase (`MosaicClient.jsx`) is the "make a scroll region focusable at its natural position" pattern, not the `tabIndex > 0` anti-pattern this audit was looking for. Closes `ROADMAP.md`'s accessibility checklist down to one remaining item (the manual text-size control). No migration needed — deploy only.
- **Added a real automated test suite — first item off `ROADMAP.md`'s Medium tier this round** ("Automated tests — there currently aren't any"). Added Vitest (`npm install --save-dev vitest`, plus `vitest.config.mjs` — a `.mjs` extension specifically, not `.js`, so its ESM `import`/`export` syntax doesn't collide with the rest of the project's CommonJS `require()`-based scripts under `package.json`'s default CJS `"type"`) and 4 test files: `lib/duplicateCheck.test.js`, `lib/currency.test.js`, `lib/mosaicData.test.js`, `lib/seriesLookup.test.js` — 48 tests total, all passing (verified with a real `npm install && npm test` run, not just written and assumed correct). Deliberately scoped to pure-logic modules only for this first pass — no React components, no Supabase/API calls, so nothing needs a DOM or a database to test — chosen specifically because they're the highest-value, easiest-to-regress-silently code in the project: card-number normalization (`4/102` → `4`, `#004` → `4`, `TG01` stays `tg01`), duplicate-title matching, the shelf-mosaic grouping/packing/capping logic, and the series/master-set matching-key logic (`ownedKeysFor`/`variantHintsFor`/`normalizeSeriesResponse`/`prefillFromSeriesEntry`) — exactly the kind of quietly-precise string/array logic that's easy to subtly break in a future edit and hard to notice by eye. `npm run test:watch` re-runs on save; `npm test` (also wired as `"test"` in `package.json`) runs once. React component tests, API route tests, and anything needing Supabase are a deliberately separate next step, not attempted here. Needs one `npm install` on existing checkouts to pull in the new dev dependency; nothing else to configure, and nothing about `npm run build`/`npm run dev` changed.
- **Broken cover-art links now fall back gracefully everywhere, not just in most places — the "at least validating them" half of `ROADMAP.md`'s "Image handling for cover art"** ("cover URLs are trusted as-is right now... relying on external links staying alive"). `GameCard.jsx`/`RecommendationCard.jsx`/`LeaderboardThumb.jsx`/`ShelfIdentityHero.jsx`/`PlayNextWidget.jsx`/`MosaicClient.jsx` already had an `onError` fallback (an established idiom: swap the broken `<img>` for the same `.placeholder` div already used for "no cover saved at all"); a codebase-wide sweep for every other place a cover renders found six real gaps that still showed the browser's default broken-image icon on a dead link: `components/CollectibleCard.jsx` (Collectible Database search results), `components/PublicListCard.jsx` (`/lists` directory tiles), `components/ItemDetailModal.jsx` (the big item detail modal), `components/SeriesGrid.jsx` ("See full series"/master-set grid tiles — its placeholder needed a DOM-built `replaceWith()` instead of the usual static-string `outerHTML` swap, since its placeholder text is a dynamic first-letter-of-label, not a fixed "No Cover"), `app/collectible/page.js` (the Collectible Database detail page — a Server Component, so the raw `<img>` there couldn't take an `onError` prop at all; swapped for the existing `CoverThumb` client component `LeaderboardThumb.jsx` already exports for exactly this reason instead of duplicating the logic), and `components/BulkSearchAddModal.jsx`'s two small inline-styled search-result/queue thumbnails (no adjacent placeholder box to swap to there, so these just hide on error instead — the title/meta text next to them already carries the real info). Actually downloading and re-hosting cover art (the other half of that `ROADMAP.md` line) is a bigger, separate infra/cost decision, left untouched — this round is the low-risk "never show a broken-image icon" half. No migration needed — deploy only.
- **Opt-in monthly email data backup — closes `ROADMAP.md`'s "Opt-in automatic backup of exports"** ("'Download my data' and 'Export CSV' already exist but are manual... would matter more once someone's tracking a genuinely valuable collection and wants an off-site backup without remembering to click Export"). A new "Email me a monthly backup" checkbox in Settings > Data (`profiles.email_backup_enabled`, see `email-backup-migration.sql`, defaults to `false` — nobody starts getting a new automated email just because the column now exists) saves instantly on toggle, same pattern as Collecting/Security/the rest of Data & danger zone. A new cron, `/api/cron/email-data-backup` (added to `vercel.json`, `0 11 1 * *` — the 1st of the month, noon UTC-ish, clear of the other 4 crons' times; registered in `lib/cronHeartbeat.js`'s `CRON_JOBS` so it shows up on `/admin/stats` automatically), emails each opted-in account the same two files the manual buttons already produce — collection CSV and the "Download my data" JSON — as real attachments, via a new `lib/accountBackup.js` that reuses the exact same query shapes as `/api/account/export` and the exact same column mapping as `lib/csvExport.js`'s `gamesToCsvRows`, so the automated email can never quietly drift from what the two manual downloads produce. Needed one new capability in `lib/resend.js`: an `attachments` param (Resend's own `[{filename, content: base64}]` shape) — the wrapper had never sent an attachment before. A per-account email address comes from `admin.auth.admin.getUserById()` (`profiles` has no email column — same lookup the newsletter send already does, and the same "fine at today's scale, would need real pagination well before this list hits the thousands" caveat that one already documents). One account's send failing (bad attachment, bounced address) is logged and counted, not treated as a whole-job failure, mirroring `price-drop-check`'s per-item-vs-job-level error split. Needs `email-backup-migration.sql` on existing projects; reuses the `RESEND_API_KEY`/`RESEND_FROM_EMAIL`/`CRON_SECRET`/`SUPABASE_SERVICE_ROLE_KEY` env vars every other cron/email feature already needs — nothing new to configure. Worth telling Taylor directly: if he wants a "quick action" dead-man's-switch check added for this cron too (see `ROADMAP.md`'s "External cron watchdog" line), it's now a 5th job, not 4.
- **Dependency security patches — `npm audit` cleanup, prompted directly by Taylor's post-`npm install` terminal output showing "7 vulnerabilities (4 low, 3 high)"** ran the real audit both on Taylor's actual machine (via device shell) and against a fresh clone, to work from the authoritative report rather than guessing. Split the 7 into three separate buckets by actual risk instead of reflexively running `npm audit fix --force`, which would have silently bundled a 2-major-version Next.js jump in with everything else: (1) `nanoid` (high — "custom generators can loop indefinitely when size is zero") was fixable in-range with plain `npm audit fix`, no `package.json` change at all, just a `package-lock.json` update — tested clean, zero risk. (2) `@supabase/ssr` and `@supabase/supabase-js` (4 low — advisories in `cookie` and `@supabase/auth-js` respectively) needed a real `package.json` bump since both were pinned to exact versions with no `^`; rather than take npm's suggested jump straight to the latest `@supabase/supabase-js` (`2.112.4`, over a year of releases past what was installed), checked the actual vulnerable range in the advisory (`2.41.1 - 2.49.10`) and confirmed `2.50.0` — the very next stable release — is already outside it, so that's what shipped instead (`@supabase/ssr` → `0.5.2`, the minimal patched version npm itself suggested). Verified safe before shipping: bumped both in a scratch clone, ran `npm install` (audit dropped from 7 to 3, then to 2 after `npm audit fix` also picked up the now-unblocked `nanoid` fix), reran the full Vitest suite (48/48 still pass), and ran a real `next build` end to end with placeholder env vars — compiled clean, every route built, no errors. (3) The remaining 2 high-severity issues (`next`, `postcss`) only resolve via `next@16.3.3` — npm's own output flags it "a breaking change," a 14→16 jump, and one of the bundled advisories (`GHSA-9g9p-9gw9-jx7f`) specifically calls out this project's own wide-open `images.remotePatterns: [{ hostname: '**' }]` config in `next.config.js`. Deliberately not attempted this round — a live production site's major framework version doesn't belong in the same pass as a routine dependency audit; flagged in `ROADMAP.md` as its own dedicated, carefully-tested upgrade. No migration needed for the bumps that did ship — deploy only, but see chat for the exact `npm install && npm audit fix` follow-up needed on Taylor's own machine after pulling.
- **Real master sets for Magic: The Gathering — closes `ROADMAP.md`'s "Magic master sets"** ("Scryfall has clean, free per-set card data... including variant/frame info, so the same approach TCGdex got should carry over as a real Magic-specific backend"). New `lib/scryfallSetLookup.js` and `/api/mtg-master-set` mirror `lib/tcgdexSetLookup.js`/`/api/pokemon-master-set`'s shape (same `{seriesName, entries}` response, same `no_series_value`/`no_series`/`query_failed` error codes) but are a genuinely separate implementation, not a shared one — Scryfall's data shape isn't TCGdex's. The real difference turned out to be a nice one: TCGdex only reports a card's print variants on that card's own detail endpoint (one fetch per card, the whole reason the Pokémon side needs the caching described below), but Scryfall's `/cards/search?q=e:<code>&unique=prints` already returns every card in a set *with* its real `finishes` array (nonfoil/foil/etched) in a single paginated call (175 cards/page) — no per-card fetch, no caching needed, a live request comfortably fits inside a normal timeout even for a large set. No API key required either. `lib/seriesLookup.js`'s `guessCardVariant` (shared by every trading-card TCG's owned-item matching) gained `foil`/`etched` keyword detection alongside the existing Pokémon vocabulary (`reverse`/`1st edition`/`promo`/`holo`) — typing "Foil" or "Etched Foil" into a Magic card's Variant Notes is what makes an owned copy match its master-set tile, same convention as Pokémon already had. `lib/useSeriesLookup.js`'s trading-card path now tries TCGdex first, then Scryfall on a `no_series`, then falls through to the crowdsourced backend Funko Pops use if neither recognizes the set — there's no field on a logged card saying which TCG it's from (`card_set` is free-typed), so this just tries both real backends in sequence; a genuine Pokémon card resolves on the first try and never reaches Scryfall, a genuine Magic card resolves on the second. The final "couldn't find a match" error message was updated to mention both TCGdex and Scryfall instead of singling out Pokémon. Verified via mocked-fetch tests (this sandbox's network egress blocks both api.scryfall.com and api.tcgdex.net directly, same restriction that would apply to any external API from here — confirmed via curl, not something specific to this code) covering pagination across pages, the nonfoil→shared-'normal'-key mapping, hint unioning, double-faced-card cover fallback, and the no-match case; a real `next build` also compiled clean with the new route registered. Added a new test case to `lib/seriesLookup.test.js` for the foil/etched keyword additions (49 tests total now, up from 48). No migration needed — deploy only.
- **Pokémon master sets now show every real print variant, not just ones you already own — closes `ROADMAP.md`'s "Pokémon master sets: show variants you don't own yet"** ("the shipped version only expands a card into its real Normal/Reverse/Holo/1st Edition tiles once you've already logged a variant copy of it... fetching real variant data for every card in a 100-250+ card set on every click risks a timeout and hammers TCGdex's free API. A precomputed/cached version... would close the gap"). Built exactly that: a new `master_set_cache` table (`master-set-cache-migration.sql` for existing projects) stores, per TCGdex set, a minimal `{[cardId]: {variants}}` blob — just the variant flags each card needs, not the full TCGdex card detail, to keep rows small. A new weekly cron, `/api/cron/refresh-master-sets` (`vercel.json`, Mondays 8am UTC; registered in `lib/cronHeartbeat.js` so it shows up on `/admin/stats` — 6 cron jobs now, not 5, see the note below), finds every distinct `card_set` value logged against a `trading_card` item, resolves it against TCGdex, and — for up to 5 sets per run that are missing or older than 30 days — fetches real per-card detail for *every* card in the set (not just hinted ones) via a genuinely concurrency-limited fetcher (`lib/tcgdexSetLookup.js`'s new `fetchDetailsForCards`, capped at 8 in-flight requests, replacing the old unbounded-`Promise.all` approach that was only ever safe because it was capped at 25 cards) and upserts the result. Deliberately scoped to only sets someone here has actually logged a card from, not TCGdex's entire catalog — most sets nobody on Shelf Life owns anything from, so pre-fetching them would just be wasted calls against a free, community-run API; and deliberately spread across multiple weekly runs (5 sets/run) rather than one big pass, so a single run stays well inside Vercel's function budget and stays a good citizen of TCGdex's rate limits. `lib/tcgdexSetLookup.js`'s `getMasterSetEntries` gained an optional `cacheLoader` callback (kept as a generic async function rather than a direct Supabase import, so the module itself stays DB-agnostic) — `/api/pokemon-master-set` passes one backed by the service-role client; a cache hit means every card in the set renders its real variants immediately, no live TCGdex calls at all; a cache miss (a set nobody's viewed since the cron last ran, or a brand-new one) falls back to the original narrow behavior unchanged, so "See master set" never blocks on fetching an uncached 250-card set live. Your own logged variant still always gets a tile either way — the hint-union logic from the original implementation is untouched, just layered on top of cached-or-narrow data instead of narrow-only. Extracted the entry-building logic into a new pure `buildEntries()` so both the live route and the cache-building cron share exactly the same card→tile logic. Verified via mocked-fetch tests covering both the no-cache narrow path (backward-compat check — still only fetches hinted cards) and the cache path (zero live per-card fetches, every card shows real cached variants, including one nobody's logged), plus a real `next build` with the new cron route registered. No user-facing UI changed at all — SeriesGrid.jsx already greys out/shows every entry generically regardless of where its variant data came from. Worth telling Taylor directly: if he wants the "External cron watchdog" dead-man's-switch check extended to this new cron too (see `ROADMAP.md`), it's now 6 jobs, not 5 — Magic needs no cron of its own (see above), so this is the only addition.
- **Visual design pass — brand tokens, category rail, trophy case, elegant cover placeholders, public profile mosaic — requested directly with a full written brief** (kept the existing dark theme; no new brand, per the brief). Reused every existing component rather than rebuilding anything — this is a restyle, not a rearchitecture. Breakdown:
  - **Brand tokens** (`app/globals.css` `:root`/`body[data-theme='light']`): navy/near-black base (`--bg` #0f1220, `--card` #171b2e, `--bg-alt` #1e2440 for modals/raised surfaces — mapped onto the existing 3-tier `--bg`/`--card`/`--bg-alt` names by tracing real usage rather than renaming any selector), warm gold (`--accent-gold` #d4af37) as the primary accent — buttons, links, "owned" status, trophies — and mint (`--accent-mint` #7dffb3) as the secondary/success accent, replacing the old purple/teal (#6c5ce7/#00d2a8) pairing everywhere it appeared (button gradients, badges, ambient page-background tints, the wrapped-year card, roughly 20 hardcoded fallback references total, not just the token definitions). Card/pill radius bumped from 12px/none to 16px/999px (`--radius`/new `--radius-pill`) to read softer per the brief. New `--ink` (#06070f) token for text sitting on a solid gold/mint fill — computed real WCAG contrast by hand first (white-on-gold lands ~2.1:1, fails AA; near-black-on-gold clears ~8.9:1) rather than guessing, and swapped roughly 6 rules (buttons, card nameplates, avatar initials, the skip-link) from white to `var(--ink)`. Light theme's gold/mint got their own darkened values (`#8a6a1a`/`#00a389`, confirmed ~5:1+ against white) rather than silently inheriting the dark-theme hex values, which would have fallen as low as ~1.6:1 in a light card. `TYPE_COLORS` in `lib/mosaicData.js` (the Shelf Identity Hero's per-category rainbow) and `lib/platformColors.js`'s real per-brand platform tile colors were deliberately left alone — different, already-established features, not the "rainbow chaos" the brief's category-chip note was about.
  - **Category rail** (new `components/CategoryRail.jsx` + `.category-rail`/`.category-chip` in `globals.css`) — a horizontal row of filter chips above the dashboard's Shelf Identity Hero, tinted with `color-mix(var(--accent))` rather than a solid fill so a long row of categories doesn't read as a rainbow; the active chip gets a gold underline + brighter text instead of a solid background. Wired into `DashboardClient.jsx` alongside the existing type-filter `<select>` (which stays, for accessibility/no-JS parity) — both drive the same `fType` state.
  - **Elegant cover placeholders** (new `components/CategoryIcon.jsx`, a small hand-drawn inline-SVG glyph per item type — no icon library in this project, so this follows the same "plain SVG/emoji, no dependency" pattern everything else here already uses) — `GameCard.jsx`'s grid tile and `ItemDetailModal.jsx`'s detail view now show a category-specific icon + "No Cover" label on a subtle gold-tinted gradient instead of bare text, both when there's no cover URL at all and when a real cover URL 404s (swapped the old raw-DOM-string `onError` hack for a proper `coverFailed` state, so React stays in control of the DOM instead of hijacking it out from under itself).
  - **Trophy Case** (`TrophyCase.jsx` + rewritten trophy CSS) — restyled as a dark velvet panel with a "Trophy Case · Level N" header and an "X / Y earned · Z pts" substat line; medallions (`.trophy-icon`) grew from 30px to 44px with layered inset box-shadows for a real bevel per tier, and a locked (not-yet-earned) trophy now shows a 🔒 glyph over its desaturated medallion instead of just sitting greyed out with no explanation.
  - **Homepage hero shelf mosaic** (`.hero-showcase-item` in `globals.css`, no JS/JSX changes — it's already real `GameCard` markup) — tightened to an 8px tile radius with a gold-tinted hover lift, and the existing mobile horizontal-scroll row now snaps (`scroll-snap-type: x mandatory`) instead of free-scrolling.
  - **Public profile header** (`app/u/[username]/page.js`) — a "Public shelf"/"Private" badge next to the display name (owner-only, mirrors `profile.is_public`), and a faint (20% opacity) mosaic of up to 8 of the collector's own real cover images behind the header — never placeholders, never another collector's items, and only rendered when `games` was actually fetched (i.e. never leaks anything through a private profile).
  - **Empty states** (`WelcomePanel.jsx`) — a small plain line-art walnut-shelf-with-gold-hook illustration above the existing (unchanged) type-aware welcome copy and three add-item CTAs.
  - **PWA/app-wrapper polish** — `.navbar` now pads for `env(safe-area-inset-top)` so the navbar itself clears a notch/status bar when installed, not just the page body (which already had left/right insets). `app/manifest.js`, `public/sw.js`, and `app/layout.js`'s `<head>` metadata were surveyed and left untouched — all three already exist with real, correct, sophisticated content (a full manifest with shortcuts/screenshots/icons, a genuine network-first-navigation/cache-first-assets service worker); an initial pass on this round wrongly assumed they were missing (survey tooling missed Next's `app/manifest.js` route-convention file) and started rebuilding them from scratch before catching the mistake against the real repo — nothing from that false start shipped.
  - **Deliberately not done this round, flagged as "later" per the brief's own framing**: `app/icon.png`/`app/apple-icon.png` (the real installed-app icon) still use the old blue palette — a refresh to navy/gold is a genuine follow-up, not done here since it also feeds `app-store-checklist.md`/App Store screenshot assets and deserves its own careful pass rather than a same-round swap. No migration needed — this whole pass is CSS/component-only, no schema or data changes.
- **Extend "See full series" to Vinyl/CD/DVD/VHS/Book — closes `ROADMAP.md`'s "Extend 'See full series' to every remaining collectible type"** (games/comics/trading cards had real per-type backends already, Funko Pops had the crowdsourced fallback; these five had nothing at all). None of these five have a natural numbered-series field the way comics/cards/Funko Pops do (issue/card number) — there's no "issue 1 of 12" for an artist's discography or an author's bibliography — so "series" for these means "everything by the same creator" instead, matched by title. `lib/seriesLookup.js` gained a `CREATOR_FIELD_BY_TYPE` map (Vinyl's own dedicated `artist` column; CD/DVD/VHS/Book all share the `writer` column, labeled Artist/Director/Director/Author respectively — mirrors the field mapping `GameCard.jsx`'s `getStatRows` already used) and every function that branched on `NUMBER_FIELD_BY_TYPE` (`seriesSupported`, `seriesQueryValueFor`, `ownedKeysFor`, `normalizeSeriesResponse`, `prefillFromSeriesEntry`) now branches on the creator map too. `lib/seriesCrowdsource.js` gained a matching `CREATOR_TYPE_CONFIG` and a new `getCrowdsourcedCreatorSeries` — same crowdsourced-from-logged-items backend Funko Pops already use (no free canonical discography/filmography/bibliography API is wired up for any of these five, same as Funko Pops), just deduped/sorted by normalized title instead of by number since there's nothing to dedupe on otherwise. Every UI component — `GameModal.jsx`, `ItemDetailModal.jsx`, `SeriesModal.jsx`, `ProfileTabs.jsx`, `SeriesGrid.jsx` — needed zero changes: the whole feature was already gated purely through `seriesSupported()`/`isMasterSetType()`, so this was a config change in two library files, not a UI rebuild. All five stay on "See full series" copy (not "See master set"), same as Funko Pops, since it's crowdsourced rather than a real canonical backend. Consoles deliberately excluded: there's no per-item "creator" field that forms a meaningful completable series the way artist/director/author do — grouping by Manufacturer would just be "every console this brand ever made," a category, not a series to complete. Added test coverage for all five new types across `seriesSupported`/`isMasterSetType`/`seriesQueryValueFor`/`ownedKeysFor`/`normalizeSeriesResponse`/`prefillFromSeriesEntry` in `lib/seriesLookup.test.js`. Verified against a fresh clone of the real repo (not just the working mirror): a real `npx vitest run` — full suite, 5 files, 73 tests, all passing — and a real `next build` with placeholder env vars, all 70 routes compiling clean. No migration needed — deploy only.
- **Full physical-release catalogue per console — closes `ROADMAP.md`'s "Full physical-release catalogue per console."** A new `/dashboard/catalogue` page (linked from "More actions," and from a "See full release list →" link that appears next to a "Browse by system" tile once it's active): pick a platform and see every main-release game IGDB has for it, greyed out except what's already logged for that system — not just a checklist of what's already in the collection. `lib/igdbSearch.js`'s `fetchWithTimeout`/`getAccessToken`/`coverUrl` helpers (previously module-private) are now exported so the new `lib/igdbPlatformCatalogue.js` can share the same Twitch token cache and timeout wrapper instead of standing up a second copy of both. Shelf Life doesn't store an IGDB platform id anywhere (`platform` is a free-typed field on a game — see `supabase-schema.sql`), so every request re-resolves the typed platform name against IGDB's own `/platforms` endpoint rather than keeping a hardcoded id table that would drift out of sync with IGDB's own list over time; prefers an exact case-insensitive match on name/abbreviation/alternative name, same defensive pattern `getFranchiseGames` already uses for games, falling back to IGDB's own top search result otherwise. Restricted to IGDB's `category = 0` ("main_game") — without it, a platform's catalog is dominated by DLC/expansion/bundle/port entries that were never their own physical release, which would make a "here's every game for this system" checklist far noisier than useful; the trade-off is a handful of genuinely standalone remakes/remasters IGDB categorizes outside `main_game` won't show up. Real server-side pagination via `/api/igdb-platform-catalogue` (offset/limit, 100 at a time, "Show more" to fetch the next page) rather than SeriesGrid's client-side-slice-of-one-big-fetch approach — a real platform's catalog can run into the thousands even restricted to main games, and IGDB itself caps a single request at 500 rows, so fetching "everything" up front the way SeriesGrid does for a card/comic set genuinely isn't viable here. A title-search box re-queries the same endpoint with IGDB's own relevance ranking instead of the default alphabetical sort, same trade-off the trading-card/comic Search buttons already make.

  "Owned" matching (`lib/platformCatalogueMatch.js`) runs entirely through real IGDB platform ids rather than ever comparing free-typed platform names directly — the first pass at this compared a logged game's `platform` value against the catalogue's resolved platform name by loose substring containment, which had a real gap: a pure abbreviation like "PS2" shares no textual overlap with "PlayStation 2", so a genuinely-owned game could show as "not logged" (flagged directly, and fixed same-round rather than left as a known gap). `app/dashboard/catalogue/page.js` now resolves every distinct platform string across the signed-in user's whole collection to its real IGDB platform id up front, once per page load (`lib/igdbPlatformCatalogue.js`'s new `resolvePlatformId`/`resolvePlatformIds`, sharing the same Twitch token cache and reusing `resolvePlatform`'s exact-name/abbreviation/alternative-name matching the catalogue's own chosen-platform lookup already does) — `ownedTitleKeysForPlatform` then just checks id equality, as accurate as IGDB's own platform database rather than a hand-maintained guess. Deliberately defensive at every layer: `resolvePlatformId` swallows any failure (missing IGDB config, a transient network error) into a plain `null` rather than throwing, since this now runs unconditionally on every catalogue page load, not just when someone actively browses; resolutions are cached in-memory per warm serverless instance, the same pattern the Twitch token cache already uses, so a repeat visit isn't re-resolving the same handful of platform strings every time. The one remaining gap: a platform IGDB has no record of at all (a genuinely obscure/regional system) resolves to `null` on both sides, which falls back to the original loose name comparison — a real degradation, but a much smaller and more defensible one than "abbreviations never match" was. Grid styling reuses `SeriesGrid`'s `.franchise-*` CSS wholesale — same "greyscale unless owned" cover grid already established for series/master-set views — plus one new small `.catalogue-grid` modifier lifting the small in-modal 260px scroll-box height cap, since here the grid is the whole page's content. 7 new tests in `lib/platformCatalogueMatch.test.js` (80 total across the full suite now, up from 73). Verified against a fresh clone of the real repo: a real `npx vitest run` (80/80 passing) and a real `next build` with placeholder env vars — the new `/dashboard/catalogue` page and `/api/igdb-platform-catalogue` route both compile clean alongside all 70 existing routes. No migration needed — deploy only, reuses the existing `IGDB_CLIENT_ID`/`IGDB_CLIENT_SECRET` env vars game search/auto-fill already need.
- **Population data for graded items — checked, not currently buildable.** `ROADMAP.md` asked about pulling the population count for a specific grade (comics/trading cards already have a Grade field) from PSA's or CGC's public lookup tools, right alongside the grade itself. Checked both directly: PSA's Public API is free but explicitly cert-verification-only — PSA's own docs say there's no official endpoint for population/pop-report data, and third-party write-ups on the API confirm developers resort to manual lookups on psacard.com for that. CGC has no public API at all — its population report is a web-only browsing tool, and CGC's own site frames it as "for hobbyists' informational use only," not something meant for programmatic access. Neither offers a free, live, queryable path to real pop data the way TCGdex/Scryfall/Comic Vine do for set data — building this would mean scraping either site, which this project has consistently avoided (same bar the Funko Pop dataset investigation above was held to). Removed from `ROADMAP.md`'s buildable list rather than left as a recurring "worth a look" — worth revisiting only if either grading company ships a real API.
- **Leaderboard's "Most valuable" podium now shows a converted figure, in the viewer's own currency — closes `ROADMAP.md`'s "Live currency conversion."** An earlier round made the ranking itself fair across currencies (`currency_rates_to_usd`, refreshed weekly by `refresh-currency-rates` — see `CHANGELOG.md`), but nothing on the page ever showed the number that ranking actually used, so a viewer had no way to tell *why* a €5,000 collection outranked a $6,000 one — the page's own copy even said so directly ("totals aren't converted, so this only ranks fairly within the same currency," which was already stale the moment the ranking fix shipped). The first pass at surfacing this showed a fixed USD conversion; corrected same-round to convert into the *viewer's own* preferred currency instead — a EUR-preferring visitor sees "£4,200 (≈ €4,850)" against a GBP collector rather than having to do their own USD math on top. `app/leaderboard/page.js` now also fetches the signed-in viewer's `profiles.currency` (the same field Settings > Currency writes; a signed-out visitor, or one who hasn't set one, falls back to `'USD'`) and the full `currency_rates_to_usd` table, passed down as `viewerCurrency`/`rates` props. `LeaderboardClient.jsx`'s `statFor()` converts through `total_value_usd / rates[viewerCurrency]` — deliberately *not* a fresh currency-to-currency rate — so the secondary figure can never disagree with the rate data that decided the rank in the first place; shown as a faded parenthetical next to the primary own-currency figure whenever it differs from the collector's own currency and a rate for it is on file. New `.podium-stat-secondary` CSS rule (just `opacity: 0.72`) for the secondary figure. Updated the tab's own sub-copy to describe what's actually true now. No new migration — `currency_rates_to_usd` and `profiles.currency` both already existed.
- **Opt-in weekly activity digest email — closes `ROADMAP.md`'s "Notification digest emails."** A new "Email me a weekly activity digest" checkbox in Settings > Data (`profiles.email_activity_digest_enabled`, see `activity-digest-migration.sql`, defaults to `false`), right below the existing monthly-backup checkbox and following the exact same instant-save pattern. A new cron, `/api/cron/email-activity-digest` (`vercel.json`, Sundays at noon UTC — distinct from `weekly-stats-digest`'s Sunday-morning slot; registered in `lib/cronHeartbeat.js`, 7 cron jobs now, not 6), emails each opted-in account two things in one message: their own week (items added/completed/rated, trophies earned — straight off `activity_events`) and a summary of what the public collectors they follow have been up to, capped at 10 lines. New `lib/activityDigest.js` builds both halves off the exact same `activity_events` table `/feed` itself reads, so the email can never quietly disagree with what the page would show for the same week; runs against the service-role client (a cron has no signed-in user for RLS to scope against), so followed-collectors' activity is deliberately fetched as two separate queries — the public-profile check as its own plain `.eq('is_public', true)`, rather than leaning on an embedded-table filter — so that privacy rule reads as an obvious, auditable line rather than something buried in a join. Skips sending entirely (not counted as a failure) on a genuinely quiet week — nothing of your own and nothing from anyone you follow — rather than deliver an empty "nothing happened" email; a single account's send failure is logged and counted, not treated as a whole-job failure, same per-item-vs-job-level split `price-drop-check`/`email-data-backup` already use. Needs `activity-digest-migration.sql` on existing projects; reuses the `RESEND_API_KEY`/`RESEND_FROM_EMAIL`/`CRON_SECRET`/`SUPABASE_SERVICE_ROLE_KEY` env vars every other cron/email feature already needs — nothing new to configure. Verified against a fresh clone: a real `npx vitest run` (80/80 still passing — no new tests, since this and the leaderboard change above both touch either live Supabase calls or plain JSX formatting, outside this project's established pure-logic-only test scope) and a real `next build`, the new cron route compiling clean alongside every existing route.
- **"Wishlist matches" dashboard panel — closes `ROADMAP.md`'s "Wantlist matching / trading."** Both signals that roadmap line asked for ("someone you follow has something on your wishlist, or a duplicate they might trade") fold into one new panel rather than two separate features: for each item on your own wishlist, which public collectors you follow already own it, plus a nudge when they own more than one copy — a real hint they might have a spare worth asking about. New `find_wantlist_matches(p_user_id, p_limit)` SQL function (`wantlist-matches-migration.sql`, mirrored into `supabase-schema.sql`) — same shape and same reasoning as `recommend_games`/`recommend_collectors` right above it: deliberately *not* security definer, so it only ever sees what the calling user's own RLS already permits (their own wishlist rows, plus a followed profile's owned items when that profile is public — the explicit `p.is_public = true` in the join is belt-and-suspenders on top of that RLS backstop, not a substitute for it). Matches on `lower(title)` within the same `item_type`, the same simple convention every other title-matching SQL function here already uses. New `components/WantlistMatchCard.jsx` reuses `.rec-card`/`.rec-cover`/`.rec-body`/`.rec-title`/`.rec-meta` wholesale — same tile shape "Recommended for you" already uses, just linking to the collector's profile instead of opening Add Item, since there's no in-app trading/messaging system to hand off to (that's its own, much bigger `ROADMAP.md` item, deliberately not attempted here). Wired into `DashboardClient.jsx` right below "Collectors you might like" inside the Tools drawer's Insights section, same collapsed-by-default/remembered-per-device pattern, and folded into the same one-way "first appearance" dot on the Tools button `recommend`/`collectors` already share, rather than a third dot to track. Needs `wantlist-matches-migration.sql` on existing projects; no new env vars.
- **8 new Shelf Life trophies — closes `ROADMAP.md`'s "More Shelf Life milestone variety."** That line asked for "platform-completionist badges, genre/decade-spanning badges, room for oddball ones beyond 'own N items'" — checked decade-spanning specifically and it isn't buildable: `games` has no original-release-date field to compute a decade from (only `purchase_date`, which is when *you* bought it, not when the game/comic/album itself came out), so that part stays open (see `ROADMAP.md`). Everything else shipped: **Platform Loyalist** (silver, own 20 items for one platform — depth, distinct from `platforms-5`/`platforms-10`'s breadth-across-many-platforms), **Genre Connoisseur** (gold, 10 genres) and **Jack of All Trades** (gold, own at least one of literally every collectible type — there are exactly 10) extend the existing `genres-5`/`types-5` breadth trophies to a higher bar, same scaling `platforms-5 → platforms-10` already used; and four genuinely new "oddball" signals off fields that already existed but had no trophy tied to them — **Well Tagged** (bronze, tag 10 items), **Archivist** (silver, write notes on 10 items), **Detail Oriented** (silver, attach condition photos to 5 items), **Yard Sale** (bronze, mark 5 items for sale), **Downsizer** (silver, mark 10 items sold). `award_achievements_for`'s six new stat lookups follow the exact pattern every existing one already does (a plain `select count(*) into v_x from games where ...`), including one genuinely new wrinkle worth knowing: `array_length(arr, 1)` returns `null`, not `0`, for an empty array in Postgres, so the tag/note/condition-photo counts deliberately rely on `null > 0` evaluating false rather than needing an extra `coalesce`/`nullif` guard. `TrophyCase.jsx` needed zero changes — trophy definitions and rarity both already render straight from `achievement_defs`/`trophy_rarity()`, no hardcoded list anywhere to update. Worth knowing: Platinum Shelf requires earning every other trophy, so this genuinely raises that bar for anyone still chasing it, same as every past trophy addition has. Needs `trophy-variety-migration.sql` on existing projects; no new env vars. No new tests — trophy awarding is server-side SQL, outside this project's pure-JS-logic-only test scope, same as every other achievement change.
- **Printable QR labels per item — closes `ROADMAP.md`'s "Printable QR/barcode labels per item."** A new `/dashboard/labels` page (linked from "More actions"): pick which owned items to print, and it lays out a small QR sticker per item on Avery 5160-compatible address label sheets — 30 per sheet, 3 columns × 10 rows, each label 2.625in × 1in. The geometry is real, published Avery measurements (checked against Letter-page arithmetic: 3×2.625in + 2×0.125in column gaps + 2×0.1875in side margins = 8.5in wide; 10×1in + 2×0.5in top/bottom margins = 11in tall, both exact), but hasn't been visually verified against an actual printer from this environment — worth checking the first sheet against a blank page before committing real label stock. Each QR code encodes `/dashboard?item=<id>` — the same deep link `DashboardClient.jsx` already opens straight to an item's detail view, originally built for price-drop notification emails — so scanning a printed label opens that item's own page, signed in, on the owner's phone: a fast way to look a boxed item back up, matching the ROADMAP line's own framing. New `qrcode` npm dependency (v1.5.4) generates the QR SVGs; picked specifically because its package.json `"browser"` field makes it genuinely client-bundler-compatible, not just a server-side tool, so QR generation happens in the browser only for the items actually selected to print — a large collection doesn't mean computing (or, if this ran server-side, shipping down the wire) hundreds of unused codes. `app/globals.css` gained a new `.label-sheet`/`.label` block: multi-sheet print jobs are split into one `.label-sheet` div per 30 items rather than one long CSS grid, each with `break-after: page`, so a second or third sheet gets its own full margin instead of the browser's default pagination cutting a sheet mid-row. Verified against a fresh clone of the real repo: a real `npx vitest run` (80/80 still passing — no new tests, pure client-side UI with no new pure-logic functions) and a real `next build`, the new `/dashboard/labels` route compiling clean alongside every existing route. Needs `npm install` for the new `qrcode` dependency; no migration, no new env vars.
- **Public read-only API / personal access tokens — closes `ROADMAP.md`'s "Public read-only API / personal access tokens."** A new `/dashboard/api-tokens` page (linked from "More actions") lets a collector generate a personal access token and use it to pull their own collection as JSON from a script, spreadsheet, or personal dashboard, instead of exporting a CSV by hand. Tokens look like `sl_live_<48 hex chars>` (`lib/apiTokens.js`) — only a sha256 hash is ever stored (`api_tokens.token_hash`), so the raw value is shown exactly once, at creation, the same "copy it now, you won't see it again" pattern any real API-key product uses; a stored `token_prefix` lets the owner tell entries apart in the list without ever being able to reconstruct the full token from it. Two endpoints, both public (no session) and authenticated instead via `Authorization: Bearer <token>` (`lib/apiAuth.js`): `GET /api/v1/me` (username, display name, public/private, currency, item count) and `GET /api/v1/collection` (optionally filtered by `?type=`/`?ownership=`), which deliberately reuses `lib/csvExport.js`'s exact `EXPORT_COLUMNS` list rather than a second, separately-maintained field list — same shape "Export CSV" already hands the signed-in owner, nothing new exposed. Since a token request has no Supabase session, RLS can't scope anything automatically — every query runs through the service-role client and is manually filtered by the token's own `user_id`, the same "no session → admin client + explicit scoping" pattern this project's cron jobs already use, and creating a token itself is the one operation (`app/api/tokens` `POST`) that goes through a server route rather than a direct client-side Supabase call, since generating the actual secret has to happen server-side — listing and revoking (`app/dashboard/api-tokens/ApiTokensClient.jsx`) go straight through the browser client instead, same as `PasskeyManager.jsx` already does for passkey credentials, since `api_tokens`' own RLS policies already scope both to the signed-in owner. Rate limited to 60 requests/minute per token (`lib/apiRateLimit.js`, `api_rate_limit_events` — same dedicated-events-table shape as `webauthn_rate_limit_events`, keyed by token id instead of client IP since a request here always has an authenticated token by the time it reaches the limiter). 6 new tests in `lib/apiTokens.test.js` (86 total now, up from 80) covering token format, hash determinism, and the `looksLikeApiToken` guard. Verified against a fresh clone: a real `npx vitest run` (86/86 passing) and a real `next build`, the new `/dashboard/api-tokens`, `/api/tokens`, `/api/v1/me`, and `/api/v1/collection` routes all compiling clean alongside every existing route. Needs `api-tokens-migration.sql` on existing projects; no new env vars — reuses `SUPABASE_SERVICE_ROLE_KEY`, already required for every other admin-client route.
- **Per-item reviews, separate from your personal rating — closes `ROADMAP.md`'s "Per-item reviews (separate from personal rating)."** The collectible detail page (`/collectible?type=&title=`) now has a Reviews section: a star rating plus real written text, one per reviewer, aggregated across everyone who's reviewed that exact item (not per-copy the way the existing personal `rating` field on `games` is) — separate from and additional to the page's existing "Avg rating" stat, which still reflects each collector's own private per-copy rating. New `item_reviews` table (`item-reviews-migration.sql`, mirrored into `supabase-schema.sql`): `rating` is the same half-star `numeric(2,1)` type `games.rating` already uses (reuses `StarRating.jsx`'s existing interactive mode as-is, zero changes needed there), `body` up to 1000 characters, one review per reviewer per item (`unique index` on `user_id, item_type, lower(title)` — posting a second one edits the first instead). Posting is gated on actually owning the item: the insert RLS policy itself requires a matching `games` row with `ownership = 'owned'` for that `user_id`/`item_type`/`title`, so there's no server-side trust check to get wrong and no moderation queue needed for "did they actually own this" — same "avoid needing a full moderation system" reasoning this project has applied to every other trust-sensitive feature. Visibility mirrors `comments`/`custom_lists` exactly: your own reviews are always visible to you, everyone else's only if their profile is public — a private collector's opinion doesn't leak out through the one place this project hasn't already applied that rule. Rate limited the same 5-per-5-minutes shape as `comments`/`reports` (`enforce_review_rate_limit()` trigger). New `app/collectible/ItemReviews.jsx` (client component, direct browser-Supabase-client insert/update/delete straight through RLS — same pattern `CommentSection.jsx`/`PasskeyManager.jsx` already use, no dedicated API route needed since there's no secret to generate server-side) handles writing, editing, and deleting your own review; `app/collectible/page.js` fetches everyone's visible reviews server-side (same `item_type` + case-insensitive `title` match the existing `games` query on that page already uses) and works out whether the viewer currently qualifies to write a new one from the same `games` rows it already loaded, no extra query needed. New `lib/itemReviews.js`/`averageRating()` — the one piece of real logic here small enough to unit test and worth pulling out of the component for it, same as every other lib/ helper on this project; handles Postgres returning a `numeric` column as a string rather than a JS number, the same wrinkle `buildCollectibleDetail` already works around for `games.rating`. 4 new tests in `lib/itemReviews.test.js` (90 total now, up from 86). Verified against a fresh clone: a real `npx vitest run` (90/90 passing) and a real `next build`, `/collectible` compiling clean with the new component alongside every existing route. Needs `item-reviews-migration.sql` on existing projects; no new env vars.
- **Manual text-size control — closes `ROADMAP.md`'s "Rest of the Accessibility checklist,"** the last item on that list. A "Text" dropdown in the navbar (next to the existing Dark/Light `ThemeToggle`, same placement logic — works for signed-out visitors too, not gated behind the dashboard's Settings modal) offers Normal/Large/Larger. The real obstacle this was blocked on: `app/globals.css` has hundreds of scattered hardcoded `font-size: NNpx` declarations with no root rem/em scale feeding them, so the usual fix (a root-font-size-based control) wouldn't reach most of the site's text. CSS `zoom` (`html.text-size-large { zoom: 1.15 }` / `html.text-size-larger { zoom: 1.3 }`) sidesteps that entirely — it scales an element's whole rendered box uniformly rather than any specific CSS unit, so it works regardless of what unit a given declaration happens to use. New `lib/textSize.js` (localStorage get/set/apply helpers) and `components/TextSizeControl.jsx`, built as close to a copy of the existing `ThemeToggle.jsx` as the two features' shapes allow: applied on mount via `useEffect` rather than a blocking pre-hydration script (same brief "flash from the server-rendered default to the stored value" trade-off `ThemeToggle` already makes for theme), stored in `localStorage` rather than on `profiles` since this is a device-local display preference, not an account one. This line was explicitly flagged as "shouldn't ship without visually verifying it first, on a real dev server" — actually done this time: spun up a real `next dev` against production Supabase (read-only, no writes) and screenshotted 5 representative pages (home, `/accessibility`, `/login`, `/leaderboard`, `/players`) at all 3 text sizes on both a 1400px desktop and a 390px mobile viewport, plus the mobile hamburger drawer specifically (where the new control actually lives on a phone) — no overflow, overlap, or broken layout at any size, though card stat-panel labels do wrap a bit more eagerly at "Larger," an expected and acceptable trade-off of a "make everything bigger" feature rather than a bug. Also fixed `/accessibility`'s page copy while in there — its "Known gaps" list had gone stale, still listing the WCAG contrast audit, color-alone status indicators, and the tab-order pass as open when `CHANGELOG.md` already shows all three closed; moved them into "What's in place today" alongside the new text-size control itself. No new tests (a DOM-dependent localStorage/classList wrapper, same "outside this project's pure-JS-logic-only test scope" reasoning `ThemeToggle.jsx`/`lib/toast.js` already fall under — neither has tests either). No migration, no new env vars — deploy only.
- **Next.js 14 → 16 upgrade (React 18 → 19 alongside it)** — closes the "Next.js major-version upgrade" item deferred from the Aug 2026 dependency-audit round, fixing the 2 high-severity `npm audit` issues (`next`, `postcss`) that only resolved on `next@16`. `package.json` bumped to `next@16.3.4`/`react`+`react-dom@19.2.8`, plus a new `engines.node: >=20.9.0` (Next 16's actual minimum). `middleware.js`/`middleware()` renamed to `proxy.js`/`proxy()` per the v16 rename — nothing here used the edge runtime, so the rename was the whole change. The real substance of the upgrade: Next.js 16 removes the v15 temporary compatibility shim that let `cookies()`/`headers()` and a route's `params`/`searchParams` be read synchronously, so all of those are now genuinely async everywhere they're used. This is the one upgrade-wide risk worth naming specifically — an un-awaited `params.x` doesn't throw, it silently evaluates to `undefined`, which for a page like `/u/[username]` means "can't find that person" rather than a visible error. Every call site across the app (`lib/supabaseServer.js`'s `createClient()`, all 4 WebAuthn API routes' `cookies()` calls, the mosaic-image OG-image route's `params`, and 11 `page.js` files' `params`/`searchParams`, both in the page component and in `generateMetadata`) got a manual pass rather than a blanket find-replace — worth flagging that a first attempt at a scripted find-replace across the 42 remaining plain `createClient()` call sites briefly added `await` to 4 client-side pages (`login`, `signup`, `forgot-password`, `reset-password`) that import the *browser* Supabase client from `lib/supabaseClient.js`, which stays synchronous — caught immediately because `next build` fails loudly on `await` in a non-async function, and reverted before it went anywhere. Verified in layers: `npx vitest run` (90/90 passing, unchanged) and a real `npx next build` with Turbopack (clean compile, all 73 routes), then — since a build alone can't prove a route that renders on-demand actually resolves its real path param rather than silently getting `undefined` — a live check against the actual deployed preview: `/u/<a real username>` came back with that person's real profile, item, follower/following counts, and a real comment, not a false "not found." No migration, no new env vars — `npm install` after pulling to get the new dependency versions, then deploy.
- **Upcoming Releases — a release calendar with spend forecasting, closes `ROADMAP.md`'s "Pull list / upcoming-release calendar with spend forecasting."** A new `/dashboard/upcoming-releases` page (linked from "More actions" as "Upcoming releases" — named that instead of "Pull List," comic-shop jargon that doesn't mean anything to someone who's never used one, on a same-day rename before this ever went out) shows what's still coming for the game franchises and comic series you already own something from, grouped by month, with a running "this week"/"this month" spend total from prices you type in yourself. Checked feasibility properly before building anything (the codebase's own earlier note that IGDB/Comic Vine only exposed a release *year* turned out to be stale/wrong): IGDB's `first_release_date` and Comic Vine's `store_date` are both real, full dates already reachable from `lib/igdbSearch.js`'s `getFranchiseGames()` and a new `lib/comicVineSeriesLookup.js` export — neither API exposes price/MSRP data for something that hasn't released yet, though, so the spend numbers are unavoidably a manual guess, not something this app can look up. `lib/igdbSearch.js`'s `getFranchiseGames()` now carries a real `releaseDate` (ms timestamp) alongside its existing display-only `year` field — additive, nothing existing changed. New `lib/comicVineSeriesLookup.js` export `getUpcomingComicIssues(seriesName)` reuses the same `findVolumeByName` the master-set lookup already has, filters Comic Vine's `issues` endpoint to a one-year-ahead `store_date` window, and falls back to `cover_date` only for an issue that has one and not the other. A new `upcoming_release_cache` table (`upcoming-releases-migration.sql` for existing projects, mirroring `master_set_cache`'s shape) is kept warm by a new weekly cron, `/api/cron/refresh-upcoming-releases` (`vercel.json`, Tuesdays 8am UTC; registered in `lib/cronHeartbeat.js`, 8 cron jobs now, not 7) — modeled closely on `refresh-master-sets`: only refreshes franchises/series someone here has actually logged an item from (across all users, not per-account), capped at 25 per run (see the follow-up fix below — shipped as 8, raised same-week), skipping anything refreshed within the last 7 days, so a live page load never hits IGDB/Comic Vine directly. New pure-logic module `lib/upcomingReleases.js` (13 tests, `lib/upcomingReleases.test.js`) handles the series-key building, cache-row flattening/future-date filtering, month-grouping, and the spend-total math, shared by both the cron and the page. Along the way, extracted `normalizeSeriesText` out of `lib/seriesCrowdsource.js` into a new zero-dependency `lib/textNormalize.js` — that file's own top-level `@/lib/supabaseServer` import made it unusable from a plain Vitest test, and `lib/upcomingReleases.js` needed the exact same series-name normalization `lib/seriesCrowdsource.js` already used, not a second copy of it; `lib/seriesCrowdsource.js` now imports it back in and re-exports it, so nothing about its own 5 existing call sites changed. The "expected price" guesses themselves live in `localStorage`, not a new per-user DB table — same per-device-preference call `lib/textSize.js`/`ThemeToggle.jsx` already make: it's a personal budgeting estimate, not account data anyone else needs to see. Verified against a fresh clone: a real `npx vitest run` (103/103 passing, up from 90) and a real `next build`, the new `/dashboard/upcoming-releases` page and `/api/cron/refresh-upcoming-releases` route both compiling clean alongside every existing route — the actual IGDB/Comic Vine calls themselves couldn't be exercised live from this sandbox (same network-allowlist limitation every other from-sandbox external-API integration on this project has carried until confirmed against live data), so a real click-through once deployed is worth doing before trusting the calendar's contents fully. Needs `upcoming-releases-migration.sql` on existing projects (`pull-list-migration.sql` if you already ran it under that name before this rename — same table, no need to run it twice); no new env vars — reuses the `IGDB_CLIENT_ID`/`IGDB_CLIENT_SECRET`/`COMICVINE_API_KEY`/`CRON_SECRET`/`SUPABASE_SERVICE_ROLE_KEY` env vars game search, comic search, and every other cron already need.
- **Fixed: Upcoming Releases showed nothing at all, right after it shipped — flagged directly, checked against real production data rather than guessing.** Queried the live database (`upcoming_release_cache`, `cron_runs`) directly: the cache table had zero rows and `refresh-upcoming-releases` had never once recorded a run, even though the account in question has 238 distinct tracked game titles and 3 comic series — real data the page should have had something to show for. Root cause: the feature was pushed to `main` well after this week's Tuesday 8am UTC cron window, so the very first scheduled run hadn't happened yet — not a bug in the matching/caching logic itself, just an empty cache with nothing yet to read. Two real fixes, not just "wait for Tuesday": `MAX_SERIES_PER_RUN` raised from 8 to 25 in `app/api/cron/refresh-upcoming-releases/route.js` — the original number copied `refresh-master-sets`' cap without accounting for how much cheaper a series lookup is here (a franchise/collection resolution or a Comic Vine volume+issues page, versus fetching per-card detail for every card in a 100-250+ card set), so it was needlessly conservative and would have taken months to backfill a real collection at 8/week. And `README.md` now documents `vercel crons run /api/cron/refresh-upcoming-releases` (a real Vercel CLI feature, confirmed against Vercel's own docs) for manually backfilling a cache on demand instead of waiting on the weekly schedule — worth running it a few times a couple minutes apart right after this deploys, since each run only picks up series it hasn't cached yet. No migration, no new env vars — deploy only.
- **Persistent dashboard sidebar, replacing the "More actions" dropdown — flagged directly ("the ui needs to be rethought, we have way to many features that are hidden away") after Upcoming Releases became the 7th page link buried inside it.** That menu had grown to 7 page links (Collection insights, Your Wrapped, Collection appraisal, Upcoming releases, Full release catalogue, Print labels, API access) plus Refresh all prices and Settings, with nothing on any of those pages linking back to it — navigate to one and you lost the menu that got you there. Three options were laid out directly (tabs, a persistent sidebar, or a command palette); picked persistent sidebar. New `app/dashboard/layout.js` — a plain server component wrapping every `/dashboard/*` route's children in `<DashboardSidebar />` — means every route under it gets the sidebar automatically, including ones added later, without each `page.js` needing to remember to render it. `components/DashboardSidebar.jsx` (`'use client'`, needs `usePathname()`/`useSearchParams()` for active-link highlighting, including off `?settings=1`) renders 4 grouped sections — My Collection, Insights & reports, Utilities, Account — and reuses two CSS patterns already established elsewhere rather than inventing new ones: `.feed-sidebar`'s `position: sticky` for the desktop column, and `Navbar.jsx`'s off-screen-`transform`-not-`display:none` drawer convention (mirrored to slide from the left instead of the right) for the mobile toggle+overlay+drawer, including the same Escape-to-close/body-scroll-lock `useEffect`. `DashboardClient.jsx`'s old `ActionMenu`-based "More actions" dropdown is gone entirely; Refresh all prices and Settings are now plain buttons directly in the toolbar (the "+ Add Item" chooser and the unrelated "More collection tools" menu are untouched — different menus). The 7 subpages that used to have a "Back to dashboard" link (now redundant with the persistent sidebar) had it removed, along with the now-unused `next/link` import in 6 of them (`WrappedClient.jsx` keeps it — still used for year-navigation). Hit one real Next.js build error along the way: `DashboardSidebar` rendering on every `/dashboard/*` route via the new layout meant `/dashboard/api-tokens` — whose own `page.js` had never used `useSearchParams()` before — failed static prerendering with "useSearchParams() should be wrapped in a suspense boundary." Fixed by adding `export const dynamic = 'force-dynamic'` to `app/dashboard/layout.js` rather than wrapping in a `Suspense` boundary — the correct fix, not a workaround: every route under `/dashboard` already does a live per-request auth check via `redirect()` off real cookies, so none of them were ever actually static-optimizable; this just makes that explicit. Verified against a fresh clone: a real `npx vitest run` (103/103 passing, unchanged — no logic touched) and a real `next build`, all `/dashboard/*` routes correctly showing as dynamic (ƒ), 66 static pages generated, no errors. No migration, no new env vars — layout/component/CSS only.
- **Removed the "Xbox/PlayStation trophies & achievements" stats panel from public profiles — flagged directly ("the trophies bit is actually not needed there").** The panel (`app/u/[username]/page.js`, the `trophy-stats-panel` block right above `ProfileTabs`) rolled up Platinum count/average completion/games tracked across a collector's own manually-entered `trophy_platinum`/`trophy_completion` fields into a card on their public profile. Removed the panel and the now-unused `trackedTrophyGames`/`platinumCount`/`avgCompletion` computation feeding it, plus the now-dead `.trophy-stats-panel`/`.trophy-stats-heading`/`.trophy-stats-row`/`.trophy-stat` CSS (checked first — none of it was shared with anything else). The underlying feature itself is untouched: the Platinum'd checkbox and completion % field in the Add/Edit form, the badge on a game's own card, the dashboard's Trophy % filter/sort, the CSV export columns, and the "Trophy completion" stat on `/dashboard/insights` (a separate, differently-styled panel — `insights-panel`, not `trophy-stats-panel`) all still work exactly as before; only the public-profile rollup is gone. Updated `README.md`'s description of this feature to match (dropped the "rolled up into a stats panel on your public profile" line, pointed at the Insights stat instead). No migration, no new env vars — component/CSS removal only.
- **A persistent bottom tab bar on phones, Spotify-style — flagged directly ("I think this will help organise the app").** New `components/MobileBottomNav.jsx`, rendered site-wide from `app/layout.js`, CSS-hidden above 640px (the same breakpoint `Navbar.jsx`'s own hamburger menu already switches on — checked directly with a real dev server at 639px vs. 641px before shipping, not just eyeballed) so desktop is untouched, per direct confirmation that this should be phone-only rather than a universal replacement for the top navbar. 4 signed-in destinations — My Collection, Search, Feed, My Profile — chosen as the ones actually reached daily; Leaderboard/Lists/Articles stay exactly where they already were, in the hamburger menu, rather than trying to cram 7 destinations into a bar that only comfortably fits 4-5. A real, if smaller, product question came up while scoping this: whether My Collection and My Profile are basically the same page (flagged directly). Checked properly rather than assuming either way — they're not: Dashboard is the editing/management console (CRUD, filters, bulk tools, Insights/Wrapped/Appraisal/Catalogue/Labels/API access, Settings) with zero social features, Profile is the public read-only page (followers, comments, Trophy Case, showcase, sharing) that happens to also display the same collection grid, which is the real source of the "feels the same" impression. Decision: keep both as separate bottom-bar destinations for now (a full merge or a Profile restyle to lead with social content over the grid are both real follow-ups, not attempted this round). New `components/BottomNavIcon.jsx` — 4 more hand-drawn monoline SVG glyphs (home/shelf, magnifying glass, activity pulse, person), same no-icon-library convention `CategoryIcon.jsx` already established, not a 5th one pulled in for 4 more glyphs. New `lib/useCurrentProfile.js` extracts the exact auth+profile-lookup logic `Navbar.jsx` already had (same query, same `onAuthStateChange` subscription, same deferred-via-`setTimeout` guard) — needed identically by the new bar, so this is a second real call site rather than a second hand-copied implementation; `Navbar.jsx` now uses the shared hook too, no behavior change there. `Navbar.jsx`'s own copies of the 4 duplicated links get a new `nav-link-primary` class, CSS-hidden at the same 640px breakpoint the bottom bar appears at — still the only way to reach them above it, where there's no bottom bar. Checked every other `position: fixed` element anchored to the bottom of the screen (`.cookie-banner`, `.toast-stack`, `.undo-toast`, `.trophy-toast-stack`) and bumped their `bottom` offset under 640px so the new bar doesn't sit on top of them; gave `body` matching `padding-bottom` so the bar never covers the last bit of real page content either. Signed-out phones get a 3-item bar (Search, Log in, Sign up) mirroring the hamburger's own signed-out branch, rather than showing destinations that need an account. Verified with a real dev server and real screenshots at a 390×844 phone viewport — but a correction on what those actually proved: this sandbox's network egress doesn't reach this project's Supabase host at all (confirmed separately while investigating the two mobile bugs below — a direct request returns "Host not in allowlist"), so every Supabase query in that dev-server session was silently failing, and the pages were rendering their signed-out/empty fallback state rather than real signed-in data. What the screenshots did genuinely confirm: the signed-out 3-item bar (Search/Log in/Sign up) renders correctly, active-link highlighting crossing from `/players` to `/` works (this is pure `usePathname()` routing logic, not data-dependent), and the bar doesn't appear at all above 640px — real CSS/layout mechanics, unaffected by the data being empty. What was NOT actually verified against real data: the signed-in 4-item bar (Collection/Search/Feed/Profile) and its active-link highlighting, since a genuine signed-in session couldn't be reached from this sandbox. Also a real `npx vitest run` (103/103, unchanged) and `next build` (clean compile, every route unaffected). No migration, no new env vars — component/CSS only.
- **Fixed: public profile's cover-art mosaic looked like "a load of squeezed together photos" on phones — flagged directly with real screenshots.** Root cause found by working through the actual layout math rather than guessing: `.profile-header-mosaic`'s CSS was a single row of 8 columns (`grid-template-columns: repeat(8, 1fr)`, no row constraint), and each cell still stretches to the header's full height via `object-fit: cover`. On desktop, `.profile-header`'s content sits in one row, so the header is short and wide and each cell comes out close to a normal landscape thumbnail. On a phone, the exact same avatar/name/stats/buttons wrap onto several lines instead (`flex-wrap: wrap`, unchanged, working as intended), making the header tall and narrow — so the same "divide the width by 8, stretch each cell to the full height" math turned every cover into an extreme, unrecognizable vertical sliver instead. This wasn't a rendering bug or a deployment issue (the earlier investigation into these two reports had already confirmed the shipped CSS matched source exactly, both fetched live from production) — the design just hadn't been checked at a tall/narrow aspect ratio before. Fixed by changing `.profile-header-mosaic` to a 4-column x 2-row grid (`grid-template-columns: repeat(4, 1fr); grid-template-rows: repeat(2, 1fr)`) instead of 8 columns x 1 row — the same 8 covers now form a proper grid whose cells stay close to square/landscape regardless of how tall or short the header ends up, rather than one row of slivers that only looked right at desktop's specific proportions. Verified locally by rendering both the old and new CSS against synthetic tall (phone-shaped) and wide (desktop-shaped) headers with distinct colored placeholder cells and screenshotting both — old CSS reproduced exactly the striped/sliced look Taylor's screenshots showed at phone proportions, new CSS gives a proper mosaic at both proportions. Also a real `npx vitest run` (103/103, unchanged) and `next build` (clean compile, every route unaffected). No migration, no new env vars — one CSS rule changed.
- **Diagnostic-only change, no user-facing effect: added temporary logging to `lib/igdbPlatformCatalogue.js` to close out the "full release catalogue shows no results" bug (`ROADMAP.md`, High priority).** Made real forward progress on this while waiting on the two mobile bug screenshots: hit the live production API directly (`mcp__Vercel__web_fetch_vercel_url`) and confirmed `/api/igdb-platform-catalogue?platform=Nintendo%20Switch` resolves the platform correctly (`platformId: 130`) but returns an empty `games` array — even with a `search=Zelda` param added — while `/api/igdb-search?q=Breath%20of%20the%20Wild` (a different, working IGDB route) confirms game id 7346 genuinely has `"Nintendo Switch"` in its own `platforms` list. So this isn't a config, auth, or "platform doesn't exist" problem — the `/v4/games` query itself is filtering out everything, including games that unambiguously match. Checked the official Apicalypse filter spec directly (apicalypse.io) — `where field = (x)` is documented as "array contains at least one of," the semantic this query already uses, so the bracket choice isn't the obvious culprit it looked like it might be. Rather than guess at a fix blind, added a `console.error` in `getPlatformCatalogue()` that logs the exact query string sent to IGDB and the raw response body, but only when the result comes back empty (matches the bug exactly, stays silent on every working request). See `ROADMAP.md` for the next step once this is live: one reproduction, then `get_runtime_logs` shows the real IGDB response directly. This diagnostic block gets removed once the actual fix ships — it changes nothing about what a user sees either way. No migration, no new env vars.
- **Added a 5th item to the phone bottom bar — Alerts, closing the "notification bell isn't reachable from the phone bottom bar" follow-up (`ROADMAP.md`) noticed while building the bar itself.** Links to the existing `/notifications` page rather than trying to reproduce `NotificationBell.jsx`'s dropdown inline — a dropdown anchored to the very bottom of the screen has nowhere sensible to open toward, and the full page already exists as exactly this bar's own "See all notifications" link. Extracted the poll/unread-count/muted-types logic `NotificationBell.jsx` already had into a new `lib/useUnreadNotifications.js` — same real-second-call-site pattern `lib/useCurrentProfile.js` set for the bar's first 4 items, rather than a second hand-copied poll running alongside the first. `NotificationBell.jsx` itself now consumes this hook too (its own behavior is unchanged — same 60s poll, same muted-type filtering, same optimistic zero-the-badge-on-open); its still-private bits (the dropdown's own list fetch, mark-as-read) stayed put. New `bell` glyph in `BottomNavIcon.jsx`, deliberately the exact same path data as `NotificationBell.jsx`'s own SVG — same feature, two entry points, should look identical. The navbar's bell (`.notif-wrap`) is now hidden in the hamburger drawer under the same 640px breakpoint the rest of the bar's destinations already hide at, so it isn't listed twice — it isn't a plain `.nav-link` so it needed its own selector rather than the shared `.nav-link-primary` class; removed `.notif-dropdown`'s now-dead mobile-width override alongside it (checked first — nothing else references either class) since the dropdown can no longer open from behind a `display: none` parent at that width. Verified with a real local render of the actual 5-item markup against the actual shipped CSS at a 390px phone width (screenshot, not just code review — the mosaic bug earlier this round was exactly the kind of thing that only shows up when you look) — evenly spaced, badge sits cleanly on the bell icon, no cramping or overlap. Also a real `npx vitest run` (103/103, unchanged) and `next build` (clean compile, every route unaffected — `/notifications` already existed as a route, this only adds a new way to reach it). No migration, no new env vars.
- **Public profile's own "More actions" menu — closes `ROADMAP.md`'s smaller-scale version of the hidden-features pattern the dashboard sidebar already fixed.** Checked what was actually inside it first: on the owner's own profile, only "Shelf mosaic" had a real visibility condition (`owned > 0`) — Gift list, Manage showcase, and Manage lists were unconditional, just sitting behind a click for no reason. On a visitor's view, Compare collections and Report profile were unconditional too; Shelf mosaic/Gift list/Refresh prices already had their own real relevance checks (`owned > 0`, a non-empty wishlist, `canView`). Promoted everything with either no condition or an already-passing one to a plain visible button next to Share/Edit or Follow, same row, same `.btn-ghost` styling these already had inside the dropdown — nothing needed touching in `ShowcaseButton.jsx`/`CustomListsButton.jsx`/`RefreshPricesButton.jsx` themselves, they were already plain drop-in buttons. The owner's row has no conditional items left at all now, so the menu is gone entirely there. The visitor's row keeps a 1-item "More actions" menu around just for Report profile — a deliberate exception, not an oversight: unlike the others, reporting isn't something a visitor is ever looking for, and sitting it in the primary row next to Follow would read as more prominent than a low-frequency, easy-to-misclick action should. Verified with a real local render of both rows against the actual shipped CSS, at both a 1100px desktop width and a 390px phone width (screenshots, not just code review) — wraps cleanly at both, nothing crowded or overlapping. Also a real `npx vitest run` (103/103, unchanged) and `next build` (clean compile). No migration, no new env vars — one file (`app/u/[username]/page.js`), JSX restructure only.
- **Calmed down the public profile header — flagged directly with real screenshots ("the banner looks messy and theres too many buttons") right after the mosaic fix and the button-promotion round above shipped together for the first time.** Checked what actually changed to cause it: the mosaic fix (above) made every cover render as a clear, recognizable photo instead of an unrecognizable sliver — correct on its own, but combined with the *other* recent change (promoting 6 previously-menued buttons into the row directly), the header went from "faint texture behind 2 buttons" to "a wall of photos behind 3 stacked rows of 6 buttons" — both individually-reasonable changes, landing together, that neither round had seen combined until Taylor did. Two real fixes, not one: (1) `.profile-header-mosaic` — opacity dropped 0.2 → 0.14, grayscale bumped 0.3 → 0.45, and a new gradient scrim (`::after`, fading to the header's own card color from halfway down) so the avatar/name/action-row area sits on a calm surface instead of directly on cover art; `.profile-header` itself now has a real `var(--card)` background + border, matching every other card surface on the site, instead of sitting bare on the page background. (2) Reversed course on the button-promotion — but not all the way back to the original bug: the owner row keeps Share/Edit profile visible and moves Shelf mosaic/Gift list/Manage showcase/Manage lists into one labeled "Manage ▾" menu (down from 6 buttons to 3); the visitor row keeps Follow/Compare collections visible and moves Shelf mosaic/Gift list/Refresh prices/Report profile into one labeled "More ▾" menu. This isn't the same mistake the original "hidden features" bug was — that menu looked broken because its contents were *conditionally* hidden and a screenshot happened to catch one where only one item's condition passed, reading as a near-empty menu; every item in these new menus keeps its own real relevance check, but each menu also has at least 2-3 unconditional items (Gift list/Manage showcase/Manage lists for the owner; Report profile always for the visitor) so neither can ever read as suspiciously sparse, and the trigger itself is a labeled "Manage ▾"/"More ▾" button, not a bare "⋯" icon, so what's inside isn't a mystery. Verified with a real local render of all 3 header variants (owner menu collapsed/open, visitor collapsed) against the actual shipped CSS, at both a 1400px desktop and a 390px phone width — mosaic reads as calm background texture, not a second grid of content; 3 buttons instead of 6-7 in the collapsed state, matching what Taylor's screenshots showed as "too many." Also a real `npx vitest run` (103/103, unchanged) and `next build` (clean compile). No migration, no new env vars — `app/globals.css` and `app/u/[username]/page.js` only.
- **Diagnostic-only change, no user-facing effect: added a temporary debug endpoint to test candidate fixes for the "full release catalogue shows no results" bug (`ROADMAP.md`, High priority) directly against IGDB's live API, instead of guessing at a fix and burning another deploy-and-wait cycle.** The empty-result diagnostic logging added earlier this round did its job: two real production searches (PlayStation 2, Nintendo Switch 2) both logged a genuinely empty, validly-parsed `[]` array back from IGDB — ruling out any error/auth/parse cause and confirming the bug is real, current, and general across platforms, which narrows it down to the actual `where platforms = (${resolved.id}) & category = 0;` filter in `lib/igdbPlatformCatalogue.js`'s `getPlatformCatalogue()`. Rather than pick one guess (e.g. reordering `sort`/`search` to come after `fields`/`where`, since that combination doesn't appear in this codebase's other working IGDB queries) and ship it blind, added `debugCatalogueQueries()` — runs 8 candidate query shapes for one resolved platform in parallel (clause order, with/without the `category` filter, with/without parens around the platform id, `platforms.id` vs. `platforms`, no ordering clause at all, filter order swapped, and a bare platform-only sanity check) and reports each one's real result count and a name sample. Only reachable via `app/api/igdb-platform-catalogue/route.js`'s new `?debug=<long fixed key>` param — not something a normal client request could hit by accident, and it changes nothing about the normal response shape or behavior. Once this is live, the plan is to hit it directly (`mcp__Vercel__web_fetch_vercel_url`) to see which variant(s) actually return games, ship whichever one works as the real fix, then remove this debug endpoint and the earlier empty-result diagnostic logging together. Also a real `npx vitest run` (103/103, unchanged) and `next build` (clean compile). No migration, no new env vars.
- **Fixed: Full release catalogue now actually shows games — closes `ROADMAP.md`'s "Full release catalogue shows 'No results' for every platform," open since the feature originally shipped.** The debug endpoint above gave a conclusive, live-data answer within one deploy: every query variant that included `& category = 0` (IGDB's "main_game" filter, the whole reason the feature was restricted to it — see the original catalogue changelog entry) returned 0 games; every variant that dropped it returned real games instantly. A follow-up probe (`fields name,category`, no filter beyond `platforms`) confirmed why: real, legitimate PS2 games on file at IGDB mostly have no `category` value set at all — the field came back completely absent from the response, not `0` — so `category = 0` was comparing against a field that's usually unset, not "not main_game," and silently excluded almost everything on every platform rather than just DLC/bundles. IGDB apparently doesn't populate this field reliably enough to filter on. The real fix: dropped the `category` filter from `getPlatformCatalogue()`'s query entirely — `lib/igdbPlatformCatalogue.js` no longer has a way to reliably distinguish main games from DLC/bundles/ports at the query level, so this trades a small amount of theoretical noise for the feature actually working; in practice, 25 distinct real PS2 titles came back across two separate test batches with no visible DLC/bundle noise in either. Removed the temporary debug endpoint (`debugCatalogueQueries()`, and its `?debug=` route wiring) and the earlier empty-result diagnostic logging alongside the fix — both were explicitly scoped as temporary, and the bug they existed to help find is closed. Verified with a real `npx vitest run` (103/103, unchanged — this feature has never had unit tests, being a live external-API integration) and `next build` (clean compile). No migration, no new env vars — deploy only.
- **Fixed: the public profile header's new "Manage"/"More" dropdown didn't visibly open — flagged directly right after the header-declutter round above shipped.** Root cause: giving `.profile-header` a real `var(--card)` background (the fix above) kept its pre-existing `overflow: hidden` — originally there to clip the mosaic's square image corners to the header's rounded ones — but that same `overflow: hidden` also clips ANY absolutely-positioned child that extends past the header's box, regardless of z-index, and the new "Manage ▾"/"More ▾" dropdown does exactly that. So the button worked (state was toggling correctly) but the menu itself rendered invisible/cut off at the header's edge. Fixed by moving the corner-clipping onto `.profile-header-mosaic` itself (`border-radius: inherit; overflow: hidden;` on that div specifically) and removing `overflow: hidden` from `.profile-header`, so the mosaic's corners still round off correctly but the dropdown is free to render below the box like every other `ActionMenu` on the site already does. Verified this time with the dropdown's real `position: absolute` markup (not a static mock) rendered against the actual shipped CSS at both a 1400px desktop and 390px phone width — menu opens fully visible with all its items, mosaic corners still clipped correctly. Also a real `npx vitest run` (103/103, unchanged) and `next build` (clean compile). No migration, no new env vars — `app/globals.css` only.
- **Site-wide font-size consistency pass — flagged directly alongside the dropdown bug above: "we need to make sure theres consistency with font ad text across the whole app and size, if some text is gonna be a different size it has to makes sense."** Audited every `font-size` in the codebase — 199 raw declarations in `app/globals.css` plus ~56 hardcoded numeric inline `fontSize` values scattered across 22 JSX/JS component files — and found the real problem wasn't randomness so much as drift: 31 distinct pixel values had accumulated over many separate rounds of work, several of them near-duplicates one pixel apart (13 vs. 13.5 vs. 14, 18 vs. 19, and so on) with no consistent reason for the difference. Built a real type scale to fix it: 14 new CSS custom properties in `app/globals.css`'s `:root` (`--fs-3xs` at 9px up to `--fs-7xl` at 40px), each step chosen from the existing values themselves rather than an arbitrary new scale, so nothing had to change size by more than half a pixel to land on one. Every fractional (".5") value rounds to its nearest step (9.5→10, 10.5→11, 11.5→12, 12.5→13, 13.5→14, 14.5→15, 15.5→16); a handful of one-off whole-number outliers folded into the nearest established step too (17→18, 19→20, 21→22, 26→24, 28→30, 38→40). 164 of the 199 CSS declarations now reference `var(--fs-*)` instead of a hardcoded number; the other 32 were deliberately left as plain px because they're not text hierarchy at all — they size something else entirely (an avatar circle's diameter, a placeholder box's icon glyph, a podium medal, a hamburger icon glyph, a corner ownership/featured flag, a trading-card slab's stat-block labels, home-wall/collectible/leaderboard placeholder glyphs, the nav toggle, mosaic-tile placeholders) and coincidentally sharing a number with a text size would be a coincidence, not consistency. Converted all ~56 inline `fontSize: N` JSX values the same way, onto the same shared scale as strings like `fontSize: 'var(--fs-md)'` (works fine in React inline styles — resolves through the browser's normal CSS custom-property mechanism) — `ApiTokensClient.jsx`, `DashboardClient.jsx`, `InsightsClient.jsx`, `UpcomingReleasesClient.jsx`, `WrappedClient.jsx`, `LabelsClient.jsx`, `AppraisalClient.jsx`, `CatalogueClient.jsx`, `ProfileTabs.jsx`, `ProfileCard.jsx`, `PublicListCard.jsx`, `LandingPageShell.jsx`, `PasskeyManager.jsx`, `ImportCsvModal.jsx`, `CollectorSuggestionCard.jsx`, `BulkSearchAddModal.jsx`, `GameModal.jsx`, `SteamImportModal.jsx`, plus the `privacy`/`accessibility`/`whats-new`/`collectible`/home page routes. Left alone for the same component-intrinsic reasons as the CSS side: avatar-circle glyphs (`collectible/page.js`, `DashboardClient.jsx`, `PublicListCard.jsx`, `CollectorSuggestionCard.jsx`, `ProfileCard.jsx`), the two OG-image generators (`app/u/[username]/mosaic-image/route.js`, `app/opengraph-image.jsx` — these render to a static image, not the DOM, so they can't reference a CSS variable at all), `StarRating.jsx`'s `fontSize: size` (a caller-parameterized prop, not a fixed value), `ValueChart.jsx`'s SVG axis labels (`fontSize="11"`, an SVG attribute rather than a CSS property, unrelated mechanism), and `LeaderboardClient.jsx`'s `fontSize: '0.85em'` (intentionally relative to its parent, not an absolute size). Verified with a real `npx vitest run` (103/103, unchanged) and `next build` (clean compile, every route) in `/tmp/verify_build`, plus real visual checks rather than code review alone: a static-HTML mock built against the actual shipped `globals.css`, served over real local HTTP and Playwright-screenshotted, spot-checking all 6 whole-number-folded heading sizes side by side — all read cleanly with sensible hierarchy, no wrapping or overflow; and, separately, a genuine `next start` production server against the just-built output, screenshotting the live `/privacy` and `/accessibility` pages (both entirely converted to the new scale) end to end — both render with consistent, correctly-sized headings, body copy, and list text, no regressions. No migration, no new env vars — `app/globals.css` plus 22 component files, pure CSS-value and inline-style substitution, no markup or behavior changes.
- **Full release catalogue: not-owned entries are now clickable, and the page uses the full desktop width — both flagged directly.** Two real, unrelated issues, same page: (1) `CatalogueClient.jsx` never wired up the "click a missing entry to check eBay" behavior every other cover grid in the app already has (`SeriesGrid`'s `missing-clickable`/`onSelectMissing` pattern, used by `GameModal`/`ItemDetailModal`/`SeriesModal` for a missing series entry) — catalogue entries were always plain, inert `<div>`s regardless of ownership. Fixed by reusing the exact same pattern directly: a not-owned game is now a real `<button className="franchise-item missing-clickable">` that calls the same `openBestListingTab()` (`lib/externalListings.js`) every other missing-entry click already opens — eBay if it has listings for that title/platform, CeX otherwise. Needed threading a `currency` prop down from a new `profiles.currency` fetch in `app/dashboard/catalogue/page.js` (same `select('currency').eq('id', user.id).single()` pattern `app/dashboard/upcoming-releases/page.js`/`app/dashboard/appraisal/page.js` already use), since `openBestListingTab` needs it to pick the right eBay marketplace domain. Owned entries stay plain, inert `<div>`s — nothing to act on, same as `SeriesGrid`. (2) The page rendered as a single narrow column on desktop no matter how wide the screen was — not the `<main>`'s own `maxWidth: 820` (a leftover from an earlier version of this page, harmless on its own), but the `.form-card` wrapping the whole picker-and-grid: `.form-card` has a real `max-width: 440px` in `app/globals.css`, meant for actual narrow forms (login, settings), and this page never overrode it the way every other dashboard page with a wide `.form-card` panel already does (`ApiTokensClient.jsx`, `DashboardClient.jsx`, `LabelsClient.jsx` all set `maxWidth: 'none'` on theirs). Fixed the same way: dropped `<main>`'s `maxWidth: 820` (now the plain `.container` default, 1440px, same as the main collection grid) and added `maxWidth: 'none'` to the `.form-card`. Verified with a real static mock against the actual shipped CSS, Playwright-screenshotted at 1600px/1920px/390px — the grid now fills the container at up to 18 columns on a wide desktop screen instead of one, and the mobile layout is unchanged (still 4 columns, nothing regressed). Also a real `npx vitest run` (103/103, unchanged) and `next build` (clean compile) in `/tmp/verify_build`. No migration, no new env vars — `app/dashboard/catalogue/page.js` and `app/dashboard/catalogue/CatalogueClient.jsx` only.
- **Fixed: wishlisted (and sold) items were showing as owned/complete in the Full release catalogue and every "See master set"/"See full series" grid — flagged directly with a real example: "it says have pokemon xd even though its just wishlisted."** Root cause, in two functions that both had the identical bug: `lib/seriesLookup.js`'s `ownedKeysFor()` (feeds `SeriesGrid` everywhere it's used — `GameModal`, `ItemDetailModal`, `SeriesModal`) and `lib/platformCatalogueMatch.js`'s `ownedTitleKeysForPlatform()` (the catalogue) both matched purely on title/number, with no check on the item's actual `ownership` column at all — a wishlisted or already-sold row with a matching title counted exactly the same as a real owned one. Both functions are literally named "owned" and every other ownership-aware spot in the app (`lib/collectionCompare.js`, `lib/valueSnapshot.js`, `lib/mosaicData.js`) already enforces `ownership === 'owned'`; these two had just drifted from that rule since they only ever needed a title/number match before ownership status was relevant to get right. Added the same `ownership !== 'owned'` guard to both, plus `variantHintsFor()` (the trading-card variant-hint helper `ownedKeysFor` shares with the Pokémon/Magic master-set backends) — same bug, since a wishlisted card's variant was getting folded in as "evidence" of an owned print right alongside a real one. `app/dashboard/catalogue/page.js`'s query needed a matching change — it only ever selected `title, platforms`, never `ownership`, so there was nothing for `ownedTitleKeysForPlatform` to check even after the fix; now selects `ownership` too. Added 4 new test cases (a wishlisted item, a sold item, and a wishlisted trading-card variant, each confirmed to no longer count) — `npx vitest run` now 107/107 (was 103), plus a real `next build` (clean compile) in `/tmp/verify_build`. No migration, no new env vars — `lib/seriesLookup.js`, `lib/platformCatalogueMatch.js`, `app/dashboard/catalogue/page.js`, and both files' test suites.
- **Confirmed live: the `CRON_SECRET`-never-set incident (`ROADMAP.md`, previous entry) is now actually fixed in production.** After the env var was added on the Vercel side, a manually-triggered run of `refresh-upcoming-releases` completed successfully — real IGDB lookups instead of an instant 503, `cron_runs` recorded its first-ever row for any job, and `upcoming_release_cache` went from 0 rows to 12 real cached series, each with real dated entries. The other 7 crons haven't recorded a run yet, which is expected and not a problem: they're only due on their own staggered weekly/daily/monthly windows, not because anything's still broken — Vercel bakes env vars into whatever deployment is live, and the successful run above proves the current deployment already has the fixed value. Nothing to build here; noting it so the ROADMAP item can close.
- **Better IGDB franchise matching for oddly-spelled titles — found from real `refresh-upcoming-releases` cron log output pasted directly ("this is the log when i run it"), right after confirming the fix above.** The log showed several tracked titles failing to resolve a franchise/collection on IGDB even though they're real, well-known games. Live-tested each directly against IGDB's search and found two genuine, generalizable spelling patterns causing it: a camelCase-joined title typed with a space ("MegaMan X") only matches unofficial fan/ROM-hack titles on IGDB, while the correctly-spaced version ("Mega Man X") finds the real 1993 SNES game; a Pokémon version name typed as two words ("Pokemon Leaf Green") returns zero IGDB results at all, while IGDB's own one-word convention for these subtitles ("Pokemon LeafGreen") finds it immediately. `lib/igdbSearch.js`'s `getFranchiseGames()` (used by both the cron above and the "Series" section in `GameModal`) now tries the stored title first, same as before, and — only if that comes back with no usable franchise/collection tag — retries once against each alternate spelling `titleVariants()` generates (the camelCase-space-insert and, only for a 3+ word "Pokemon ..." title, the last-two-words merge), stopping at the first one that resolves. A variant can only ever help, never override an already-successful match on the stored title, since it's only tried after the primary lookup fails. Two other failure modes seen in the same log — a title with genuinely zero IGDB results under any spelling ("FIFA Football 2005", likely a US/UK region-name mismatch) and a title matching a real but wrong candidate among several similarly-named ones ("Lemmings" picking an obscure Apple IIGS port) — aren't fixed by this and are flagged as a known limitation in `ROADMAP.md` instead, since neither is a spelling problem a variant guess can safely bridge. Added `lib/igdbSearch.test.js` (new file, 7 tests) covering `titleVariants()`'s pure logic directly; `npx vitest run` now 114/114 (was 107), plus a real `next build` (clean compile) in `/tmp/verify_build`. No migration, no new env vars — `lib/igdbSearch.js` only.
